# Non-secret release metadata. Exact production SHA/URL are verified in the external receipt.
PROD_URL="https://parlay-lab-six.vercel.app"
PROD_ALIAS_DEPLOY="See release-verification.json; pending until its SHA matches HEAD."
PROD_ALIAS_VERIFIED="Pending matching release receipt."
LAST_SHIPPED="September 26: generator follow-up (0a6693c). A rolled-out ticket holds per request and the reveal lands on it. The exclude button is at the far left and the lock alone at the far right. Hold-and-drag reordering works on phone and desktop. The generator reaches 20 legs."
GATE_TSC="2026-09-26: TypeScript PASS (npx tsc --noEmit) and next build PASS, before the 0a6693c push."
GATE_TESTS="2026-09-26: full suite 3,768 tests, 3,666 pass, 102 fail = the pre-change baseline set by name. Zero new, zero fixed. New tests/gen-hold-drag.test.ts; pins updated in builder-speed-reveal, mobile-density, parlay-gen-reorder, parlay-gen-ui and parlay-gen. 300-case engine fingerprint differs only on the stacks+onePerGame failure code."
GATE_VERIFIED="2026-09-26 production /props, DOM-only headless Chrome, MLB only with paid endpoints blocked (none requested). Phone 390x844 touch at 4x CPU: 0 label changes after landing; exclude/lock 310 px apart; hold-drag follows the finger 1:1 under zoom 0.7 with 0 px page scroll and reorders; tap and swipe never reorder; picker 19 options; 20 legs after relax, landing about 2.7 s. Desktop: mouse drag reorders, 0 dialogs. /api/version answered 0a6693c."
KNOWN_RED="102 pre-existing full-suite failures (baseline set, unchanged by name). Football Generate was not exercised on production (can spend Odds credits). Physical iPhone not directly tested. Anchor + Kicker cannot build 12+ legs by its own rule, and says so."
