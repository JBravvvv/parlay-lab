# Pre-release alias proof; dated review records the new deployment after push.
PROD_URL="https://parlay-lab-six.vercel.app"
PROD_ALIAS_DEPLOY="parlay-mih5dl0t0-jbravvvvs-projects.vercel.app"
PROD_ALIAS_VERIFIED='2026-09-18: commit ade259c deployed as parlay-mih5dl0t0 (Ready, target production, created 12:57 PDT) and serving on the production alias; POST /api/mlb/hit-rates on the alias answers 200 {"season":2026,"players":{}} (the route did not exist before this build). Before it: 0819dcd as parlay-luqnxhok8 (pink brand), 504e241 as parlay-dwv67ilwt (backdrop), 3c7d288 as parlay-akv4q0mmx (INSTRUCTION 72).'
LAST_SHIPPED='UI overhaul (2026-09-18): parlay generator rebuilt as Caesars-tight chip rows — multi-select categories with the spread rule, odds Min/Max + two-thumb slider, hit-rate floor + L7..L120 window, games filter; Safer/Balanced mixes, Favorites/Even/Longshots presets and the DraftKings-only toggle removed (DK always); exclude is a 24px ghost; free-statsapi hit rates (HitChip/HitDots) on Props rows, generator slots and Board picks; muted/faint text, glass and scrim lifted for readability. Before it: brand retinted hot pink.'
GATE_TSC='2026-09-18: TypeScript PASS (npx tsc --noEmit, 0 errors) before the UI-overhaul push.'
GATE_TESTS='2026-09-18: 3379/3379 tests PASS across 220 files; serial full gate run alone before the UI-overhaul push (scratchpad gate-2026-09-18-ui.log; a first run was red on one test — the player-card statsapi host guard, which now reads the route together with the shared src/lib/mlb/player-index.ts module — and the whole gate was rerun alone after that test fix).'
GATE_VERIFIED='2026-09-18: multi-category pool, spread rule, hit-rate floor and games filter pinned by tests/parlay-gen-multi (captured MLB board + CFB synthetic props); hit-rate counting rules pinned by tests/prop-hit-rate; no engine edit (served-engine sha 52510d67 still MATCH).'
KNOWN_RED='None at release gate.'
