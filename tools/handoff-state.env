# Pre-release alias proof; dated review records the new deployment after push.
PROD_URL="https://parlay-lab-six.vercel.app"
PROD_ALIAS_DEPLOY="parlay-b0bqm39lg-jbravvvvs-projects.vercel.app"
PROD_ALIAS_VERIFIED='2026-09-19: commit 7474b6c deployed as parlay-b0bqm39lg (Ready, target production, created Sat Sep 19 2026 04:31:45 PDT), aliased to parlay-lab-six.vercel.app; the served board/builder/props/games chunks contain "md:max-h-none md:overflow-visible", "md:whitespace-normal", "grid gap-3 md:grid-cols-2 xl:grid-cols-3", "md:w-auto md:flex-wrap" and "md:w-[calc(33.333%-8px)]", the served stylesheet carries the @media (min-width:768px) block that wraps .chip-row and .carousel plus --text-display:clamp(1.5rem,2.4vw,2.25rem), and GET /api/version answers {"sha":"7474b6cae2047805547443bf6b1e8f0e757aa1e8"}.'
LAST_SHIPPED='Desktop density pass (2026-09-19, Josh: "On the web version of builder tab & every other tab, boxes need to be shrunk. the way you put the daily board on a horizontal scroll with no scroll is embarrassing & unacceptable. It all goes vertical & every box shrunk vertically"): measured on production first (DOM-only headless Chrome, no screenshots) — the football Builder ticket carousel held 3,508px of tickets in a 1,003px box with the scrollbar hidden, the football Board featured carousel and category chip-row, the /props game chips, the Games date rail and the Calc chips overflowed the same way, and at 1,100px the MLB Board table itself overflowed its box. From md (768px) nothing scrolls sideways: a min-width:768px block in globals.css wraps .chip-row and .carousel into rows, DataTable has no height cap and no overflow from md with wrapping text cells, the football TicketStack is a 1/2/3-column grid, featured cards fill thirds, and every strip (DateRail, GenSheet chips, RankedPicks tabs, the parlay filter) wraps. Every box shrunk vertically from sm/md: Panel p-4 with a py-2 header, PageHeader mb-4 under a display title capped at 36px, StatTile 20px figures, 36px selects and search fields, 44px generator slots, tighter section gaps on Board, Builder, football Board, Games, Stats, Ledger, Sharp, Simulator, Settings, Calc and Ballpark. Phones keep the 2026-09-19 phone-density shape. No budget, cap, rail, schedule or plan changed.'
GATE_TSC='2026-09-19: TypeScript PASS (npx tsc --noEmit, exit 0) before the desktop-density push.'
GATE_TESTS='2026-09-19: 3567/3567 tests PASS across 232 files; full serial gate (npx vitest run --no-file-parallelism) run alone before the push (scratchpad gate-2026-09-19-desktop.log).'
GATE_VERIFIED='2026-09-19: tests/desktop-density (new, 13) pins the min-width:768px wrap block, a DataTable SSR render (open box from md, --dt-max-h:62vh below, text cells wrap from md, fit/numeric cells nowrap, md:py-1 rows), the grid TicketStack with no carousel, every wrapped strip and every shrunk surface/tab literal; pins updated in lockstep in board-compact, mobile-density, cfb-picks-ui, cfb-props-ui, games, cfb-card-ui and cfb-builder-ui (its carousel pins are now grid pins).'
KNOWN_RED='None at release gate.'
