# Non-secret release metadata. Exact production SHA/URL are verified in the external receipt.
PROD_URL="https://parlay-lab-six.vercel.app"
PROD_ALIAS_DEPLOY="See release-verification.json; pending until its SHA matches HEAD."
PROD_ALIAS_VERIFIED="Pending matching release receipt."
LAST_SHIPPED="September 26: Parlay Builder speed (formatter cache + once-per-pool generator setup, output identical on 300 seeded cases), instant half-hour game-start slider starting 9am PT, Customize Your Picks rail removed, Season Lab off the side list, slot-machine reveal on Generate."
GATE_TSC="2026-09-26: TypeScript PASS (npx tsc --noEmit) and next build PASS with no warnings, before the 56fd656 push."
GATE_TESTS="2026-09-26: full suite 3,732 tests, 3,630 pass, 102 fail = the pre-change baseline set by name (two nav titles renamed, same pre-existing causes). Zero new failures. New tests/builder-speed-reveal.test.ts plus updated game-time-window, nav, cfb-season-ui, parlay-gen-ui pins all pass."
GATE_VERIFIED="2026-09-26 production /props, DOM-only headless Chrome at 4x CPU: slider ticks 6-27 ms (were 45-99 s); Generate paints in about 190 ms with 4 reels and odds shown as dots, settling to +1332 with no reels; both game-start tracks 9-24 step 0.5, 209/240 px wide; no /season link, no category rail. /api/version answered 56fd656."
KNOWN_RED="102 pre-existing full-suite failures (baseline set, unchanged by name). The single-strategy generate path is still 62-167 ms desktop on a 6,030-leg pool (seeded mixOrder shuffle). Physical iPhone not directly tested."
