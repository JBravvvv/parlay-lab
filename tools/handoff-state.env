# Pre-release alias proof; dated review records the new deployment after push.
PROD_URL="https://parlay-lab-six.vercel.app"
PROD_ALIAS_DEPLOY="parlay-3thqonanw-jbravvvvs-projects.vercel.app"
PROD_ALIAS_VERIFIED='2026-09-18 (evening): commit 95adbd1 deployed as parlay-3thqonanw (Ready, target production, created 19:45 PDT) and serving on the production alias (alias /props HTML answers 200 and carries the compact generator markup: a native Legs select and no THE PARLAY LAB eyebrow). No headless-Chrome screenshot run this pass — Josh declined the local screenshot run earlier today; the live fallback, the HR rule, the controlled ranked filter and the compaction are pinned by tests/hr-line-rule, tests/mlb-market-phases, tests/ranked-picks and tests/parlay-gen-ui, and the ranked chips were verified working on the alias by a DOM-only CDP check (NFL Spread 28 rows, MLB RL 33 rows).'
LAST_SHIPPED='Live legs from the board own in-play rows + HR line rule + rail-driven ranked list + compact generator (2026-09-18, evening): liveMarketBoard projects a live:true row as a live leg when the statsapi game is priceable and the row settle-book bookQuotes[*].at is within 30 minutes (quoteAt stamped, pO nulled, fO kept; overlay wins; live:false started games stay out); phase-empty carries {pregame, live} and the sheet names the missing side + last refresh; hrLineAllowed/pruneHrLines keep HR above 0.5 out unless live with the homer in the boxscore or under the browse-only Show O1.5 HR chip (props browse, both live sources, Board ALL scope); RankedPicks controlled filter driven by the MLB rail/tab/generator category taps, ranked pool over mixed; GenSheet compacted (one-line hero, Legs/Sides/Timing + hit floor/window as native selects, tooltips, Save/Load under Advanced).'
GATE_TSC='2026-09-18 (evening): TypeScript PASS (npx tsc --noEmit, 0 errors) before the live-legs push.'
GATE_TESTS='2026-09-18 (evening): 3469/3469 tests PASS across 227 files; serial full gate run alone before the live-legs push (scratchpad gate-2026-09-18-four.log), green on the first run.'
GATE_VERIFIED='2026-09-18 (evening): tests/hr-line-rule pins the HR predicate, pregame pruning with and without the manual filter, the live tally exception and where the rule is wired; tests/mlb-market-phases pins the board-row live fallback (own quote, pO null, overlay precedence, stale/future/missing timestamps, post-refresh starts excluded, decided lines, mixed from board rows, phase-empty counts); tests/parlay-gen-ui pins the compact sheet (selects, tooltips not text, per-side phase-empty copy, Save/Load under Advanced, dark selects); tests/ranked-picks pins the controlled filter and the rail wiring.'
KNOWN_RED='None at release gate.'
