# Pre-release alias proof; dated review records the new deployment after push.
PROD_URL="https://parlay-lab-six.vercel.app"
PROD_ALIAS_DEPLOY="parlay-6b8ukxd5q-jbravvvvs-projects.vercel.app"
PROD_ALIAS_VERIFIED='2026-09-18: commit f731863 deployed as parlay-6b8ukxd5q (Ready, target production, created 17:38 PDT) and serving on the production alias (alias HTML answers 200 with the cerulean theme-color #1e2126). No headless-Chrome smoke this pass — Josh declined the local screenshot run; the theme, board and lean surfaces are pinned by tests/theme-cerulean, tests/board-compact and tests/prop-lean. Before it: 1c9c990 as parlay-evq9gozv4 (eight-item UI pass), ade259c as parlay-mih5dl0t0 (UI overhaul).'
LAST_SHIPPED='Cerulean on graphite + compact Board + prop lean (2026-09-18, later the same day): whole theme rethemed in globals.css with token names kept (graphite hsl(216 9% 13%) base, cerulean #3ab0e8 pos/brand, lavender live, gold + NFL/CFB accents unchanged, llama footage hue-rotated under a 62% scrim, Board tab periwinkle); DataTable fit/headerTitle columns + px-2 cells + a per-row "why" toggle on SettledGrade so both boards fit without a horizontal scroll; every priced player prop shows a price-implied market lean (LeanChip, de-vigged DK two-way price, never labelled bet % / money % because no public prop splits exist) on Props, Board and the ranked picks of every desk. Before it: eight-item UI pass (NFL first, splits, ranked S→F, generator reorder).'
GATE_TSC='2026-09-18: TypeScript PASS (npx tsc --noEmit, 0 errors) before the cerulean push.'
GATE_TESTS='2026-09-18: 3449/3449 tests PASS across 226 files; serial full gate run alone before the cerulean push (scratchpad gate-2026-09-18-three.log), green on the first run.'
GATE_VERIFIED='2026-09-18: tests/theme-cerulean forbids any pink literal and any new green in globals.css and pins the graphite/cerulean tokens, manifest, layout, VideoBackdrop filter and AppShell Board tone; tests/board-compact pins DataTable fit/px-2, both boards fit columns, the SettledGrade why toggle and the compact LiveGrade; tests/prop-lean pins the de-vig arithmetic, MLB/football lean rows, the LeanChip render + tooltip and every wiring site; no engine edit (served-engine sha 52510d67 still MATCH).'
KNOWN_RED='None at release gate.'
