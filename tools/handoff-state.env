# Pre-release alias proof; dated review records the new deployment after push.
PROD_URL="https://parlay-lab-six.vercel.app"
PROD_ALIAS_DEPLOY="parlay-mudy3bhud-jbravvvvs-projects.vercel.app"
PROD_ALIAS_VERIFIED='2026-09-19: commit 24dbb55 deployed as parlay-mudy3bhud (Ready, target production, created Sat Sep 19 2026 02:34:44 PDT), aliased to parlay-lab-six.vercel.app; the served MLB board chunk contains "live=1&force=1", the football board chunk contains "cfb-board-updated", "board re-pulled" and "refresh", and GET /api/version answers {"sha":"24dbb55006cccae4eb427fa04cd373736a67713c"} with cache-control: no-store.'
LAST_SHIPPED='Board refresh pass (2026-09-19, Josh: "The CFB & NFL boards should function the same way as the MLB one does. It should show the time of last board refresh. MLB should also do a FULL refresh every single time i refresh."): football Refresh Board with the sync phrase = a FULL re-pull (?refresh=1 + x-pl-sync on /api/cfb, /api/nfl and both props routes — odds cache bypassed, fresh-board rail skipped, every selected game with rows re-priced under the unchanged budget rails, answer stored, refreshed:true; the flag is ignored without the phrase) + CfbBoardStamp/NflBoardStamp "updated h:mm" in both football headers (newest ACTIVE board, props generatedAt else slate); MLB tap = /api/generate?live=1&force=1 on every tap (manualReprice = force && boardOnly: outside the 45-min limiter and the run cap, tallied on pl:gen:manual:<date>, never counted against the card ladder, K_LASTGEN untouched, card untouched), the stored board adopted on the device before invalidation, refillRepricedBoard the one early return; installed-app self-update (NEXT_PUBLIC_BUILD_SHA vs /api/version in SwRegister, reload once per new build on open and foreground).'
GATE_TSC='2026-09-19: TypeScript PASS (npx tsc --noEmit, exit 0) before the board-refresh push.'
GATE_TESTS='2026-09-19: 3541/3541 tests PASS across 230 files; full serial gate (npx vitest run --no-file-parallelism) run alone before the push (scratchpad gate-2026-09-19-refresh.log).'
GATE_VERIFIED='2026-09-19: tests/board-refresh-full (new, 17) runs the real football props body on the 2026-09-05 fixtures — a fresh stored board served from redis with no flag and with an unauthed ?refresh=1, a FULL re-pull with the phrase (every selected game fetched no-store, slate pulled { fresh: true }, board stored, refreshed:true, key never in the body), the EMPTY-EVENT RULE surviving a refresh, the budget rail refusing a spent day — plus boardStampOf over a seeded QueryClient (inactive boards ignored, props before slate), SSR renders of both stamps, GET /api/version, and source pins on the routes, clients, pill, generate route (manualReprice, K_MANUAL, one K_LASTGEN SET, two forced spreads), live-board-client and SwRegister; live-board-only, board-refresh, board-settled, cfb-picks-ui, cfb-props, calibration-window and refill-helper re-pinned.'
KNOWN_RED='None at release gate.'
