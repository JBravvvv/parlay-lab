# Pre-release alias proof; dated review records the new deployment after push.
PROD_URL="https://parlay-lab-six.vercel.app"
PROD_ALIAS_DEPLOY="parlay-evq9gozv4-jbravvvvs-projects.vercel.app"
PROD_ALIAS_VERIFIED='2026-09-18: commit 1c9c990 deployed as parlay-evq9gozv4 (Ready, target production, created 15:07 PDT) and serving on the production alias; GET /api/splits?league=nfl on the alias answers 200 with 16 games (the route was 404 on the alias before this build). Headless-Chrome smoke on the alias NFL desk: /props ranked list 1135 picks S-first with category counts, 4 numbered draggable lock-in slots; /games 84 odds cells each with a grade badge and a splits chip; /board and /builder splits chips; /stats 53 headshots; zero page errors. Before it: ade259c as parlay-mih5dl0t0 (UI overhaul), 0819dcd as parlay-luqnxhok8 (pink brand).'
LAST_SHIPPED='Eight-item UI pass (2026-09-18): sport order NFL/CFB/MLB; the wordmark links /games; bet % / money % splits from the public scoresandodds consensus page via /api/splits + SplitsChip on every side pick (no player-prop splits exist publicly); logos drawn raw (no black disc) and Stats headshots; a grade badge on every OddsGrid cell and MLB ML rows; Board/Builder pick boxes compacted; the generator stacked with numbered, drag/▲▼-reorderable Lock-in slots; RankedPicks (every pick today S→F with category chips) as the default view under the generator on all desks. Before it: UI overhaul (generator chip rows + hit rates).'
GATE_TSC='2026-09-18: TypeScript PASS (npx tsc --noEmit, 0 errors) before the eight-item push.'
GATE_TESTS='2026-09-18: 3416/3416 tests PASS across 223 files; serial full gate run alone before the eight-item push (scratchpad gate-2026-09-18-eight-2.log; the first run, gate-2026-09-18-eight.log, was red on one test — nfl-board-ui pins the featured pick price class literal "hero-price is-nfl num mt-0.5", which the compaction had trimmed — the literal was restored and the whole gate rerun alone).'
GATE_VERIFIED='2026-09-18: consensus parsing + side lookup pinned by tests/splits (saved copies of the three consensus pages); pin-travels-with-seat and the numbered/draggable slots pinned by tests/parlay-gen-reorder; S→F ordering, chips and default view pinned by tests/ranked-picks; no engine edit (served-engine sha 52510d67 still MATCH).'
KNOWN_RED='None at release gate. Observed once on the alias /board (NFL desk, headless Chrome): a minified React #418 hydration text mismatch that did not reproduce on the next load and left the page fully rendered — not attributed to this ship; watch for it.'
