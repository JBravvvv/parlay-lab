# Pre-release alias proof; dated review records the new deployment after push.
PROD_URL="https://parlay-lab-six.vercel.app"
PROD_ALIAS_DEPLOY="parlay-7lozeyhqh-jbravvvvs-projects.vercel.app"
PROD_ALIAS_VERIFIED='2026-09-18 (late evening): commit da40781 deployed as parlay-7lozeyhqh (Ready, target production, created Fri Sep 18 2026 20:27:53 PDT), aliased to parlay-lab-six.vercel.app; the served props chunk contains "Two legs from one team" and "Allow two legs from one team".'
LAST_SHIPPED='One leg per team in the parlay generator (2026-09-18, late evening): GenSpec.onePerTeam (undefined = off, both desks default ON, seed t1 only when on) enforced in seating, the price lookahead, the mixed/spread checks via one blocks() helper, payout repair, pinned conflicts (same-team) and an exact capacity with the club as the seat-owning node; relaxHint names same-game before same-team; the sheet has a "Two legs from one team" Toggle under "Two legs from one game" plus the "Allow two legs from one team" one-tap fix and a stacked-club note; recipes saved before the rule load with it ON.'
GATE_TSC='2026-09-18 (late evening): TypeScript PASS (npx tsc --noEmit, exit 0) before the one-per-team push.'
GATE_TESTS='2026-09-18 (late evening): 3491/3491 tests PASS across 228 files; full serial gate (npx vitest run --no-file-parallelism) run alone before the push (scratchpad gate-2026-09-18-five.log).'
GATE_VERIFIED='2026-09-18 (late evening): tests/parlay-gen-team (new, 17) pins the rule, the exact capacity under both rules, the doubleheader lookahead, pins, the relax order and the sheet; tests/football-gen +4 (untagged legs never blocked, two tagged clubs); tests/parlay-gen-setup +1 (round-trip, legacy recipe loads ON).'
KNOWN_RED='None at release gate.'
