# Pre-release alias proof; dated review records the new deployment after push.
PROD_URL="https://parlay-lab-six.vercel.app"
PROD_ALIAS_DEPLOY="parlay-iw3y2nyx4-jbravvvvs-projects.vercel.app"
PROD_ALIAS_VERIFIED='2026-09-19: commit a21cc03 deployed as parlay-iw3y2nyx4 (Ready, target production, created Sat Sep 19 2026 03:20:34 PDT), aliased to parlay-lab-six.vercel.app; the served props chunk contains "ranked-odds-min", "ranked-sort", "Shortest price first" and "Player props did not load", and GET /api/version answers {"sha":"a21cc035efb835635acf526e7a2d00c0f14a9a04"}.'
LAST_SHIPPED='Ranked-list odds range + price sort pass (2026-09-19, Josh: "Need to be able to sort Every pick today underneath parlay builder by odds … filter between -200 to +250 for players or whatever other odds I want" / "Prop bets are not loading for CFP games that start in 6 hours 15 minutes"): RankedPicks gained an Odds min/max row (American prices compared numerically, open bound = no bound, chip counts follow the range, header names it, clear ×) and a sort select (Grade S → F / Shortest price first / Longest price first) on every desk, plus a "Still pricing this slate" note; both football props routes declare maxDuration 300 (they ran on the platform default and a cold 60-event pull could be killed mid-pull); the football ranked view shows a failed props pull with Retry. No budget, cap, or schedule changed — the Tuesday/Wednesday pre-pricing and the 60-event cap are open spend decisions; the Odds API account showed 843/20,000 credits left at 02:47 PDT.'
GATE_TSC='2026-09-19: TypeScript PASS (npx tsc --noEmit, exit 0) before the ranked-list push.'
GATE_TESTS='2026-09-19: 3554/3554 tests PASS across 231 files; full serial gate (npx vitest run --no-file-parallelism) run alone before the push (scratchpad gate-2026-09-19-ranked.log).'
GATE_VERIFIED='2026-09-19: tests/ranked-picks (+9) renders the list with a -200..+250 range under the Anytime TD chip (rows S down inside it, chip counts narrowed, header named, inputs carry the bounds), both price sorts, the default open controls, the empty-range text, the still-pricing note and the CfbProps error wiring, and unit-tests inOddsRange / rangeText / sortRanked; tests/football-props-duration (new, 4) pins maxDuration 300 on both football props routes and the arithmetic behind it; tests/nfl-props-route + tests/cfb-props export-count pins updated to two.'
KNOWN_RED='None at release gate.'
