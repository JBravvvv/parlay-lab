# Facts that cannot be read out of the repo. Updated BY THE SESSION THAT
# VERIFIED THEM, then picked up by tools/sync-handoff.sh into 01-STATE.md.
# Keep every value dated AND name the commit it was verified against — an
# undated claim here is worse than no claim.
PROD_URL="https://parlay-lab-six.vercel.app"
PROD_ALIAS_DEPLOY="parlay-jzuzbm5lv-jbravvvvs-projects.vercel.app"
PROD_ALIAS_VERIFIED="2026-09-12 15:22 PDT, commit 8705291 — deployment created 15:13:41 PDT, three seconds after the commit, so the push is what built it; Ready, target production; `vercel inspect` shows https://parlay-lab-six.vercel.app aliased to it; / /board /props /builder /sharp all 200. Verify on the ALIAS: a parlay-<id>.vercel.app deployment URL sits behind Vercel auth and returns a login page, which reads as an empty body if you grep it."
LAST_SHIPPED="INSTRUCTION 53 — the handoff folder became a generated mirror: tools/sync-handoff.sh + FOUR git hooks (post-commit, post-merge, post-checkout, post-rewrite; a fifth on post-index-change was tried and removed), a coalescing mkdir lock, and PASTE-THIS.md — the whole handoff as one ~13k-token file for a chat that cannot be handed the 128MB folder. Commits 54e9676, 8dc38de, 8705291, then the lock fix. Before it: INSTRUCTION 52 — in-game live lines restored on all three desks and the Parlay Generator ported to CFB + NFL (67a7d3c, review round f6e996b, docs 6fadc68 / c2f76fe)."
GATE_TSC="0 errors"
GATE_TESTS="3,192 of 3,193 passing"
GATE_VERIFIED="2026-09-12 15:26 PDT, run alone on a quiet tree — the tree this commit publishes (the coalescing-lock change). Identical result to the three gates before it."
KNOWN_RED="tests/workflow-branch-sync.test.ts:250 — seven workflow waivers expired (since: 2026-08-29, 14-day limit). Pre-existing, time-triggered, awaiting Josh. DO NOT RE-DATE."
