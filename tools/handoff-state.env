# Pre-release alias proof; dated review records the new deployment after push.
PROD_URL="https://parlay-lab-six.vercel.app"
PROD_ALIAS_DEPLOY="parlay-4a9npa21b-jbravvvvs-projects.vercel.app"
PROD_ALIAS_VERIFIED='2026-09-19: commit 943d804 deployed as parlay-4a9npa21b (Ready, target production, created Sat Sep 19 2026 01:31:37 PDT), aliased to parlay-lab-six.vercel.app; the served shell chunk contains "shell-more-menu", "rotate-lock" and "Parlay Lab runs in portrait", the board chunk contains "board-market-select", and /manifest.webmanifest carries "orientation": "portrait".'
LAST_SHIPPED='Phone density pass (2026-09-19): the four non-tab header icons became one ⋯ More menu (Settings keeps its gear; MORE derives from NAV), header ground bg-bg/92; portrait lock (manifest orientation + guarded screen.orientation.lock in standalone + a CSS-only "Parlay Lab runs in portrait" sheet under standalone+landscape+max-height 500px — the sheet IS the iOS behaviour); Stats chips in one strip with the selects behind a Filters button; Board market pills become a native select on the phone and the # column is hideBelowSm; Builder tickets are one line with a ▾ drawer and the refused list folds; generator slots 52px → 36px with the hero hidden; Panel/Pill/PageHeader/PaperBanner/SportsbookSelector a size down below 640px. Desktop unchanged (mobile-first Tailwind, sm: restores the desktop shape).'
GATE_TSC='2026-09-19: TypeScript PASS (npx tsc --noEmit, exit 0) before the phone-density push.'
GATE_TESTS='2026-09-19: 3521/3521 tests PASS across 229 files; full serial gate (npx vitest run --no-file-parallelism) run alone before the push (scratchpad gate-2026-09-19-mobile.log).'
GATE_VERIFIED='2026-09-19: tests/mobile-density (new, 30) pins the manifest orientation, the lock effect, the rotate sheet + its media query, the More menu, PageHeader subMobile, Panel/Pill/PaperBanner/SportsbookSelector phone sizes, DataTable hideBelowSm, the Stats Filters fold, the Board market select + pills + rank column, the Builder ticket drawer + blocked fold + grids, and the GenSheet 36px slot; tests/nav rewritten for the More menu (press count 3 → 4).'
KNOWN_RED='None at release gate.'
