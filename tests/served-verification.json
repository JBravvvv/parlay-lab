{
  "_": "SERVED-ARTIFACT VERIFICATION STATE (2026-07-30, owner's item 3). Test-owned: no runtime code reads this file. `pending` is set TRUE by an engine ship (the served artifact cannot carry the new string until that ship deploys), and set FALSE by the post-deploy re-grep, which also updates SERVED_ENGINE_SHA_VERIFIED in src/lib/engine-echo.ts. The resolution guard in tests/engine-echo.test.ts FAILS THE BUILD once a pending marker is older than MAX_PENDING_H hours — so the exception cannot become permanent by default.",
  "pending": true,
  "since": "2026-08-03T00:35:00Z",
  "note": "PENDING — the 2026-08-02 singles-vintage ship (owner's word): 2-leg floor configurable behind SH_CFG.singlesOn (ships FALSE), per-type singles builder behind the same flag, per-group simJoint j2/pm emission behind SH_CFG.sjEmit (ships FALSE). Engine string 281,096 -> 283,305 chars, sha b862b2b2... -> 87c5dd17b2f25b8e9590ffbdc2b0dbe595abcf185bb97b85ef193b73e7ee2583 (an intermediate 06ddf821/282,894 existed for minutes mid-build: the singles-vintage guard caught the singles builder consuming player caps ahead of the mixed set — flag-ON removed a parlay, 94->93 — and the builder moved after the entire set before anything was committed). BOTH FLAGS SHIP OFF, so served behaviour is expected byte-equivalent on the board path; the pinned parity/armed baselines remain valid and the flips are their own later commit with baseline re-derivation. Resolve within 24h by the post-deploy re-grep of the served chunk."
}
