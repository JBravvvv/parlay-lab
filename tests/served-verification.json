{
  "_": "SERVED-ARTIFACT VERIFICATION STATE (2026-07-30, owner's item 3). Test-owned: no runtime code reads this file. `pending` is set TRUE by an engine ship (the served artifact cannot carry the new string until that ship deploys), and set FALSE by the post-deploy re-grep, which also updates SERVED_ENGINE_SHA_VERIFIED in src/lib/engine-echo.ts. The resolution guard in tests/engine-echo.test.ts FAILS THE BUILD once a pending marker is older than MAX_PENDING_H hours — so the exception cannot become permanent by default.",
  "pending": false,
  "since": "2026-08-03T02:31:00Z",
  "note": "RESOLVED 2026-08-03T02:31Z, ~16 minutes after the ship. Post-deploy re-grep via tools/verify-served-engine.mjs against a LIVE fetch of chunk 256-2c7681954a0ee8e7.js (renamed from 256-9af597fd7617fa86.js - deploy evidence): engine string BYTE-IDENTICAL to the repo, 283,396 chars, sha256 39fc86814eb4e8be561278b48822810ee77f33ff822510a8b8f5c530c9acc0ce. THE SINGLES FLIP IS LIVE: singlesOn:true and sjEmit:true each grep exactly once in the SERVED bytes, so the flip is verified in production and not merely in the repo. Baselines generated beside the retired ones (baseline43 scoped to singlesOn:false for its legacy provenance; baseline44-singles + baseline-armed-v2-singles generated for the shipped config); board digests re-cut to 4151ad2522d400bcf679cd8b6155a431 (dormant) and fd73ad6d2d934dce7cf08978ad68cfd1 (armed) using clamp-instrumentation's own boardHash after a reimplemented one disagreed."
}
