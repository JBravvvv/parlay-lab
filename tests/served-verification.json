{
  "_": "SERVED-ARTIFACT VERIFICATION STATE (2026-07-30, owner's item 3). Test-owned: no runtime code reads this file. `pending` is set TRUE by an engine ship (the served artifact cannot carry the new string until that ship deploys), and set FALSE by the post-deploy re-grep, which also updates SERVED_ENGINE_SHA_VERIFIED in src/lib/engine-echo.ts. The resolution guard in tests/engine-echo.test.ts FAILS THE BUILD once a pending marker is older than MAX_PENDING_H hours — so the exception cannot become permanent by default.",
  "pending": false,
  "since": "2026-08-03T00:47:00Z",
  "note": "RESOLVED 2026-08-03T00:47Z — 12 minutes after the ship. Post-deploy re-grep via tools/verify-served-engine.mjs (double-anchor extractor): served chunk 256-9af597fd7617fa86.js (renamed from 256-7cc559a830020345.js, itself evidence the deploy landed) carries an engine string BYTE-IDENTICAL to the repo, 283,305 chars, sha256 87c5dd17b2f25b8e9590ffbdc2b0dbe595abcf185bb97b85ef193b73e7ee2583. The 2026-08-02 singles-vintage ship: floor configurable behind SH_CFG.singlesOn (FALSE), per-type singles builder behind the same flag, per-group simJoint j2/pm behind SH_CFG.sjEmit (FALSE) — both flags OFF in the served bytes, pinned parity/armed baselines valid, flips are their own later commit with baseline re-derivation. (Build note preserved: an intermediate 06ddf821/282,894 existed for minutes — the singles-vintage guard caught the builder consuming player caps ahead of the mixed set, flag-ON removed a parlay 94->93, placement moved before commit.) A stale /tmp/chunk.js first made the verifier throw ANCHOR-1 MISSING; the tool takes --chunk and the live fetch resolved it — a reminder that the default path is a foot-gun, not a defect in the extractor."
}
