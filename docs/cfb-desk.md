# CFB desk — pricing, storage, and separation reference (written 2026-09-05)

Companion to INSTRUCTION 38 in `session-handoff.md`. Every constant below is the value in `src/lib/cfb/rules.ts` on the day this was written; every prod figure was read from `https://parlay-lab-six.vercel.app/api/cfb` that day; figures that come from the test fixtures say so in the same line.

## Shape — one shell, two desks
- `src/lib/sport.ts` holds the switch: `Sport = "mlb" | "cfb"` (since 2026-09-08 `"mlb" | "cfb" | "nfl"` — the NFL desk shares these football surfaces through a LeagueContext, see `docs/nfl-desk.md` and the "Allocation widened" section below), localStorage key `pl_sport`, window event `pl:sport`, `useSport()` through `useSyncExternalStore` with a server snapshot of `mlb` (SSR always renders MLB; the client flips on mount). The `SportSwitch` pill sits under the brand in the rail and in the phone header.
- Every page (`app/{games,stats,board,builder,props,ledger,sharp,settings}/page.tsx`) calls `useSport()` after its own hooks and early-returns its College Football surface with `PageHeader eyebrow="College Football"`. The MLB hooks still mount behind the CFB desk (accepted: cached and cheap); the Games page disables the MLB schedule query while CFB is showing.
- `CFB_ENABLED` in `src/lib/features.ts` is `true`; flipping it hides the switch and the desk.
- The Stats page keeps its own pill under `pl_stats_sport` (it used to share `pl_sport`; a legacy JSON-quoted value under `pl_sport` fails `isSport` and reads as `mlb`). The NCAAF pill on that page opens the CFB desk's FPI panel.

## Feeds and the route
| feed | constant | cache | on failure |
|---|---|---|---|
| ESPN scoreboard for one calendar date — the route fetches the slate date AND the next date so a Friday/Saturday hand-off never shows an empty board | `CFB_ESPN_SCOREBOARD` | `revalidate: 60` | `/api/cfb` answers 502 |
| ESPN FPI power index (`limit=400`) | `CFB_ESPN_FPI` | `revalidate: 21600` | FPI part dropped from the blend, `fpiUpdated: null` |
| The Odds API `americanfootball_ncaaf`, regions `us,eu`, markets `h2h,spreads,totals`, american odds | `CFB_ODDS_URL` | `revalidate: 240` | `oddsMissing: true`, scores-only board, no tickets |

- One fresh odds pull costs 6 credits (3 markets × 2 regions): the prod read on 2026-09-05 moved the quota from 17578 to 17572 remaining. The key is `ODDS_API_KEY`, server only. Local dev has no key, so `/api/cfb` returns `oddsMissing: true` locally — that is the environment, not a defect; price checks happen on prod. Top-ups (INSTRUCTION 48, 2026-09-09): ≤6 attempts per arm per date (`CFB_TOPUP_MAX`, was 2), 6 credits each attempt that reaches pricing, so ≤42 lines credits per date incl. the lock (6 + 6 × 6 — arms only close, so the per-arm counters share CFB_TOPUP_MAX priced boards rather than each buying six; a fix-round draft said 78). WHEN they run (INSTRUCTION 49, 2026-09-09): not every tick — only on the first scheduler tick after 08:00 / 09:30 / 12:00 / 15:00 / 16:45 PT (`decideRefillTick`, `REFILL_SLOTS_PT` = `GRADE_SLOTS_PT`) or on Josh's own Refresh (`POST /api/refill?desk=cfb` → `/api/cfb/lock?manual=1`, the same pass with slot `manual`); `CFB_TOPUP_RETRY_MS` is 0 (the slot calendar is the only pacing), the slot is stamped on the claim row and a same-slot repeat is refused free, and a non-slot tick makes zero odds calls and no top-up ESPN read (the keyless sweep/settle reads still run) — slot-only ≤ 5 attempts = 30 lines credits; 6 with one manual = 36; 42 including the lock's own 6-credit pull. Enforcement of "never remove a pick" on this desk lives in `/api/cfb/ledger`'s PUT (`restoreShrunkDays`), not in `applyTopUp`, which only ever appends.
- `/api/cfb` (`app/api/cfb/route.ts`, `force-dynamic`, `Cache-Control: no-store`): `?date=YYYY-MM-DD` (default PT today), `&bankroll=` (Kelly base), `&mode=board|finals`. Body = `CfbSlate` (`src/lib/cfb/types.ts`): the board plus `finals`, `quota {remaining, used}` and `oddsMissing`; the quota also rides in response headers.
- Client: `src/lib/cfb/client.ts` — `loadCfbSlate(date, {bankroll})`, `loadCfbFinals(date)`, query key `["cfb","slate",date,bankroll]`, stale 240 s. Board, Builder and Parlay Builder share `useCfbDesk()` (exported from `CfbBoard.tsx`): the query is keyed on a concrete date, waits for the bankroll, and auto-advances to the next slate date when the chosen day has no games.

## Model constants (`CFB_MODEL`)
| constant | value | meaning |
|---|---|---|
| `sigma` | 16.5 | standard deviation of the final margin, points |
| `sigmaTotal` | 18 | standard deviation of the total |
| `hfa` | 2.6 | home-field points added to the FPI margin; 0 at neutral sites |
| `blend` | mkt 0.6 · spread 0.25 · fpi 0.15 | win-probability blend, renormalised over the parts that exist |
| `spreadBlend` | mkt 0.75 · fpi 0.25 | margin blend |
| `pinnacleWeight` | 2 | Pinnacle counts twice in the weighted median |
| `minBooks` | 2 | a market with fewer books is null |
| `settleBook` | `williamhill_us` | Caesars — the only book tickets are written at |
| `matchWindowMs` | 3 h | ESPN kickoff vs odds `commence_time` tolerance |

## Pricing pipeline (`src/lib/cfb/model.ts`, `buildCfbBoard`)
1. Per book, devig the two sides proportionally (`devigProportional`).
2. Per book, turn the spread into an implied margin, μ_b = −s_b + σ·Φ⁻¹(pCover), and the total into an implied mean, T_b + σ_T·Φ⁻¹(pOver). Φ⁻¹ is Acklam with one Halley step, Φ is Hart/West (`src/lib/cfb/normal.ts`).
3. Consensus per market = weighted median across books (Pinnacle ×2); null under `minBooks`.
4. `pHome` = blend of {devigged moneyline, spread-implied, FPI-implied} over the parts that exist. `muMargin` = `spreadBlend` of {market margin, FPI margin}, or σ·Φ⁻¹(pHome) when only a moneyline exists. FPI margin = fpiHome − fpiAway + hfa.
5. Each `CfbRow` prices its side at the consensus line (`fair`, `push`, `fairAm`), then quotes every book at the book's OWN line with EV re-evaluated there — that is why a Games card can show `-115@54.5` against a consensus 55. `cz` = Caesars, `best` = highest decimal at the consensus line, `dk` / `fd` / `pin` for reference.
6. `evCz` / `evBest` are percentages; the grade (S…F) comes from `evCz` through the shared `gradeFromEv` in `src/lib/grade.ts` — the same letter scale as the MLB board; `playable` = Caesars posts it ∧ status upcoming ∧ kickoff after `now`; `kelly` = quarter-Kelly of the bankroll, capped at 2 %, only when playable.
7. Games survive only when `ptDateOf(start) === date` (`src/lib/cfb/dates.ts`); `slateDates` = PT dates of every odds event still to kick off, plus the requested date.

## Team matching (`src/lib/cfb/names.ts`)
- `normTeam`: strip accents (NFD), lowercase, drop apostrophes, "&" → " and ", the token "st" → "state".
- `ALIASES` (Odds API name → ESPN display name), 10 entries:

| Odds API | ESPN |
|---|---|
| Sam Houston Bearkats | Sam Houston State Bearkats |
| Southern Miss Golden Eagles | Southern Mississippi Golden Eagles |
| Houston Christian Huskies | Houston Baptist Huskies |
| App State Mountaineers | Appalachian State Mountaineers |
| SE Louisiana Lions | Southeastern Louisiana Lions |
| The Citadel Bulldogs | Citadel Bulldogs |
| Youngstown State Penguins | Youngstown St Penguins |
| Nicholls Colonels | Nicholls State Colonels |
| Louisiana Ragin' Cajuns | Louisiana Ragin Cajuns |
| Hawai'i Rainbow Warriors | Hawaii Rainbow Warriors |

- `GENERIC` tokens ignored in the overlap test: university, state, the, of, and, at, college, a, u.
- `matchOddsEvent` tiers: exact on both teams → alias on both → one team exact plus token overlap on the other, same PT date, kickoff inside `matchWindowMs`. Ties go to the nearest `commence_time`; a matched odds id is consumed so two ESPN games cannot claim one event.
- Read on prod 2026-09-05: 68 ESPN games, 68 matched, 0 unmatched.

## Card rules (`CFB_RULES`, `CFB_PAPER`) and `buildCfbCard` (`src/lib/cfb/card.ts`)
- Paper: **$250** core + $25 fun per slate day (`CFB_PAPER`, `since` still 2026-09-05; $150 core until 2026-09-08 — see "Allocation widened" below; history already stored keeps its own recorded `daily`).
- Core: candidates need `evCz ≥ 2 %` and a Caesars decimal ≤ 2.60; one side per game (`bestPerGame`); singles and cross-game doubles (product ≤ 2.60) ranked by EV; greedy admit with Kelly stakes clamped to $5–**$50** (kellyCap 0.02 × CFB_BANK_BASE 2500); never two core tickets on one game; 3 to **10** tickets (values since 2026-09-08; $5–$25 and 3–7 before); leftover raised likeliest-first; if still short, a forced pool (decimal ≤ 1.75, EV ≥ 0) by probability with a "Top-up:" note; undeployed money is written into `notes`, never silently spent. The first fixture card (fixture data, 12 games) deployed only $75 in three $25 singles and said so in its note; the prod card of 2026-09-05 deployed all $150 in six singles (both under the pre-2026-09-08 $150 / $25-max / 3–7 rules).
- Fun: $25 on one parlay of 3–5 legs across distinct games; legs are the likeliest sides by no-push probability at Caesars' line among playable rows with `evCz ≥ −3 %` (grade D or better, never an F), moneylines and spreads before totals, adding legs until the decimal is ≥ 4 (cap 40). Named "FAVORITES PARLAY" when at least half the legs are ≥ 50 % to hit, else "FUN PARLAY". Decision record: the first fixture card's fun ticket, ranked by EV, was the same three underdogs as its core singles at +631 (fixture data) — the opposite of fun money on favourites — so the leg picker went probability-first. When Caesars posts no moneyline on the biggest favourites (it did not on six games of the fixture slate; the same happened on prod), the likeliest priced sides are big underdog spreads and the parlay leans on those.
- Games whose kickoff is at or before `opts.now` are excluded from every ticket.
- Ticket ids `cfb-<date>-core-<n>` and `cfb-<date>-fun-1`; names "SINGLE · …" / "DOUBLE · … + …".

## Separation — every CFB key, route, and store
- localStorage: `pl_cfb_ledger`, `pl_cfb_bank2` (`CFB_KEYS`).
- Redis: `pl:cfb:ledger:v1`, `pl:cfb:bank:v1` (`CFB_REDIS`).
- Routes: `/api/cfb`; `/api/cfb/ledger` (`app/api/cfb/ledger/route.ts`) — GET `{ledger, bank, at}`, PUT `{ledger, bank?}` behind the same `x-pl-sync` gate as MLB sync, `validateLedger` plus every entry `sport === "cfb"`, merged by date, 413 over the byte cap, no epoch.
- Query keys start with `"cfb"`.
- Bank: `CFB_BANK_BASE` 2500, `asOf` = `CFB_PAPER.since`; Settings shows "COLLEGE FOOTBALL BANK" beside the MLB bank with its own append-only adjustment log (`src/components/cfb/CfbBankPanel.tsx`).
- Ledger entries (`CfbLedgerEntry`): `sport: "cfb"`, `locked: true`, `daily` 150, `fun` 25, `core[]`, `funT`, a `games{}` snapshot, `grading?`. `upsertCfbEntry` refuses to re-lock a date (`src/lib/cfb/store.ts`).
- Sync: `syncCfbNow` / `useCfbSyncBeacon` / `CfbSyncChip` (`src/lib/cfb/sync.ts`); the phrase is the one sync phrase from Settings, sent only as the `x-pl-sync` header; the two stores never cross.
- Exposure: `cfbExposure` / `cfbExposureOn` count only CFB entries.
- `tests/cfb-separation.test.ts` pins all of it: no CFB module references an MLB key or route, `/api/cfb/ledger` never names the MLB Redis keys, every page imports `useSport`, `AppShell` mounts the CFB beacon.

## Grading (`src/lib/cfb/grade.ts`)
- `gradeCfbEntry(entry, finals, now)`: each leg against the ESPN final for its game at the Caesars line and price on the ticket; pushes void the leg; a game with no final after `CFB_UNGRADABLE_MS` (48 h) is void; auto-grade skips NO-PLAY entries.
- Stats and bankroll go through the shared helpers (`ledgerStats`, `computeBankroll`, `todayExposure`) fed only CFB entries (`src/lib/cfb/ledger.ts`).

## UI
- `src/components/cfb/`: `TeamMark`, `CfbGameCard`, `CfbPicksBoard` (the Board since 2026-09-05: StatTiles PICKS / +EV AT CAESARS / BEST EDGE / PARLAYS, TOP 50 / ALL scope, category strip ALL · ML · SPREAD · TOTAL · one tab per prop market, pick search, the ranked read-only table, and `CfbParlaysSection` — PARLAYS / MIXED / LIVE views with SAFER / LONGSHOT / MIX tiers from `buildCfbPicks` in `src/lib/cfb/picks.ts` under `CFB_PARLAYS`; no games list — that is Games), `CfbBoard` (now only the desk-hook re-exports from `src/lib/cfb/useCfbDesk.ts` plus an alias of the picks board), `CfbGames` (day rail with ‹ › pills, finals poll while live), `CfbBuilder` (CORE / FUN / CFB BANKROLL / EXPOSURE tiles, TODAY'S CARD, NO-PLAY state), `CfbSlip`, `CfbProps` (sandbox, Caesars / Best toggle), `CfbLedger` plus `CfbLedgerActions` (Grade now / Export / Copy for phone / Import / Wipe device), `CfbSyncChip`, `CfbBankPanel`, `CfbSharp` (the constants panel), `CfbFpiPanel`.
- Shell (`src/components/shell/`): `SportSwitch` (motion `layoutId` thumb), rail active pill, `useCfbSyncBeacon()` beside the MLB beacon, footer "MLB & CFB · informational only, not betting advice". `app/template.tsx` is the page-enter transition (fade + rise over 0.7 s; reduced motion → appears).
- Primitives added in `src/components/ui/`: `Segmented`, `StatTile`, `EdgeMeter`, `Sparkline`; `PageHeader` gained `eyebrow` and `chip`. Tokens in `app/globals.css`: `--color-cfb` (amber), `.press`, `.card-lift`, `.segmented`, `.ticket` (mask-clipped, so glows go on a wrapper), `.shine`, `.stat-tile`, `.rail-glow` / `.is-cfb`.

## Props, picks and parlays (INSTRUCTION 39, 2026-09-05)
- Six player markets, contract in `src/lib/cfb/props-types.ts`: anytime TD (`player_anytime_td`, yes-only), pass TDs, pass yds, receptions, rush yds, rec yds (over / under). A row key is `gameId|market|playerSlug|side|line`.
- Pricing in `src/lib/cfb/props.ts` (`parseEventProps`): O/U pairs devigged per book at the same point; the consensus line is the weighted median of posted lines (always a posted line); fair = the true median of the devigged probabilities at that line, `null` under `CFB_PROPS.minBooks` (2); anytime-TD yes-only quotes are priced against a 1.08 overround; every book is quoted at its own line with EV re-evaluated there; Caesars (`settleBook`) is the settle book and the grade / Kelly source, as on sides.
- ESPN season context (`src/lib/cfb/props-context.ts`, `loadCfbPropsContext`, revalidate 3600 s) attaches per-game and season passing / rushing / receiving lines to a row (`ctx`) when the athlete resolves.
- Route `app/api/cfb/props/route.ts` (`?date&bankroll`; since 2026-09-08 a thin shell over the shared body `footballPropsGet(CFB_LEAGUE, req, { storeKeys })` in `src/lib/server/football-props.ts`, the same body `/api/nfl/props` runs on `NFL_LEAGUE`): builds the slate through the shared helpers in `src/lib/cfb/slate-server.ts`, picks the games that have an odds event and a Caesars side price and are either upcoming (kickoff still ahead) or LIVE — never final / postponed (`selectPropEvents`: two pools since the 2026-09-05 same-day follow-up — the in-play games, at most `liveMaxEvents` = 6, then the pre-kick games, at most `maxEvents` = 12, each pool by kickoff then rank; changed under INSTRUCTION 40 so in-game prop lines keep populating like the MLB side. Read on prod after the first deploy: one sorted list capped at 12 let nine in-play afternoon games take every slot, so the twenty-plus evening kickoffs had no props — hence the two pools), then pulls each event's props with the server key. Prod's `/api/odds?u=` proxy refuses player-prop market shapes (403 "market shape not allowed"), which is why this route fetches itself.
- INSTRUCTION 42 (2026-09-05, "grade every possible pick"): every eligible game on the slate is priced (60 / 24 pools), and the carry decision is PER GAME — `CfbPropsBoard.pricedAt` stamps each priced game; a game inside its own 2 h window is carried, a LIVE game inside its window is re-priced every 10 min UNLESS its last pull returned zero rows (the empty-event rule: small games have no player props at the API, so they are held, carried, and never flag the board stale). The stored board is written in gzip + base64 chunks of `CFB_PROPS_CHUNK_ROWS` (400) rows under an index at the board key (Upstash REST caps a request at 1 MB; a 60-game board would have failed the SET silently and disabled every credit saver — review fix). Cost math (estimate 31 credits per event with props): pre-kick pool ≤ 60 × 31 per 2 h; live pool ≤ 24 × 31 = 744 per 10-min pull, so on a busy Saturday the 2,500-credit day is spent after roughly three live pulls and the board then serves its last priced lines flagged stale — the daily budget and the Odds API plan (16,480 credits left in the month when read 2026-09-05) are Josh's to raise.
- INSTRUCTION 43 (2026-09-05, Josh: "They are still 12 games today that haven't started w/ current Anytime TD odds" · "It should be showing 50+ Anytime TD parlays" · "move The Sharp and Simulator up above Parlay Builder, Ledger down above Settings, color the tab titles"): three changes, each pinned. (1) THE CAESARS-MISSING RULE — `CFB_PROPS.czMissingRevalidateSec` (1800) / `czMissingWindowSec` (4 h): an upcoming game on the stored board with rows and, on some market with rows, no Caesars quote (`czMissingGameIds`; zero-row games never qualify) and kickoff inside 4 h is re-fetched once its own `pricedAt` is 30 min old (`czMissingDue`, checked BEFORE the fresh-board answer); the pull's need orders live → never priced → Caesars-missing → 2 h-expired; re-pulls bypass the data cache (`cache: "no-store"`); the board carries `czMissing` / `noProps` (`propsCoverage`, priced games only) and both footnotes print them off the constants, each only when > 0. Full detail in the Caesars-missing bullet below. (2) TIERED ANYTIME TD SETS — `CFB_PARLAYS.setFloorEvPct` (−12) is tier 2 for the single-market sets only; `CFB_PARLAYS.setBands.anytime_td` = 2–4 legs, decimal 4–250 (the shared 60 cap forbade a third 3–8 leg); each `CfbParlay` carries `gated` (true = every leg ≥ −3), gated tickets rank first by EV, then the rest by EV; the Board tags an ungated ticket EDGE − (`OpenTag`, `data-testid="cfb-parlay-open"`) beside its tier chip, (the EV chip keeps its full-strength red — the tag and the sign carry it; review fix dropped the dim), counts them on the PARLAYS tile ("N below gate") and under the set blurb, and the set blurbs read leg count / price band off `setBandOf` (anytime TD: "2–4 players … priced +300 to +24900"). Pinned in `tests/cfb-picks.test.ts` (CFB_PARLAYS deep-equal, the 6-leg → 50-ticket slate) and `tests/cfb-picks-ui.test.ts`. (3) NAV — `AppShell.tsx` NAV order top: Games, Stats, Board, Builder, The Sharp, Simulator, Parlay Builder, Parlay Calc; bottom: Ledger, Settings; every `NavItem` carries a `tone` hex (Games #7DD3FC, Stats #C4B5FD, Board #B6FF3D, Builder #FCD34D, The Sharp #FDA4AF, Simulator #67E8F9, Parlay Builder #FDBA74, Parlay Calc #5EEAD4, Ledger #FDE68A, Settings #D4D4D8) applied to the rail label / icon, the phone top-bar icons and the bottom bar (active full, idle `tint(tone, 0.7)`; the active pills take the tone) — pinned in `tests/nav.test.ts`.
- INSTRUCTION 44 (2026-09-05, Josh, verbatim: "no they should be using in game lines as well; make it also use in game prop lines for all of the same props as they are available; which is through 3rd quarter in most games"): prod at 16:55 PT carried Caesars Anytime TD on two LIVE games only (MOST@TA&M, WMU@MICH) and the ANYTIME TD set read 0, because the nine single-market sets and COMBO built from pregame legs alone while live legs fed only MIXED and LIVE. Now (`src/lib/cfb/picks.ts`, `buildCfbPicks`) every single-market set (ml, spread, total, anytime_td, pass_tds, pass_yds, receptions, rush_yds, rec_yds) and COMBO draw from BOTH pregame and in-game legs: tier 1 = upcoming + live legs with EV ≥ `CFB_PARLAYS.minLegEvPct` (−3), tier 2 (single-market sets only) = upcoming + live legs down to `setFloorEvPct` (−12); a live leg keeps `CfbParlayLeg.live = true`, one leg per game, the per-market bands (`setBands.anytime_td` 2–4 legs / dec 4–250) and the 50-per-category fill order (gated first, then by EV) are unchanged, and within a set tickets are still ranked by EV — a live leg is never pushed up or down. MIXED stays live + pregame on one ticket, LIVE stays live-only, the legacy tiered view (SAFER / LONGSHOT / MIX) stays pregame-only. Each `CfbParlay` now carries `liveLegs` (the count of in-play legs; a ticket with any live leg is never a paper stake — like a live pick row it has no Kelly figure). Board (`CfbPicksBoard.tsx`): a ticket with `liveLegs` > 0 wears an "N in-game" tag (`InGameTag`, `data-testid="cfb-parlay-ingame"`, the live tone with LiveTag's pulse dot at OpenTag's chip size) in its header chip row on the phone feature card and the desktop card, the whole-ticket LIVE pill stays the LIVE set's alone, the per-leg LIVE tag survives, the PARLAYS tile appends " · N with in-game legs" when > 0, and the ten pregame-category blurbs say the lines are Caesars' pregame and in-game. Pinned in `tests/cfb-picks.test.ts` (the "INSTRUCTION 44" describe: an ATD slate whose only Caesars rows sit on two live games fills the set to 50 two-leggers, every leg `live: true`, `liveLegs` exact, gated ordering kept, MIXED / LIVE / legacy / combo unchanged in kind; the INSTRUCTION 42/43 pins that encoded "pregame only" for the sets were rewritten with this instruction) and `tests/cfb-picks-ui.test.ts`. Review fix: the twelve sets stay disjoint — `emit()` keeps a cross-set `taken` key set with precedence in `CFB_PARLAY_CATEGORIES` order (single-market > combo > mixed > live); the first build duplicated COMBO tickets into MIXED / LIVE (24 / 32 / 22 duplicate leg sets at 2 / 3 / 6 live games), pinned in `tests/cfb-picks.test.ts`. MIXED and LIVE tickets carry no in-game chip and are not in the tile's in-game count. Benchmark ~90 ms.
- INSTRUCTION 45 (2026-09-05, Josh, verbatim: "Parlay Lab CFB should've been running the same $150 per day theoretical Core money and $25 Fun money per day"; the other half built 2026-09-06, and hardened the same day by four adversarial passes over the money path): the gap, verified on prod — `pl:cfb:ledger:v1` (GET `/api/cfb/ledger`) held ZERO entries, because the CFB card locked only when a person tapped LOCK in `CfbBuilder`, while the MLB desk locks server-side (the scheduler writes a `source: "server-lock"` day every day). The CFB desk now has its own server lock, its own top-up and its own settle pass. ROUTE — `app/api/cfb/lock/route.ts`, GET, `dynamic = "force-dynamic"`, `maxDuration = 60` (declared 2026-09-06; the peers it was sized against, listed in the route header: scheduler 90, generate 300, sharp 300, calibrate 60, clv 60, propsnap 60, ufcprops 60 — and deliberately ABOVE the caller's 25 s abort so the CALLER's timeout is the binding one and a poke the caller gave up on can still finish writing the day). Gated EXACTLY like `/api/scheduler`: 503 `cfb-lock-not-configured` when `CRON_SECRET` is unset (fails closed — this route WRITES), 401 `unauthorized` unless `cronHeaderAuthed` (`x-cron-key` or the Vercel `Bearer` spelling), 503 `sync-not-configured` unless `storeEnv()`. Parameters: `?date=YYYY-MM-DD` (default `ptToday()`; anything that fails `/^\d{4}-\d{2}-\d{2}$/` is a 400 `bad date`) and `?dry=1` (decide and report, write nothing — no ledger row, no odds-gap marker, no top-up, no top-up CLAIM, no grading; a dry poke therefore prices a board without consuming a top-up attempt, which is stated rather than hidden because only a hand-typed poke carrying the cron header can send it). The statuses it can answer, in the order the code can reach them: 400 `bad date`; then the two OPENING store 502s, deliberately worded apart — `store unreachable: …` for an upstream failure the next poke will very likely survive, and `cfb store unreadable: …` (the `CfbStoreUnreadable` class) when the blob arrived and could not be understood; then, from the already-locked exit onward, 200 `already-locked` (carrying `topUp`, and `raced: true` when the write block found the date taken), 502 `espn unavailable`, 200 `no-slate`, 200 `waiting`, 502 `board failed`, 502 `odds-missing`, 502 `lock build failed`, 200 `locked` (`?dry=1` returns the same summary plus the whole `entry`), 413 `merged ledger too large`, 502 `store unreachable` at write time. WINDOW — `CFB_LOCK.leadMs` (60 min, `src/lib/cfb/rules.ts`): `decideCfbLock` over the date's kickoffs answers `no-slate` (no parseable kickoff), `waiting` when `now < firstKickoff − leadMs` (body carries `firstKickoff`, `locksAt`, `leadMs`), else `lock` with `ahead` / `total`. WHEN IT ACTUALLY LANDS, honestly: there is no cron on this route, so it fires on the scheduler pulse — the external ticker in `docs/cron-jobs.md` (every 15 min, UTC hours 15–23 and 0–2, i.e. roughly 08:00–19:45 PT) plus `vercel.json`'s two entries (`/api/scheduler?poke=evening` at `45 21 * * *` and `?poke=night` at `0 0 * * *`). The first poke of a PT day is therefore 15:00Z, and the lock is taken by the FIRST poke at or after `locksAt`, not at `locksAt` itself: a 16:00Z first kickoff locks at the 15:00Z poke with every game on the slate still ahead, and a later first kickoff locks with a realised lead of `leadMs` minus however far past `locksAt` the next pulse falls — under an hour, never over it. A first kickoff between 15:00Z and 16:00Z still locks pregame but with less than an hour's lead, shrinking to nothing at 15:00Z; only a first kickoff at or before 15:00Z takes the LATE path, because its window opened before the ticker's first poke of the day. A poke after the first kickoff still locks, from the games still ahead (`buildCfbCard` only prices kickoffs after `now`), and the note reads "locked after first kickoff — N of M games still ahead"; a poke after the LAST kickoff locks a NO-PLAY entry whose note says the window was missed — never a card on lines that are gone. When locking (`src/lib/cfb/lock-server.ts`): `buildCfbCard(slate, { bankroll, daily 150, fun 25, now })` → `assertCfbCardMoney` → `lockCfbCard` → stamped `source: "server-lock"` with a `trigger` (the MLB scheduler's own field names, not a new `lockedBy`; `CfbLedgerEntry` carries optional `source`, `trigger`, `note`) → MERGED into the stored ledger with `mergeLedgers`, the same kernel `PUT /api/cfb/ledger` runs, the store re-read at write time so a device push that landed mid-build is kept (and if it took the date, the answer is `already-locked` + `raced`, nothing written) → `{ status: "locked", date, at, dry, core, coreStake, fun, funStake, noPlay, ahead, games, oddsMissing, pricedAhead, bankroll, cause, note, lockedAt, source, trigger }`. Four triggers, all in `lock-server.ts`: `cfb-lock` (same-day), `cfb-lock-odds-gap` (same-day, window closed unlocked because the odds feed never priced the day), `cfb-lock-sweep` and `cfb-lock-sweep-odds` (the previous-date pair) — so the failures stay apart on the ledger forever. ODDS OUTAGE — `oddsPayload()` in `src/lib/cfb/slate-server.ts` NEVER throws: a missing `ODDS_API_KEY`, a 401, a 429, a non-array body and a network error ALL return `{ events: [], missing: true }`, so `slateFromEspn` hands back a scores-only board, `priceGame` pushes no row, and `buildCfbCard` returns `{ noPlay: true }` — which the first cut WROTE, after which every later poke exited already-locked, so one transient upstream blip cost the whole $150 / $25 day. Now, with the window open and games still ahead, a day that carries no usable Caesars price — either `slate.oddsMissing` (the fetch itself failed) or `cfbPricedAhead(slate.games, now) === 0` (the fetch returned but matched none of the games still ahead: the same hole wearing a different hat) — writes NOTHING and answers `{ status: "odds-missing", date, at, dry, oddsMissing, pricedAhead, ahead, games, note }` with HTTP 502 so the forward reports it; the next poke retries and locks the day normally, and `?dry=1` reports the same refusal. It writes nothing precisely because an unpriceable day and a no-bet day produce identical NO-PLAY cards, and only `pricedAhead` tells them apart — a written NO-PLAY is permanent, a refusal is retried in 15 minutes. `cfbPricedAhead` was tightened 2026-09-06 to count character-for-character what `buildCfbCard` counts (`r.playable && r.cz != null && r.evCz != null`, on games whose start is still ahead), because `playable` also demands `status === "upcoming"`: a postponed or mislabelled event ESPN flags live on a FUTURE start used to inflate the count, suppress the refusal, and let the route lock a NO-PLAY it then described as the genuine kind. Two NO-PLAYs are still written on purpose: the missed-window one (`ahead === 0` — that record is about the window being gone, not about the lines) and the GENUINE one (Caesars prices present, nothing clearing +2 % EV under `CFB_RULES.maxDec`), which is a real no-bet day and belongs on the ledger. CREDIT DISCIPLINE (2026-09-06) — the route now DECIDES BEFORE IT SPENDS: it builds the ESPN-only board first (`buildCfbBoard` with `oddsEvents: []` and no FPI), runs `decideCfbLock` on ITS games, and answers `no-slate` / `waiting` from that, so `slateFromEspn` — the only path to `oddsPayload()` — is reached only by a poke that is actually going to lock. That is safe because `buildCfbBoard` fills `games` from `espnEvents` alone (shape → drop `g.date !== date` → dedupe → sort) and an odds event can only ever fill `rows` and `slateDates`; it can never add, drop or shift a game. What it saves: a fresh slate pull is 6 credits and the odds data cache revalidates every 240 s against a ~15-min pulse, so before this EVERY poke was a fresh billed call — on a date with no kickoff at all (February through July, and most Mon/Tue/Wed in season) the route paid 6 credits to answer `no-slate` having written nothing, and the long `waiting` stretch before a late kickoff did the same, none of it visible to `CFB_PROPS.dailyBudget` (which counts props pulls only). `espnEvents(date)` is two keyless ESPN scoreboard fetches (the date AND the next date, `revalidate` 60) and costs no credits at all. THE PREVIOUS-DATE SWEEP — the forward passes no `?date`, so the route always means `ptToday()`, and the ticker covers only ~08:00–19:45 PT: a slate whose lock window opened after the last poke of that PT day (a 21:00 PT kickoff, say) answered `waiting` at the 19:45 PT poke and was never seen again, because by the next poke `ptToday()` had already rolled over — never locked, never recorded, a SILENT day, which `/api/scheduler` and `src/lib/server/lock-card.ts` (`buildReasonRecord`) both refuse to allow on the MLB rails. A poke that carried no explicit `?date` therefore sweeps PREVIOUS PT dates: `sweepPrevDates` walks `CFB_SWEEP_DAYS` (3, a constant beside `CFB_LOCK`) dates newest-first via `prevPtDates(args.date, CFB_SWEEP_DAYS + 1).slice(1)` (`app/api/cfb/lock/route.ts` L369, read this turn; the bare `date` argument this bullet gave is corrected 2026-09-06) and STOPS at the first date that is `before-desk` or `already-recorded`. Depth three because on CFB the week's whole meaningful slate IS Saturday, and from any poke through Tuesday three days still reaches back to it (Tue − 3 = Sat); the first cut reached back exactly one day, so a two-day outage left the day that carries the money silent forever. FLOORED at `CFB_PAPER.since` ("2026-09-05"): a date before it is reported `before-desk` and never fetched, because the first prod poke otherwise swept 2026-09-04 — a Friday with real FBS kickoffs — and wrote a locked NO-PLAY for a day on which this route did not exist and the paper card had not started, against the opposite invariant `src/lib/cfb/store.ts` rests on. Per date, in cost order: the FREE ledger check (device OR server entry) first, then one keyless ESPN read, board built with `oddsEvents: []` (the `finalsFromEspn` trick) so the sweep NEVER spends an Odds API credit; a genuinely missed day gets `buildCfbSweepEntry`'s missed-window NO-PLAY, `source: "server-lock"`, `lockedAt` = the sweeping poke's own instant, and the note names the date it is about and says it was swept a day late. TWO NOTES, and the difference is READ rather than guessed: the odds-missing refusal stamps a dated marker `pl:cfb:oddsgap:v1:<date>` (`CFB_REDIS.oddsGap`, `EX CFB_ODDS_GAP_TTL_SEC` = `(CFB_SWEEP_DAYS + 1)` days so it cannot expire while the walk that reads it can still reach its date), and both the sweep and the same-day `ahead === 0` record read it back — with the marker, trigger `cfb-lock-sweep-odds` / `cfb-lock-odds-gap` and a note saying the day "was lost to the odds feed, not to a gap in the ticker"; without it, trigger `cfb-lock-sweep` / `cfb-lock` and a note claiming only what is known ("no lock ever landed for <date> and no odds-outage refusal was recorded for it"). The old single note asserted "because no scheduler poke ever landed inside <date>'s lock window", which the refusal makes false — pokes landed all day INSIDE the window and refused on purpose — and Josh reads that string under the locked card. The claim the whole sweep rests on: every game on a CFB board for PT date D kicks off on PT date D (`src/lib/cfb/model.ts` drops `g.date !== input.date`, where `g.date` is `ptDateOf(start)`), so once we are inside D+1 the whole of D has kicked off by definition. COST: once a date carries a record the walk stops there and the sweep costs nothing at all; the worst case is `CFB_SWEEP_DAYS` ESPN reads and zero credits. It can never cost the poke its answer — every failure is caught and reported, and today's body and status code are untouched. CORRECTION (2026-09-06): the sweep result does NOT ride on "every answer past the gate" as this bullet previously claimed — it is computed after the store read, so the 400 `bad date` and the OPENING `store unreachable` 502 both return before it exists. It rides on every answer from the already-locked exit onward (`say()` adds it to all of them), including `already-locked` itself, which is exactly the day a silent yesterday would otherwise hide on. Shape: `sweep` = the newest date's fields (`{ date, action: before-desk | already-recorded | no-slate | not-missed | would-record | recorded | error, … }`) plus `days`, the whole walk, so a two-day outage is visible in one line. An explicit `?date` (a backfill by hand) suppresses it entirely. THE TOP-UP (`CFB_TOPUP_MAX` 2, `topUpDate` + `decideCfbTopUp` / `planCfbTopUp` / `applyCfbTopUp`) — the lock fires an hour before the FIRST kickoff, exactly when the pool of posted Caesars prices is thinnest, and `buildCfbCard` says so out loud when it cannot spend the allotment ("$N of the $150 stayed undeployed"; on the 2026-09-05 FIXTURE slate the server lock stakes three $25 singles for $75, pinned in `tests/cfb-lock-route.test.ts`). Every later poke hit the already-locked exit and returned, so the day ended permanently short while the ledger recorded it as a full paper day and `cfbBankroll` sized every later day off it — MLB settled this on Josh's own word (`src/lib/paper-mode.ts` `TOPUP_MAX`, quoting him: "I said $150 every day no matter what so we could track and calibrate off of it") and `CFB_TOPUP_MAX` is the CFB twin of that number, mirrored rather than imported so the two desks cannot silently share a knob. It fires ONLY on the already-locked exit, and `decideCfbTopUp` refuses for FREE, in the order that spends the least: an entry whose `source` is not `"server-lock"` (A BUILDER LOCK IS NEVER TOPPED UP — Josh's own card is his, and it costs no fetch), BOTH ALLOTMENTS CLOSED — the core arm shut (less than `CFB_RULES.minStake` ($5) of room, or all `CFB_RULES.tickets.max` (7) ticket slots spent) AND a fun parlay already on the day, the two arms being INDEPENDENT gates since 2026-09-06 so either one open fires the attempt and the free refusal names both; the cap already spent (ATTEMPTS, not successful writes — see below), a previous attempt that bought a board within `CFB_TOPUP_RETRY_MS` (45 min, mirroring `/api/generate`'s own "ran recently" limiter on `pl:gen:lastRun`) and seated NOTHING (an attempt that DID seat tickets says the board is moving and the next may run on the following pulse), or `now` not past `lockedAt` (a top-up exists for prices that post AFTER the lock; at the lock's own instant a rebuild can only re-offer what the lock already declined). Only past all five does it spend one ESPN read to check anything is still pregame, then a free check that some game still ahead is not already core-seated (`cfbCoreGamesOf` counts CORE legs only — `buildCfbCard` builds the fun parlay without consulting `usedGames`, so counting fun legs would let the card's own parlay veto a core top-up on a game the core never touched), and only then pays for a priced board (one game-lines pull, 6 credits). It APPENDS, never replaces: `planCfbTopUp` narrows the board to un-seated games and runs `buildCfbCard` with `daily` set to the ROOM (so the sizer's own greedy admit/raise does the work and a top-up is never sized by a different rule than the lock), filtered again on the way out to the free ticket slots, the room and un-seated games, with ids `cfb-<date>-topup<n>-core-<entry.core.length + i>` — the index counts on from the day's OWN core length instead of restarting at 1 (2026-09-06), so an id one attempt minted can never be minted again by another; `applyCfbTopUp` carries `lockedAt`, `source`, `daily`, `fun` and every existing core AND fun ticket through byte for byte (it APPENDS to `funT` — see the fun paragraph below — and never replaces a parlay that stands), unions the games map with the existing entry winning, EXTENDS the note ("· Top-up n at <ISO>: N core tickets for $X — the day now carries $Y of the $150. Fun: +$Z / nothing added — $W of the $25."), REPLACES this attempt's row on the entry's own `topUps` (so the cap survives a cold start, a redeploy and an overlapping poke), REOPENS grading when the day was already `done` and any core or fun ticket it now carries has no verdict — a DEEP copy, `{ ...grading, tickets: { ...graded }, legs: { ...legs }, done: false }`, so the entry handed in is never mutated, mirroring `mergeDay`'s own reopen rule; without it a top-up onto a graded day deployed money that `cfbSettleCandidate` then refused to score for ever — and drops `noPlay` once money lands. THE CAP BOUNDS ATTEMPTS, NOT SUCCESSFUL WRITES (2026-09-06, the critic's pass, and it is the defect on this route that cost the most): `CFB_TOPUP_MAX` was read off the LENGTH of `topUps` and a row was written only by `applyCfbTopUp`, i.e. only when an attempt actually seated a ticket, so on a day that stays short — the COMMON case, see the fixture note below — the counter never moved and every poke from the lock to the last kickoff ran the whole path and bought a fresh priced board to learn the same "nothing to seat". `CfbTopUpRecord` is now `{ at, n, core, stake, fun?, filled? }` and records an ATTEMPT: `claimCfbTopUp` writes the row `{ core: 0, stake: 0, filled: false }` at the POINT OF COMMITMENT — past every free refusal and one line above `slateFromEspn`, the only call on this path that reaches the Odds API — so a crash, a platform kill or an answer the caller gave up on between the claim and the write still costs the attempt, which is the failure mode a cap must have; `applyCfbTopUp` then REPLACES the row carrying the same `n` if the attempt seated anything (and appends it if a racing writer clobbered the claim), so `topUps.length` is the number of attempts and each row says what that attempt actually bought. `n` rides on the row rather than being inferred from position because the write block re-reads the store and must recognise ITS OWN in-flight claim — `decideCfbTopUp(live, args.now, { claim: n })` (`app/api/cfb/lock/route.ts` L836, read this turn; the bare `now` argument this bullet gave is corrected 2026-09-06) excludes it, or the poke would count itself at the cap. THE ORDINAL IS NOT THE IDENTITY (corrected 2026-09-06): a claim is matched by the PAIR (that ordinal, still UNFILLED) — `rows.find((r) => r.n === opts.claim && isClaimRow(r))` — so a COMPLETED attempt counts against the cap no matter which poke wrote it, and the two filters that replace a row (`claimCfbTopUp`, `applyCfbTopUp`) narrowed from `r.n !== n` to `!(r.n === n && isClaimRow(r))`; and UNFILLED is the row's own `filled` flag rather than `core === 0` since 2026-09-06 (DEFECT M(b)), because a completed FUN-ONLY attempt legitimately records `core: 0, stake: 0` — `isClaimRow` (`const isClaimRow = (r: CfbTopUpRecord): boolean => (r.filled === undefined ? r.core === 0 && r.stake === 0 : !r.filled);`, `src/lib/cfb/lock-server.ts` L398, read verbatim this turn — the receiver-less form this bullet quoted until the 2026-09-06 audit round is in no file) is now the ONE reading of "in flight", used by the claim lookup, the retry gate, `claimCfbTopUp`, `releaseCfbTopUp` and `applyCfbTopUp` alike, and its `undefined` arm is the ledger's own history rather than a fallback, since `claimCfbTopUp` was the only writer that could ever produce those two zeroes: a claim may only ever replace an unfilled row, never another poke's seated attempt. Matching on the ordinal ALONE was measured to let a second poke erase the first's completed row and re-mint the ticket ids it had already seated; a fresh attempt now also takes `Math.max(used + 1, maxN + 1)`, so it can never land on an ordinal the entry already carries. `releaseCfbTopUp(entry, n)` (new, pure) removes an unfilled row at an ordinal and nothing else. A row written before this change carries no `n` and nothing breaks: it is still counted, it never matches a replace filter, and `core > 0` on every such row so it cannot trip the retry window either. The precedent is the MLB desk's and it counts attempts the same way — `app/api/generate/route.ts` claims `topup-N` before the fire and writes the registry row whatever the fire seated, and `src/lib/server/blocks.ts` `decideTopUp` counts those keys rather than the tickets they produced. WHAT IT COSTS: a date can now never buy more than `1 + CFB_TOPUP_MAX` = THREE PRICED boards, and since the fun arm shipped the two allotments SHARE that budget rather than each getting one however long its slate runs — the lock's own pull plus at most two attempts, and the bound is on boards that actually priced: an attempt whose odds call comes back empty is handed BACK rather than spent (the outage give-back below), so while the feed is down a poke may re-attempt on every pulse, buying no board and seating no money — which at the 6 credits a game-lines pull costs (measured on prod 2026-09-05, the quota moved 17578 → 17572, recorded in the route header) is 18 Odds credits a date, none of it visible to `CFB_PROPS.dailyBudget`, which counts props pulls only. Measured on the 2026-09-05 FIXTURE (`tests/cfb-lock-route.test.ts`, the "CRITIC 1" describe, 4 tests green): one lock plus two attempts is exactly three `slateFromEspn` calls for the whole day, after which every later poke costs nothing at all — not even the keyless ESPN read, because the cap refusal is free and comes first. THE FUN PARLAY IS NO LONGER THROWN AWAY WHEN NO SIDE CLEARS THE CORE GATE (2026-09-06, INSTRUCTION 45 — the headline of this round, `src/lib/cfb/card.ts`): a board that can only make a fun parlay now SEATS one instead of recording NO-PLAY. `buildCfbCard` used to RETURN `{ core: [], funT: [], noPlay: true }` from inside its core section the moment `bestRows` came back empty, and the fun section sits BELOW that return — so the CORE's verdict was silently applied to the FUN bucket's money and the $25 was thrown away on a day that could still stake it. The early return is gone; the whole core body (admit, raise, the forced top-up, the undeployed and minimum-ticket notes, the bench sweep) is wrapped in `if (bestRows.length)` byte for byte, and a core-empty board FALLS THROUGH to the fun section, which reads `playable` and never `cands` and is gated on its own `CFB_RULES.fun` band (3–5 legs on distinct games, decimal 4–40, every leg at or above −3% EV at Caesars) rather than on the core's +2% under 2.60. No gate was widened and no bound removed. `noPlay` KEEPS ITS MEANING — nothing was staked — and is now `!bestRows.length && !funT.length`, the old condition ANDed with an empty bucket, so it can only ever become true LESS often; a core-empty day that seats the parlay carries `noPlay: false` and its first note reads "No core ticket — … None of the $150 core is staked; the $25 fun parlay is the day's only money" instead of "NO-PLAY". This lands on the LOCK path as well as the top-up — a locked day that would have recorded NO-PLAY may now record a $25 fun ticket — so `buildCfbLockEntry`'s `detail` was widened from `card.noPlay` to `card.noPlay || !card.core.length`, or that note would have been dropped from `locked.note`, the line Josh reads under the card, on exactly the day that staked $0 of the $150. MEASURED THIS TURN on such a board — the repo's own 2026-09-05 FIXTURE narrowed to the nine games the lock's own card does not seat, 46 priced sides, ZERO clearing +2% EV under 2.60 and SIX clearing −3% across FIVE distinct games — `buildCfbCard` at `daily: 0` and at `daily: 150` alike answers core 0 / $0, funT 1 / $25, `noPlay: false`. It answered `noPlay: true` with $0 of the $25 before. THE RE-MEASURED FIXTURE FIGURES, run this turn through `buildCfbBoard` → `buildCfbCard` → `buildCfbLockEntry` → `decideCfbTopUp` → `planCfbTopUp` over the repo's own `tests/fixtures/cfb/` captures (12 games, 72 priced rows), so nobody has to re-derive them. AT THE LOCK, UNCHANGED: core 3 tickets at $25 / $25 / $25 = $75 OF THE $150, and fun 1 parlay = $25 OF THE $25 (Coastal +21 + North Texas +39.5 + East Carolina +28), `noPlay: false`, under the card's own note "$75 of the $150 stayed undeployed — every ticket is at the $25 max and no other game offers a side ≥ 0% EV under 1.75." The lock is unchanged because this fixture's core is not empty; the change is entirely downstream of it. THE FUN TOP-UP ARM, CHANGED: on that same locked day with its bucket EMPTY, `planCfbTopUp` over the board it actually prices seated 0 core / $0 AND 0 fun / $0 before, and now seats 0 core / $0 and ONE FUN TICKET FOR $25 — id `cfb-2026-09-05-topup1-fun-1`, legs Ohio +24 + Oregon St +21 + James Madison −6 — at `room: $0` and at `room: $75` alike, so the six Odds credits that attempt costs now buy $25 of seated money instead of nothing. THE HONEST REMAINDER, so this is not over-read: the fixture's own locked day ALREADY CARRIES its parlay, so its own top-up still adds $0 core and $0 fun and that day still ends at $75 + $25 — the $25 is gained on a day whose bucket is empty, which is the day this arm exists for. AND ON A FULL-CORE DAY the arm now REACHES the plan but is still only as good as the board: a SYNTHETIC day carrying six $25 tickets ($150) on six of the fixture's games with `funT: []` answers `{ fire: true, room: 0, slots: 1, used: 0, funUsed: 0, n: 1, core: false, fun: true }`, buys its board (32 priced sides on the six games left) and seats $0, because six games cannot yield a 3-leg parlay inside the 4.0–40.0 band. Every figure in this paragraph was RE-DRIVEN ONE MORE TIME on 2026-09-06 AFTER the four adversarial fixes landed — end to end through `buildCfbBoard` → `buildCfbCard` → `buildCfbLockEntry` → `decideCfbTopUp` → `planCfbTopUp` over the repo's own `tests/fixtures/cfb/` captures, not carried forward — and EVERY ONE IS CONFIRMED, none corrected. RE-MEASURED AGAIN IN THE CLOSING DOC PASS (2026-09-06), driving the repo's own `tests/fixtures/cfb/` captures — `espn-scoreboard-2026-09-05.json` (12 events), `odds-ncaaf-2026-09-05.json` (14 events), `espn-fpi.json` — through `buildCfbBoard` → `buildCfbCard` → `buildCfbLockEntry` at `now = 2026-09-05T12:00:00Z` with `bankroll: 2500` and `CFB_PAPER` (read the same turn: `{ since: "2026-09-05", daily: 150, fun: 25 }`): board 12 games / 72 rows, all 12 ahead, `cfbPricedAhead` 60; the LOCK ENTRY carries `core` 3 tickets at 25 / 25 / 25 = 75, `funT` 1 ticket at 25, `"noPlay" in entry` FALSE, `daily` 150, `fun` 25, and `buildCfbCard`'s first note character-for-character as quoted below. EVERY ONE OF THOSE SEVEN FIGURES IS CONFIRMED AND NONE IS CORRECTED. THE SENTENCE, in full: the fixture board is 12 games and 72 priced rows, all 12 games still ahead at the lock instant and 60 of those 72 rows priced-and-playable; AT THE LOCK the day deploys 3 CORE TICKETS AT $25 / $25 / $25 = $75 OF THE $150 and ONE FUN PARLAY AT $25 = $25 OF THE $25 (Coastal +21 + North Texas +39.5 + East Carolina +28); `"noPlay" in entry` is FALSE — the flag is absent, not present-and-false — and `buildCfbCard`'s own first note reads, character for character, "$75 of the $150 stayed undeployed — every ticket is at the $25 max and no other game offers a side ≥ 0% EV under 1.75." On that same locked day with its bucket emptied the fun arm plans `cfb-2026-09-05-topup1-fun-1` for $25 (Ohio +24 + Oregon St +21 + James Madison −6) off `decideCfbTopUp` answering `{"fire":true,"room":75,"slots":4,"used":0,"funUsed":0,"n":1,"core":true,"fun":true}`. AND ON A FULL-CORE DAY the seat depends only on what is still OPEN, which is worth stating precisely because the two shapes read differently: six of the fixture's games seated leaves six games and 32 priced sides and seats $0 — six games cannot yield a 3-leg parlay inside the 4.0–40.0 band — while a six-ticket $150 day sitting on only THREE distinct games leaves nine and DOES seat the $25, ending at core 6 / $150 and funT 1 / $25 with no `noPlay` flag and the note "Fun: +$25 — $25 of the $25." WHAT THE TOP-UP STILL DOES NOT DO, and this bullet must not be read as promising it: it does not deploy the CORE's missing $75 on this slate. The reason is in `buildCfbCard`'s own note (`src/lib/cfb/card.ts`): every ticket is ALREADY at the `CFB_RULES.maxStake` $25 max, and no other game offers a side at or above `forcedMinEvPct` (0%) EV under `forcedMaxDec` (decimal 1.75). The top-up re-runs that same gate over the same games on a freshly pulled board, so it can only help when PRICES MOVE or a game newly comes into range — which is exactly what the MLB desk's top-up does, and exactly what a thin slate does not give it. Deploying the full $150 on a slate like that would need a change to the CFB ALLOCATION RULES themselves — the one-leg-per-game rule (`CFB_RULES.oneLegPerGame`), the forced band (`forcedMinEvPct` / `forcedMaxDec`), or the $25 per-ticket and 7-ticket caps — and that is an owner's decision which has NOT been made. What INSTRUCTION 45 has bought so far is that a short day is no longer SILENT: it is recorded, its own note says how much stayed undeployed and why, a later poke seats money if the board offers any, and the day now gets scored. THE $25 FUN MONEY IS TOPPED UP TOO, ONCE (2026-09-06; the earlier claim in this bullet that the fun bucket "is one ticket by construction … a short fun bucket stays short" is WITHDRAWN, because it justified having no fun top-up at all and the second half of Josh's sentence is money): `planCfbTopUp` now carries the narrowed rebuild's fun parlay as well, under conditions that are all in the code — ONLY when `entry.funT` is empty, AT MOST ONE ticket, minted `cfb-<date>-topup<n>-fun-1`, `stake > 0`, and the fun sum capped at `CFB_PAPER.fun` ($25); the plan's games map is extended for its legs so the grader's 48-hour window keys off a real kickoff rather than the end of the date. `entry.funT.length === 0` rather than "top up toward $25" is the rule because a day that already carries its parlay can only be given more by REPLACING what stands, which is the re-lock the once-per-date rule forbids; seating the FIRST parlay on a day that carries none removes no bet, moves no stake and moves no `lockedAt`. There is also no PARTLY-FILLED bucket to lose by gating on emptiness: `buildCfbCard` stakes its parlay at exactly `opts.fun` (`finish(..., opts.fun)`, `src/lib/cfb/card.ts`) and both the lock and the top-up pass `CFB_PAPER.fun`, so a CFB fun bucket is $0 or $25 and never something in between. BOTH OF THE GAPS THIS PARAGRAPH USED TO NAME ARE NOW CLOSED (2026-09-06, DEFECT M — and this is the sentence that answers Josh's): the two allotments are INDEPENDENT GATES in `decideCfbTopUp`, `coreOpen = room >= CFB_RULES.minStake && slots > 0` and `funOpen = entry.funT.length === 0`, and EITHER opens the attempt — so YES, a day whose core is already the full $150 can now deploy its $25, which is exactly the day the old single CORE gate threw away. MEASURED this turn against the current code: a synthetic day carrying six $25 core tickets ($150) with `funT: []` answers `{ fire: true, room: 0, slots: 1, used: 0, funUsed: 0, n: 1, core: false, fun: true }`; the same day carrying its parlay answers `fire: false` with the reason "the day is fully deployed — $150 of the $150 core is staked, less than one $5 minimum short, and the day's $25 fun money is already on a parlay — neither allotment has room, so no board is bought."; and a seven-ticket $35 day with an empty bucket answers `{ fire: true, room: 115, slots: 0, used: 0, funUsed: 0, n: 1, core: false, fun: true }` (all three RE-MEASURED 2026-09-06 against the code as it now stands, and identical) — the ticket-cap shape that used to strand $115 of core and $25 of fun at once. A FUN-ONLY plan is no longer discarded either: the route's two `if (!plan.tickets.length)` discards in `topUpDate` are now `if (!plan.tickets.length && !plan.fun.length)`, so a plan of no core and one parlay is WRITTEN, and both the `topped-up` and `would-top-up` answers carry `buckets: { core, fun }` saying which half of the sentence fired. What made that safe is the row shape: a completed fun-only attempt records `{ core: 0, stake: 0, fun: 1, filled: true }`, which the old `core === 0` reading would have mistaken for an in-flight claim — hence `isClaimRow` (above). `topUps` goes on counting CORE tickets in `core` and CORE stake in `stake`, so nothing that reads an existing row changes meaning; the attempt's fun money rides beside them in `fun`. WHAT THE FUN MONEY COSTS, stated rather than buried, and RE-STATED 2026-09-06 because L1 changed the COUNTING: each arm now counts its OWN attempts. `CfbTopUpRecord` carries `arms: { core, fun }`, stamped at the CLAIM and taken from the decision made over the LIVE entry rather than the snapshot the request opened with, and `decideCfbTopUp` computes `used = others.filter(spentCoreAttempt).length` beside `funUsed = others.filter(spentFunAttempt).length`, refusing only when every arm that is OPEN is also SPENT. The claim in this bullet that the two arms "SHARE ONE attempt budget" is WITHDRAWN as a description of the counting — it was written before the arms had separate counters, and under it a completed fun-only attempt spent a CORE attempt: MEASURED, a synthetic day carrying two fun-only rows and $75 of core still owed answered "the top-up cap is spent (2 of 2) — $75 stays undeployed…" and the core's own two chances were gone without ever having been offered. THAT SENTENCE ALSO NAMED ONLY ONE ALLOTMENT, and since D2 (2026-09-06) it names both — MEASURED THIS TURN on a day with two spent both-arm rows, $75 of core owed and an empty bucket, `decideCfbTopUp` answers "the top-up cap is spent (2 of 2) — $75 of the $150 core and $25 of the $25 fun stay undeployed rather than grind the odds quota all day." The condition is untouched; only the wording widened, so every existing pin on `/cap is spent \(2 of 2\)/` still matches. Beside it, the both-arms-closed refusal is unchanged and was re-measured on a full $150 core carrying its parlay: "the day is fully deployed — $150 of the $150 core is staked, less than one $5 minimum short, and the day's $25 fun money is already on a parlay — neither allotment has room, so no board is bought." What that wording was PROTECTING is still true and is now proved rather than asserted, in the `arms` docblock: both arms only ever CLOSE (core room and ticket slots only shrink, and a seated fun bucket is never empty again), so every attempt with the core arm open precedes every attempt without it, an attempt with both arms open charges BOTH counters, and no interleaving can reach a third board. THE WORST CASE PER DATE IS THEREFORE UNCHANGED at 1 + `CFB_TOPUP_MAX` = THREE priced boards, 18 Odds credits (6 a game-lines pull, measured on prod 2026-09-05, quota 17578 -> 17572; `CFB_TOPUP_MAX` 2 and `CFB_TOPUP_RETRY_MS` 45 min re-read in `src/lib/cfb/rules.ts` 2026-09-06), none of it visible to `CFB_PROPS.dailyBudget`, which counts props pulls only. A legacy row carrying no `arms` counts against BOTH arms — the direction that refuses MORE spending, never less — so a blob already in the store cannot buy an extra board on the deploy that ships this, exactly as a row with no `filled` keeps the old reading. What widened is still only the SET of dates that can reach the ceiling: a full-core day with an empty fun bucket used to refuse for FREE. AND ONE FREE REFUSAL WAS PUT BACK IN FRONT OF IT (2026-09-06, L1, `topUpDate` in `app/api/cfb/lock/route.ts`) — A FUN ARM THAT PROVABLY CANNOT SEAT ANYTHING NOW REFUSES BEFORE IT SPENDS: `if (!d.core && d.fun && openAhead < CFB_RULES.fun.legs.min)` sits ABOVE the claim and above `slateFromEspn`, alongside the other free refusals, because `CFB_RULES.fun` demands `legs.min` (3) legs, `oneLegPerGame` makes them DISTINCT games, and a game the core already sits on is spoken for — so fewer than three un-seated games still ahead means no fun parlay exists at any price and the board is not worth buying. `openAhead` is deliberately an OVER-estimate (start time ahead of `now`, not `status`), so the refusal fires only where seating is IMPOSSIBLE rather than merely unlikely; it is gated on `!d.core` so an open core arm is never held back by the fun bucket's arithmetic; and what it cannot prove without a board — a slate with enough games that still prices nothing inside the 4.0–40.0 band — still costs its pull, because that cannot be known without one. So the fun arm's price is up to 12 credits on a date with three or more open games still ahead, and NOTHING AT ALL on a date without them. AND AN ATTEMPT THAT SEATS NOTHING DECLINES FOR FREE INSIDE THE RETRY WINDOW — checked this turn rather than assumed, and it needed no new rule: `decideCfbTopUp`'s empty-attempt gate is ARM-AGNOSTIC, `if (last && isClaimRow(last) && now - last.at < CFB_TOPUP_RETRY_MS)` (quoted off `src/lib/cfb/lock-server.ts` this turn), and an attempt that seats nothing leaves its OWN claim row unfilled, because `topUpDate` answers `skipped` on an empty probe without ever calling `applyCfbTopUp`. A fun-only attempt therefore inherits the same 45-minute cooldown as a core one FROM THAT SINGLE LINE, rather than from a second refusal that would have to be kept in step with the first. MEASURED THIS TURN: a full-core day with an empty bucket claims attempt 1 with `arms: { core: false, fun: true }` and `filled: false`, and pokes at +15 min, +30 min and +44 min each answer `fire: false` — "top-up attempt 1 priced a board N min ago and found nothing to seat — the next attempt waits M more min rather than re-buy the same prices." — buying no board and spending no credit; past the window it is a cooldown and not a kill, and the arm buys its second board. So the per-date ceiling stays 1 + `CFB_TOPUP_MAX` = three priced boards and 18 Odds credits however often the day is poked. The note now ALWAYS states the fun outcome — "Fun: +$25 — $25 of the $25" or "Fun: nothing added — $0 of the $25" — because the silent $0-of-$25 day is what let this stand, and the answer carries `fun` / `funStake` / `funRoom` on `topped-up` and `would-top-up`, and `funRoom` on the "nothing clears the gate" refusal. THE CLAIM PROTOCOL, and what two overlapping pokes now cost: the stored entry is read TWICE — once immediately before the claim, where the ordinal is RE-DERIVED from that fresh copy (`decideCfbTopUp(held, now)`; if the day no longer fires, the poke answers `skipped` + `raced` and spends nothing), and again in the write block, where the whole decision is recomputed (room, slots, cap, ordinal) against the copy actually being written. Nothing is decided by the snapshot the request opened with. Both writes are still read-then-check-then-write and NOT atomic, so two pokes whose reads interleave can both write and the second SET takes the blob; what that costs is bounded and stated — an attempt spent without seating money, or one poke's appended tickets lost to the other's write, after which the day is short again and a later poke may re-seat under the same $150. What can no longer happen is the expensive pair: a completed attempt erased from the count, and two tickets sharing an id. The loser reports `raced` WHEN IT IS REFUSED OUTRIGHT — and since the two allotments became independent gates it often is NOT refused, which is a CORRECTION to what this bullet said two overlapping pokes cost (2026-09-06). MEASURED, in the `tests/cfb-lock-route.test.ts` pin "TWO OVERLAPPING POKES CANNOT PUSH EITHER ALLOTMENT PAST ITS OWN LIMIT": a racing writer's card lands carrying the full $150 of core over an EMPTY fun bucket, the loser is refused on the CORE arm and FIRES on the FUN arm, and the day ends with six core tickets summing to `CFB_PAPER.daily` and exactly ONE parlay summing to `CFB_PAPER.fun`, every id across both buckets distinct, over three ledger SETs (the winner's write, the loser's claim, the loser's top-up), the answer reading `action: "topped-up"` with `buckets: { core: false, fun: true }` and the claim row `{ at, n: 1, core: 0, stake: 0, filled: false, arms: { core: true, fun: false } }`. The invariant that pin exists for is unchanged and is now asserted TWICE, once per allotment: neither bucket can be pushed past its own limit. What changed is that the loser's 6 credits now buy the $25 nobody had seated instead of buying a `raced` refusal. AN ODDS OUTAGE NO LONGER SPENDS AN ATTEMPT (2026-09-06): the claim is written one line above `slateFromEspn` on purpose, so a crash or a platform kill between the two still costs the attempt — that is the failure mode a cap must have — but an outage is not a crash, and the LOCK path already answers this identical condition by refusing and writing nothing. The top-up now gives it the same treatment: on `slate.oddsMissing` and not dry, the store is re-read, the claim is rolled back through `releaseCfbTopUp` (which can only ever remove an unfilled row at that ordinal, never another poke's completed one), the shorter ledger is SET under the same `MAX_BYTES` check, and the date is stamped with the same `pl:cfb:oddsgap:v1:<date>` marker the lock's refusal stamps, so the sweep and the `ahead === 0` record can still tell an unpriced day from a missed one. The answer is `{ action: "skipped", oddsMissing: true, n, room, reason }` and the reason names the outage instead of blaming the card's rules; `?dry=1` on an outage writes nothing at all — no roll-back, no marker. Reported under `topUp` as `topped-up | would-top-up | skipped` (with `reason`) `| error`; it never throws, and the already-locked answer, its status code and its sweep are untouched whatever happens. THE SETTLE PASS (`CFB_SETTLE`, `settlePass` + `overlayCfbGrading` / `cfbSettleCandidate` / `cfbSettleReady`) — nothing settled a server-locked CFB day: the grading chain is browser-only (`gradeCfbEntry` ← `gradeCfb` ← `gradeCfbPending` ← the Ledger tab's button) and the scheduler's grading tick forwards to `/api/calibrate?grade=only`, which has no CFB code, so `cfbLedgerStats` read 0-0 forever and `cfbBankroll` stayed pinned at `CFB_BANK_BASE` (2500) — every later day Kelly-sized off a bankroll that could never move. The pass runs after the sweep on any poke with no explicit `?date`, and ITS ORDER IS ITS FAIRNESS (corrected 2026-09-06, then corrected again the same day): the queue tiers on the date's LAST ATTEMPT rather than on whether it was ever attempted — `const attempted = (e: CfbLedgerEntry) => Math.max(stampOf(e, "attemptedAt"), stampOf(e, "gradedAt")) || (e.grading ? 1 : 0);` over `const stampOf = (e: CfbLedgerEntry, k: string) => Number((e as Record<string, unknown>)[k]) || 0;` (`app/api/cfb/lock/route.ts` L492–L493, read this turn — the `Math.max(Number(entry.attemptedAt), Number(entry.gradedAt)) …` form this sentence used to quote matches no line in the tree and is corrected 2026-09-06 by the audit round) ascending, oldest-first inside a tier, so a date nobody has graded still goes first and the oldest unscored day (the one that has distorted the bankroll longest) leads the rest. THE TIER FIELD IS `attemptedAt`, AND A DATE WHOSE ESPN READ THROWS IS NOW STAMPED (2026-09-06, DEFECT S2): `gradedAt` is written in exactly one place and only for a date that produced a VERDICT, so a date whose `espnEvents` call threw pushed `{ action: "error" }`, was never stamped, and stayed at 0 or 1 — which sorts ahead of every successfully-read date (an epoch, ~1.78e12). With `CFB_SETTLE.maxDatesPerPoke` = 2, two such dates consumed the ENTIRE per-poke read budget on every poke for ever and every newer date answered `deferred`: the exact starvation I(b) closed, re-entered through the one door it left open. `settlePass` now records the date in `attemptedDates` ONE LINE ABOVE the call that may throw, and the write gate moved from `if (verdicts.length)` to `if (!args.dry && attemptedDates.size)`, stamping `attemptedAt: args.now` on every ATTEMPTED date while `grading` is still written only when the overlay actually changed and `gradedAt` only when a verdict came back. It is a SEPARATE field rather than a widened `gradedAt` because `gradedAt` has a second writer — `applyCfbGrading` stamps it on the phone — and `gradedAt` stays in the expression as the FALLBACK so a blob written by the previous deploy keeps its queue position instead of jumping to the front on the first poke after the ship. A dry poke stamps nothing, by contract. Oldest-first ALONE was not a fair budget, and neither was tiering on `grading != null`, which never changes once it is true: TWO permanently stuck dates therefore sat in the front tier for ever, took the whole `CFB_SETTLE.maxDatesPerPoke` (2) budget on EVERY poke, and a third date that could actually have finished was `deferred` for ever — its realized P/L never reaching `cfbBankroll`, which sizes every later card through `ticketKelly`. For the tier to rotate at all, the write block now stamps `gradedAt` on a date it READ even when the re-grade comes back byte-identical (`touched` is kept apart from `changed`; a date already stamped at this same instant is left alone), because a tier a stuck date can never leave is not a queue. What that costs is one extra ledger SET on a poke that read and learnt nothing — no ESPN read, no odds credit — and the log line says when no verdict moved. The never-attempted tier still drains at two dates a poke, after which the older tier gets the whole budget back. Candidates are CFB entries that carry money, whose EVERY ticket has at least one leg, dated on or after `CFB_PAPER.since` and NOT after the poke's own date (it never grades forward of today), and not already `grading.done`. The legless-ticket guard is deliberate: `settle()` in `src/lib/cfb/grade.ts` walks leg results, and an empty list has no lost, ungradable or pending leg — it falls through to `stood === 0`, returns a PUSH with the stake handed back, marks the day done and never looks again. Then, free: not `cfbSettleReady` (every kickoff in the entry's OWN games snapshot at least `CFB_SETTLE.finishMs` — 6 h, a long game plus overtime and a delay — in the past) → `immature`, no read; the `CFB_SETTLE.maxDatesPerPoke` (2) budget spent → `deferred`, no read, the next poke takes it; otherwise ONE keyless `espnEvents` read → `finalsFromEspn` (built with `oddsEvents: []`) → `gradeCfbEntry` → `overlayCfbGrading`. The budget is spent by the ATTEMPT, not the success, so a date whose read throws cannot hand its slot to the next date. WHAT IT NEVER OVERWRITES: `overlayCfbGrading` (a deliberate twin of the module-private `overlayGrading` in the `"use client"` `src/lib/cfb/store.ts`) never replaces a SETTLED ticket or leg result — won / lost / push stands, so a verdict the phone graded and synced up survives a server pass reading a scoreboard that has since changed — and it recomputes `done` from the ENTRY's own tickets so it can never claim more than the merged map holds. CORRECTION (2026-09-06): this bullet used to say `done` was "strictly stricter than `gradeCfbEntry`'s own `done`, which counts an ungradable ticket as finished". That reads like a safety property and was measured to be a STARVATION BUG, so it no longer holds. The two questions are now separated in `lock-server.ts`: SETTLED (won / lost / push) is what may never be overwritten, and RESOLVED (won / lost / push OR ungradable) is what `done` counts — the same rule `gradeCfbEntry` applies with `result !== "pending"`, deliberately mirrored rather than invented. A LEG STILL PENDING 48 H PAST ITS KICKOFF IS A VOID, whatever status ESPN last reported (`CFB_UNGRADABLE_MS`, `src/lib/cfb/grade.ts`; corrected 2026-09-06 — the escalation used to ALSO demand that the game be absent from the finals map or carry `status === "postponed"`, so a lightning-suspended or abandoned game that ESPN goes on serving as `live` stayed `pending` for ever, and the escalation is now the clock alone) — WITH ONE EXCEPTION SINCE DEFECT C1 (2026-09-06): a game that IS final but whose score cannot be READ waits `CFB_VOID_RECHECK_MS` (604,800,000 ms, seven days) instead of `CFB_UNGRADABLE_MS` (172,800,000 ms, 48 h), both constants re-read this turn, because there the result EXISTS and only our read of it failed, so voiding on the 48-hour clock books a real winner as a wash. The pure helper `voidWindowMs` (`src/lib/cfb/grade.ts`) picks the window on `f.final === true && f.status !== "postponed"` with `readScore(f.home) === null || readScore(f.away) === null` (quoted off that function this turn; it read `Number.isFinite(Number(f.home))` until defect C1), and the void's own detail says which clock ran ("48h past kickoff — void" or "7d past kickoff — void"). MEASURED THIS TURN through `gradeCfbEntry`: a final whose scores are `undefined`, and one whose scores read `"TBD"`, are both still `pending` with `done: false` at kickoff + 49 h and `ungradable` with `done: true` at + 8 d, while a game ESPN still calls `live` at + 49 h voids at 48 h exactly as DEFECT I(a) left it — nothing is reverted, one shape is escalated later. A FINAL WHOSE SCORES CANNOT BE READ IS NO LONGER BOOKED AS A 0-0 PUSH THAT CAN NEVER BE CORRECTED (defect C1, 2026-09-06). The TRAP this bullet used to state — that `{ final: true, home: null }` "does NOT take this branch, since `Number(null)` is 0 and finite, so a null-scored final grades as a genuine 0-0 game" — is WITHDRAWN, and it was never a curiosity: `Number(null)` and `Number("")` are both 0, so `gradeCfbLeg` scored an unreadable final as a REAL 0-0 tie, which is a `push` — and `push` IS in the overlay's SETTLED set, so the wash was permanent and no later read of the true score could ever replace it. One private helper now answers the question once for both readers: `readScore` returns a number only for a finite number or a non-blank numeric string and `null` for everything else, `gradeCfbLeg` returns `{ result: "pending", detail: "score unavailable" }` when either side reads `null`, and `voidWindowMs` mirrors it — so the null shape gets the seven-day window `undefined` and `"TBD"` already had, and `pending` is in NEITHER the SETTLED nor the RESOLVED set, so the date stays a `cfbSettleCandidate` and the real score lands when it arrives. MEASURED THIS TURN through `gradeCfbEntry` on a $25 single: `{final: true, home: null, away: null}` and `{final: true, home: "", away: ""}` are both `pending` with `done: false` at kickoff + 49 h and `ungradable` / payout 0 / `done: true` at + 8 d, while a REAL `{final: true, home: 0, away: 0}` still `push`es at + 49 h with `done: true` and payout 25 — the half that must not move, and did not. Its live reach is narrower than the rule sounds, and that is stated rather than implied: `finalsOf` in `src/lib/cfb/slate-server.ts` skips a final whose scores are null, so the ESPN path emits such a game as ABSENT, and C1 bites on finals maps from other producers — persisted, merged or hand-built. AND A VOID IS NOT in the overlay's SETTLED set, so a real final arriving later still overwrites it — which is what DEFECT S1 below turns from a theoretical possibility into a reachable one — while under the OLD `done` rule such a date could never reach `done`, stayed a `cfbSettleCandidate` forever, and spent one ESPN read per poke rewriting byte-identical grading; with `CFB_SETTLE.maxDatesPerPoke` = 2, TWO stuck dates consumed the entire per-poke budget and every newer date was deferred forever — `cfbBankroll` frozen, which is the exact failure this pass exists to end and which sizes every later card through `ticketKelly`. There is only ONE grader, so that widening landed on both surfaces at once: `src/lib/cfb/store.ts` calls `gradeCfbEntry(entry, finals)` for the phone and `app/api/cfb/lock/route.ts` calls `gradeCfbEntry(e, finals, args.now)` for the poke, and both overlays count `ungradable` as RESOLVED, so the void is terminal on the device and on the cron together. A VOID IS PROVISIONAL, NOT TERMINAL (2026-09-06, DEFECT S1 — and this bullet's own earlier claim that "the void is terminal on the device and on the cron together" is WITHDRAWN, because terminal is a stronger claim than the desk can support). THE SAME CLAIM WAS STRUCK FROM THE CODE ITSELF (2026-09-06, L3), since a comment that contradicts the test beside it is worse than no comment: `src/lib/cfb/lock-server.ts`'s SETTLED / RESOLVED block no longer says "no later read of any scoreboard will ever turn a void into a score" and now says RESOLVED means "is this poke DONE with the ticket for now", naming DEFECT S1 as the disproof and pointing at `overlayCfbGrading` for what a late read must SHOW; `overlayCfbGrading`'s own docblock no longer calls a never-finalised game's verdict "the desk's own terminal state"; `src/lib/cfb/store.ts` carries the same withdrawal beside its own `overlayGrading`; and the describe in `tests/cfb-lock-route.test.ts` was renamed from "a void date is terminal, and a stuck date never starves a fresh one" to `CRITIC 2 (2026-09-06) — a void date stops being re-poked, and a stuck date never starves a fresh one` (`tests/cfb-lock-route.test.ts` L2018, grepped verbatim this turn), which is the SCHEDULING property that block actually pins; the name is given in full here because the version of this sentence the audit round found on 2026-09-06 quoted it in emphatic capitals and without its `CRITIC 2 (2026-09-06) — ` prefix, and neither string is what the file holds. No assertion changed at any of those four sites (all read 2026-09-06). `cfbSettleCandidate` was `entry.grading?.done !== true` and nothing else, so the clock-alone escalation made `done` reachable BY a void and the date then left the queue FOR EVER: a game suspended for weather and resumed 50 hours later — which ESPN serves as `live` throughout — is `pending` at K+48h-1ms and `ungradable` with `done: true` at K+48h+1ms, after which no later ESPN read was ever made for it, so `ticketPL` (`src/lib/bankroll.ts`, the push/void/pending arm) booked a genuine winner as a wash and `cfbBankroll` — which Kelly-sizes every later day — stayed low by that amount permanently. A date whose ONLY unsettled verdicts are voids is now a candidate until `CFB_VOID_RECHECK_MS`, SEVEN DAYS (`7 * 24 * 3600_000`, `src/lib/cfb/rules.ts`), past its LAST kickoff — measured by the new `cfbLastKickoffOf`, which is `cfbSettleReady`'s own body extracted so the two windows can never be read off different clocks — and closed for good past it. Three cases, deliberately distinct: not done, a candidate exactly as before; done with NO void, never re-read, exactly as before; done WITH a void, a candidate inside the window only. Seven days because a resumed or rescheduled college game is played inside the same week (the NCAA's own make-up practice is the following weekend at the latest), and because it spans a full ticker week, so a date cannot be closed by an outage that happens to straddle a weekend. It costs at most ONE keyless ESPN read per voided date per poke and ZERO Odds credits, and it cannot re-open the starvation DEFECT I(b) closed, because a date read on this poke sorts to the BACK of the queue. Nothing about the 48-hour escalation itself changed — the desk still says "void" while it waits, and still stops waiting — only its permanence; and a real final that arrives inside the window still lands, because `ungradable` is not in the overlay's SETTLED set — BUT ONLY IF IT IS CORROBORATED (2026-09-06, L2, `overlayCfbGrading` in `src/lib/cfb/lock-server.ts`). Re-opening a voided date for seven days is right for the money and it opened a door that was shut before it: inside the window a SINGLE later ESPN read could overwrite a real 48-hour void with a win or a loss, nothing refused it (`ungradable` is deliberately not in SETTLED), and the replacement IS settled, so nothing could ever correct it afterwards. `settle()` (`src/lib/cfb/grade.ts`) carries two shapes that reach a verdict off an INCOMPLETE payload — it returns "lost" as soon as ANY leg lost, with the other legs still unread, and it hands a LEGLESS ticket a "push" with the stake back through its `stood === 0` arm — so a stored `ungradable` may now be replaced only when EVERY leg of that ticket is SETTLED in the INCOMING leg map, which is exactly the condition `gradeCfbLeg` requires before it returns won / lost / push (`f.final`, both scores finite), read off what the grader itself computed rather than re-derived. A leg-level push IS corroboration — it comes from a real final; a ticket-level push over NO legs is not. THE COST, stated rather than buried: a parlay with one genuinely lost leg and one leg that never finalises stays a $0 void instead of being booked at −stake, which is the direction this desk was already in before DEFECT S1 made a void re-readable at all, so it only narrows the NEW window's power, and `ticketPL` scores it 0 either way until the missing game finalises — at which point the whole ticket IS corroborated and the real verdict lands. The one-way-in-time edge is pinned explicitly (a corroborated final lands inside `CFB_VOID_RECHECK_MS` and cannot after it). The LEG map is deliberately NOT gated, and the docblock says why: a leg verdict is not money, it is what `src/components/cfb/CfbTicketCard.tsx` prints, and a leg carrying a real final should say so under a ticket the desk is still calling void. THE DEVICE DOES NOT HAVE THIS WINDOW, flagged rather than fixed: `gradeCfbPending` (`src/components/cfb/CfbLedger.tsx`) still selects on `!e.grading?.done`, which is exactly the rule the server had before this change, so a phone that never syncs would show a void beside a settled record for ever. It is the milder half — the SERVER writes the money and the bankroll, and the device's own merge takes the richer copy on sync — and it is written into `cfbSettleCandidate`'s docblock rather than left to be rediscovered. SECOND CORRECTION, the same defect wearing another hat: `if (!cur) return inc` bypassed the whole rule on a FIRST pass, so `done` came from `gradeCfbEntry` when no grading existed yet and from this function on every later pass — the same day finished or unfinished according to whether an earlier poke happened to run, an order-dependent verdict on money. That clause is gone; an absent `cur` is simply an empty one, one rule runs on every pass, and overlaying twice equals overlaying once. Both halves of the never-overwrite rule are pinned — the ticket map AND the leg map that `src/components/cfb/CfbTicketCard.tsx` prints under each ticket — and the leg pin also asserts that a PENDING leg still takes the incoming verdict, so the guard cannot be "fixed" by freezing everything. The write re-reads the store and overlays onto the CURRENT stored copy, stamping `gradedAt`, and deliberately does NOT go through `mergeLedgers`: this pass adds no entry, it annotates ones that exist, and `pickBase` would have to choose between two copies of one day — the choice this pass must not make. COST: zero Odds API credits ever, at most two ESPN reads per poke, dropping to ZERO the moment the backlog is scored (a graded day fails the free ledger check and is never read again; a live day answers `immature` off its own games map). After a settle write the in-memory ledger and `bankroll` are recomputed, so the day's own card is sized off a bankroll that finally moves. Reported under `settle` = `{ reads, wrote, days: [{ date, action: settled | would-settle | immature | deferred | nothing | error, … }] }`; a per-date failure never changes the poke's status code, body or sweep, and an explicit `?date` suppresses the pass exactly as it suppresses the sweep. THE MONEY GUARD (2026-09-06) — `buildCfbLockEntry` validated only with `validateCfbLedger`, which checks ledger SHAPE and the sport tag and nothing about the money, so a $200 core, a $40 ticket or an $80 fun parlay would have serialized cleanly onto Josh's ledger. `assertCfbCardMoney` now runs before anything is stamped: core sum ≤ `CFB_PAPER.daily` (150), fun sum ≤ `CFB_PAPER.fun` (25), core tickets ≤ `CFB_RULES.tickets.max` (7), every core stake inside `CFB_RULES.minStake`–`maxStake` ($5–$25), with a 1e-9 epsilon that is float slack and not a tolerance; and, added 2026-09-06, NO TICKET ID TWICE across `[...core, ...funT]`, which is ONE id space — the ids key the grading map, so a repeat would hand two bets a single verdict and put a payout on the ledger that no bet earned. Every message prints BOTH numbers and ends "Nothing written. STOP." `assertCfbEntryMoney` runs the same thresholds over the MERGED entry inside `applyCfbTopUp`, because a $75 top-up card is legal on its own and illegal only once appended to a day already carrying $75. A throw is a 502 with nothing written (a top-up's is caught and reported under `topUp`), so a broken sizer costs a day loudly instead of mis-staking one quietly — the CFB twin of `src/lib/server/lock-card.ts`'s "a crash, never a quietly wrong card". A BLOB NOTHING CAN READ IS NOT AN EMPTY LEDGER (2026-09-06, the critic's pass) — both readers used to map a JSON parse failure, or a value of the wrong shape, to `null`, which is the SAME answer they give for a key that does not exist, so every write path below read it as "the ledger is empty", merged the day's one entry into nothing and SET the blob: the whole CFB season replaced by a single entry, which the device's own sync would then merge back down onto the phone. A MISSING key still IS an empty ledger (the desk's first poke ever must lock normally), but `readStore` now THROWS `CfbStoreUnreadable` on a parse failure or a missing `ledger` array, and `readBank` throws on a parse failure, a missing `.bank`, or a `validateBankStore` failure — a card silently sized off `CFB_BANK_BASE` because the stored bank failed its own validator is a wrong stake, not a missing one. The answer is a 502 with NOTHING written and the next poke retries; the six downstream `readStore()` calls (the sweep write, the settle write, the top-up claim, the outage roll-back, the top-up write and the lock write) all sit inside try/catch that report and write nothing. A day retried is cheap; a season overwritten is not recoverable from anything this server holds. FORWARD — no new external ticker: `app/api/scheduler/route.ts` renamed its body `mlbTick` (byte-identical) and the exported GET runs it FIRST, then gates on AUTHORISATION ALONE — `if (!process.env.CRON_SECRET || !cronHeaderAuthed(req)) return res;`. That is a 2026-09-06 correction of the first cut's `if (res.status !== 200 || !process.env.CRON_SECRET) return res;`: `mlbTick` answers 503 when `storeEnv()` is false and Next renders any unhandled throw inside it as a bodyless 500 — those are purely MLB-SIDE — and `vercel.json` declares crons only for `/api/scheduler` (two entries, both `?poke=`), so there is NO other path to `/api/cfb/lock` and one MLB-side outage silently cost the CFB desk the entire $150/$25 day, nothing written, no CFB-side signal. CORRECTION (2026-09-06): this bullet previously listed the tick's 401 among those MLB-side outages and claimed a "401-shaped body still forwards". It does not, and it must not. The tick's 401 (a header problem) and its CRON_SECRET-unset 503 are AUTHORISATION, not an outage, and the gate refuses both BY NAME before the forward — `cronHeaderAuthed(req)` is false in the first case, `process.env.CRON_SECRET` unset in the second — so the MLB answer returns untouched and the server never reaches `/api/cfb/lock` carrying the real secret off an unauthenticated public poke. What now forwards that did not before is the MLB-SIDE 503 (`sync-not-configured`) and the 500; the CFB answer then rides under `cfb` with the MLB status and every MLB field untouched (`attachCfb`; `cfb` is the only added key, and a body it cannot parse is returned as-is). The auth half is load-bearing in the other direction: an UNAUTHENTICATED public poke returns before the forward, so the server never reaches `/api/cfb/lock` carrying the real secret. `forwardCfbLock` (`src/lib/server/cfb-lock-forward.ts`) sends the same `x-cron-key` header the generate forward sends, `cache: "no-store"`, and `AbortSignal.timeout(CFB_LOCK.forwardTimeoutMs)` (25 s); its result is ADDITIONALLY `.catch`-wrapped inside GET, because the fire-and-report guarantee must live in the file that claims it — the first cut's bare `await` let a rejection escape and Next rendered the poke as a bodyless 500, which the pin caught with `Error: ECONNRESET`. THE BUDGET, stated honestly (corrected 2026-09-06): the generate forward ahead of it is sent with NO signal and NO timeout at either call site, so ~60 s of generate plus this 25 s abort is 85 s of the scheduler's 90 s `maxDuration` — FIVE seconds of headroom, and a generate slower than 65 s kills the invocation before the abort can bind. What IS guaranteed: the cap bounds the CFB forward's own share, and the forward is the tick's LAST step (after `mlbTick` has answered and after `/api/generate` has committed under its own 300 s budget), so a platform kill costs the poke its HTTP answer and that pulse's CFB lock, never an MLB write, and the next pulse retries. Closing the 5 s gap for real means timing the generate fetch, in a file this change does not own. WHERE THE WITHDRAWN VERSION STILL STANDS, so nobody re-derives it from the code: `src/lib/server/cfb-lock-forward.ts`'s own header still says the abort means the forward "can never cost the poke its 90 s function budget" — that is exactly the claim this paragraph withdraws, and the scheduler route's header now carries the corrected wording beside its own CORRECTION about the tick's 401 (read 2026-09-06). UI — `CfbBuilder.lockedLine` appends " Locked by the server at H:MM PT." on a `source: "server-lock"` day and renders the entry's `note` under it (which is why the sweep's and the top-up's wording is held to what it can prove); the manual LOCK still refuses an already-locked date. The phone reaches the server entry through the existing pull → merge → push in `src/lib/cfb/sync.ts` (`mergeLedgers` unions by date; a device copy of the same date outranks the server's in `pickBase` by grading richness, so a phone lock is never replaced). THE CORE UNION (2026-09-06, `src/lib/ledger-merge.ts`) — that same sync path was silently deleting the top-up's money. `mergeDay` unioned `funT` by ticket id and never `core`, because until this server lock shipped only the supplemental FUN lock ever appended to an already-locked day; the funT union exists for exactly that hazard, and the CFB top-up now appends CORE tickets hours after the lock for exactly the same reason, so core had become what funT has always been. The sequence: Josh pulls the $75 server entry at noon and the Ledger tab grades it, so his copy gains grading richness; the server tops the day up to $150 at 1pm; his app pushes at 2pm; `pickBase` prefers his graded $75 copy and `out` is a deep copy of it — the appended core tickets and the day's extra $75 are DELETED from durable state, while the ledger goes on calling it a full paper day, `ledgerStats` scores $75, and `cfbBankroll` sizes every later day off the wrong number. `core` is now unioned as well, but GUARDED rather than blind, because CFB core ids are POSITIONAL (`cfb-<date>-core-<i>`, `src/lib/cfb/card.ts`) and not content-addressed the way MLB's are (the engine's `shTicketId` is the ticket type plus a hash of its sorted legs, so one bet carries one id on every device), which means two independent locks of one CFB date — an unsynced Builder lock and the server's — both mint `cfb-<date>-core-1` over DIFFERENT bets, and a blind union would stake a Saturday twice. TWO GATES, both of which a genuine append passes by construction: AGREEMENT, the two sides must mean the same wager by every id they SHARE (legs projected onto `lkey` / `gkey` / `label` / `prop` / `market` / `side` / `line` — never a price, never a stake, because a re-quoted leg is the same wager and MLB's own `withTopUp` legitimately raises a shared ticket's stake under its existing id), which an append always satisfies because it copies the existing tickets through byte for byte, while two rival cards disagree on the very first positional id and the union then refuses ENTIRELY and the base's card stands whole — the behaviour that shipped before this block, and what the route's own "a device copy of the SAME date outranks the server's" pin expects; and ALLOTMENT, appends may never carry the merged core past the entry's own recorded `daily` (CFB stamps `daily: CFB_PAPER.daily` in `src/lib/cfb/ledger.ts`), so merged core stake is at most max(the base's own core stake, `daily`) and no merge can inflate a day even if two rival cards coincidentally agreed on their shared ids — and when NEITHER side carries a usable `daily` the bound falls back to the DESK'S OWN allotment — `CFB_PAPER.daily` for a `sport: "cfb"` entry, `PAPER.daily` otherwise, both 150 — rather than to unbounded. That is a 2026-09-06 correction: the rule this bullet previously stated, that a `daily`-less day "unions unbounded rather than lose a real append to a missing number", is WITHDRAWN, because it let a legacy day be staked past the desk's own allotment by a merge — measured on a SYNTHETIC pair recorded in `tests/ledger-merge.test.ts`, a graded $75 copy and a copy carrying two extra $100 tickets, both with `daily` deleted, merging to a core sum of 275 on a day whose ceiling is $150, with no guard anywhere downstream of a merge to catch it. `validateLedger` still does NOT require `daily`, deliberately: `buildReasonRecord` runs its own no-bet output through that validator and emits no `daily`, so requiring the field would turn a latent merge hazard into a live outage on every no-bet day. THE UNION CARRIES RAISES AS WELL AS APPENDS (2026-09-06, `unionCore`): under the same two gates it does appends FIRST and then raises, each in ticket-id order against one running sum, and a shared id whose other side carries the LARGER stake takes that stake — carrying `topUp` with it, or dropping a stale one, because `stake − topUp` is how the allocator's own sizing stays recoverable. THE RAISE NEEDS A RECEIPT, and the justification this bullet used to give for it is WITHDRAWN (defect N3, 2026-09-06). Plain larger-wins shipped on the claim that "no deliberate writer on either desk lowers a stake under a fixed id"; the kernel file now records that as FALSE on the MLB rail, because `buildModeCard` (`src/lib/server/lock-card.ts`) applies its carried-ids/legs exclusion only to the FORCED pass's `rest`, so a later block fire re-picks a bet an earlier fire already staked and — MLB ids being content-addressed — re-mints the SAME id at a smaller pro-rata stake, which the carry filter then lets replace the larger one. MEASURED there: a device holding the earlier $40 copy PUTs it and plain larger-wins raised the stored $12 straight back to $40, merged core 90 on a day whose own `allocSum` records 62, with nothing anywhere to reconcile the two. So the union takes the larger stake ONLY when the raiser's own `topUp` accounts for the delta — the one shape `withTopUp` can produce, and a statement that the smaller stake IS this ticket's own pre-top-up allocator sizing. THE RECEIPT IS A DIFFERENCE, NOT AN ABSOLUTE (2026-09-06, defect K1): the rule this bullet stated a day ago, `m.stake - t.stake === m.topUp` to 1e-6, is CORRECTED, because it is right only for a base carrying NO `topUp` of its own and a two-fire day breaks that assumption. The gate now reads `const receipted = Number.isFinite(tu) && Math.abs((Number(hi.stake) || 0) - (Number(lo.stake) || 0) - (tu - prev)) < 1e-6;` over `const prev = Number(lo.topUp) || 0;` and `const tu = Number(hi.topUp);` (`src/lib/ledger-merge.ts` L975–L977, read verbatim this turn; the `Math.abs(delta - (tu - prev)) < 1e-6` / `prev = Number(t.topUp) || 0` form this sentence carried was written before the pass moved onto the unordered `hi`/`lo` pair and matches no line in the file — corrected 2026-09-06 by the audit round, which also found that `delta` now names a local in `carryMoneyMeta` and nothing else), so a SECOND top-up on a ticket an earlier fire already topped up is CARRIED rather than refused. MEASURED against the old rule: a stored 22/topUp 2 merged with a fresher 25/topUp 5 stayed stuck at 22, and at fire-1/fire-2 scale a 40/topUp 10 against a 50/topUp 20 DELETED from durable state the $10 the desk had actually deployed. Both N3 refusals still hold on a topUp-carrying base and are pinned in both merge orders: no receipt at all (`tu` not finite) is refused, and a receipt that does not explain the delta (40/topUp 10 against 50/topUp 5) is refused, leaving the stored value standing exactly as it did before the raise pass existed. `topUp` travels WITH the stake it explains, because `stake - topUp` is how the allocator's sizing stays recoverable. The `buildModeCard` double-pick is a pre-existing MLB ALLOCATOR defect and is NOT fixed — this gate only stops a merge laundering its output into durable state — and it is named here so nobody reads the receipt as a claim that the allocator is sound. CFB never travels this pass at all: `planCfbTopUp` mints new ids, so a CFB top-up is always an APPEND. Appends-before-raises is load-bearing rather than arbitrary: with raises first a $15 raise could eat a whole $25 append's seat under the cap, and a refused append loses a wager from the record entirely while a refused raise only understates one. Appends are deep-copied and ordered by ticket id, so a merged day is a pure function of its two inputs whatever order either side stored its tickets in; a shared id is a FILL, never a second copy, so a re-merge cannot double-stake a day; and `mergeDay`'s reopen rule sees the appended tickets and flips `grading.done` back to false so the grader picks them up. A RAISE ALSO INVALIDATES THE VERDICT IT WAS PRICED UNDER (defect N1, 2026-09-06): a stake and the payout beside it are ONE quote — `settle` (`src/lib/cfb/grade.ts`) prices `payout = stake x dec` — so the moment the union raises a shared id, the verdict already sitting in `grading.tickets` describes a bet that no longer exists, while `done: true` tells every grader there is nothing left to recompute; the reopen rule cannot catch it, because that fires on a MISSING id and a raised id is PRESENT. MEASURED on the MLB PUT rail: a phone copy of 2026-09-01 that graded `p:abc` won at payout 76 (= 40 x 1.90) merged against a server copy whose residue top-up had raised `p:abc` to $60 gave stake 60 / payout 76 / done true, realizedPL -34 and `computeBankroll` 2466 against an honest +4 / 2504 — a $38 permanent error on ONE ticket, flowing into `ticketKelly` and every stake sized after it. `unionCore` therefore returns `raised`, the ids whose stake actually MOVED, and `mergeDay` DELETES those ticket verdicts and sets `done: false`. DELETE, NOT RESCALE, because the record does not reliably hold the settling decimal: `dec` is written on `won` and on the all-legs-pushed `push` only (never on `lost` / `pending` / `ungradable`; `TicketGrade` in `src/lib/useLedger.ts` marks it optional), a PUSH payout is the stake handed back rather than stake x dec, and a won parlay's `dec` is the product of the legs that STOOD — so rescaling would invent money, while deleting hands the arithmetic back to the grader, which reads finals AND the new stake. Between the merge and the re-grade the ticket scores 0, which is exactly what `ticketPL` already returns for an ungraded ticket and the honest state: nobody has yet priced a $60 winner. Its legs go too, but ONLY the ones no surviving ticket still references — leg verdicts are stake-independent, so that half is hygiene rather than money, and a leg another ticket still owns is left standing because that ticket's verdict still stands. The withdrawal runs AFTER the fill-only grading-map merge, not beside `out.core`, or the loser's copy of the same stale verdict would be merged straight back in. AND THE MLB MONEY METADATA NOW FOLLOWS THE UNITED CORE (defect K2, 2026-09-06): the union appends tickets, and until this turn the merged day kept the BASE's `allocSum` / `gatedSum` / `topUpSum` — a stale statement of how much of the day's budget is spent, sitting on a day that has just grown. That is money, not bookkeeping: `decideTopUp` (`src/lib/server/blocks.ts` L189) computes `const owed = daily - Number(entry.allocSum ?? 0);`, so a day already carrying the full $150 core with a stale $75 `allocSum` reads owed = $75, fires another top-up generate run — roughly 120 Odds credits — and deploys a THIRD block on top of a full allotment; nothing downstream compares the core stake sum to `daily`. It is reachable in production because `buildLockEntry` copies neither `grading` nor `clv` from `carry`, so a stored day that has accrued a CLV sighting reliably outranks the fresher entry on `pickBase`. `carryMoneyMeta` now moves each field by exactly what the union added, on the SAME projection the write path sums (`MONEY_META`: `allocSum` = Σ stake, `gatedSum` = Σ (forced ? 0 : stake − topUp), `topUpSum` = Σ topUp), which RESTORES the write path's own invariant rather than inventing a number — and note a RAISE moves `gatedSum` by zero on its own (before `S − p`, after `(S + tu − p) − tu`), which is right, because residue is never allocator sizing. ONLY FIELDS THE BASE ALREADY CARRIES ARE MOVED, so a merge cannot mint an MLB allocator field onto a CFB day; CFB is immune to the underlying defect anyway, because `planCfbTopUp` reads the money straight off the tickets (`const staked = cfbStakeOf(entry.core)`, `src/lib/cfb/lock-server.ts` L462) and never consults `allocSum`. `blocks` is unioned unconditionally beside `games` — it is an accrual map keyed by block id, and a block fire recorded only on the loser must survive exactly the way an appended ticket does. `capResidue` is DELIBERATELY left alone: it is not cumulative and not a position but what the $25 per-ticket ceiling could not absorb IN ONE FIRE (`capResidue: primaryCard.capResidue`, `src/lib/server/lock-card.ts` L573), so a merge — which performs no allocation — recomputing one would be inventing a fire that never ran, and the base's is the honest record of the last fire that did; the cost is that on a merged day the note's residue line describes that earlier fire and not the merged whole. THE FUN UNION IS NOW BOUNDED BY THE FUN ALLOTMENT (defect N2, 2026-09-06): the funT union had never carried a cap at all, which was harmless purely by accident — every CFB copy's parlay was the single id `cfb-<date>-fun-1`, so no new id could ever be found to append, and MLB's fun bucket is built once per day and carried byte for byte afterwards — until `planCfbTopUp` started minting a SECOND distinct id, `cfb-<date>-topup<n>-fun-1`, and the append became reachable. MEASURED: two overlapping pokes of 2026-09-05, the phone holding `topup1-fun-1` and the blob's last write carrying `topup2-fun-1`, merging to a funT sum of $50 on a $25 day — and `assertCfbCardMoney`'s funSum guard runs only on the server WRITE paths while `validateLedger` looks at dates and accrual shapes and never at stakes, so NOTHING on the merge rail would have caught it. `funCap` is the LARGER OF THE TWO RECORDED CLAIMS held under the desk's own allotment as a CEILING — `return recorded.length ? Math.min(desk, Math.max(...recorded)) : desk;` over BOTH sides' recorded `fun` (`CFB_PAPER.fun` 25 for a `sport: "cfb"` entry, `PAPER.fun` otherwise), read off `funCap` (`src/lib/ledger-merge.ts` L446–L453) this turn and identical in `allotmentCap` (L375–L382) on the core side. THE DIRECTION AND THE CITATION ARE CORRECTED here 2026-09-06 by the audit round: this sentence quoted `Math.max(desk, ...recorded)` with the desk "as a FLOOR", a form that survives in the tree at exactly ONE place — the docblock at `src/lib/ledger-merge.ts` L415, which records it as the answer A1 gave and says in the same breath that it "WAS RIGHT ABOUT THE FLOOR AND WRONG ABOUT THE CEILING" — and a floor-only rule lets the larger of two recorded claims RAISE the merged day past the desk's own $25, which is the one thing this gate exists to refuse; the `base.fun ?? other.fun` shape N2 shipped is WITHDRAWN (defect A1's second shape, 2026-09-06), because `pickBase` chooses the base on grading richness and that is no claim at all about a day's allotment, so a copy carrying a token `fun: 1` shrank the whole merged day's ceiling to $1 and refused a real $25 wager into an empty bucket — the gate exists to stop a merge INFLATING a day past what a writer recorded, never to shrink one below it. AND A FUN TICKET CARRYING A SETTLED VERDICT IS NEVER DROPPED BY THE $25 CAP IN FAVOUR OF AN UNGRADED ONE (defect A1, 2026-09-06, the regression this closing round exists to shut): the collision rule was `pickBase` plus ticket-id BYTE ORDER, and neither of those is a fact about the money, so the cap could delete a parlay that had already WON and seat a loser in its place. `unionFun` now ranks its candidates by (1) does EITHER side's `grading.tickets` carry a won / lost / push verdict for this id, then (2) the larger settled payout, then (3) the order the cap always consumed — the base's own bucket in the base's own order, then the ids only `other` holds in ticket-id order. Key (3) is the old behaviour byte for byte, so a day with no settled money anywhere seats exactly what it seated before and every N2 / K4 pin is arithmetically untouched; and the choice is ORDER-FREE, because `verdictOf` reads BOTH sides' grading, making a candidate's rank a property of the input PAIR rather than of which copy won `pickBase`. MEASURED THIS TURN by driving `mergeLedgers` directly, all four combinations: a $25 fun ticket graded `won` at payout 47.73 against a rival $25 parlay carrying NO verdict is the one seated and the ungraded id is the one on `funDropped` — in BOTH argument orders, and with the settled id at EACH end of the byte order (`zfun-1` beating `afun-1`, and `afun-1` beating `zfun-1`). The bias toward the winner is answered rather than denied: the day now also carries `funDroppedPL`, which records the dropped ticket's `result`, `payout` and `stake`, so no settled money is deleted in silence in either direction — measured on the graded pair, `funDroppedPL {"cfb-2026-09-05-afun-1":{"result":"lost","payout":0,"stake":25}}`. WHAT THE CAP REFUSES IS NOW NAMED RATHER THAN SWALLOWED (defect K4, 2026-09-06): the loop used to do `if (sum + stake > cap + 1e-6) continue;` and nothing else, so a ticket the allotment could not seat simply VANISHED — not counted, not flagged, no trace for anyone reconciling the ledger against a phone that still shows the bet. The cap STAYS (dropping is the correct outcome; silence was not): `unionFun` now returns `FunUnion<T> | null` — `type FunUnion<T extends SyncEntry> = { funT: NonNullable<T["funT"]>; dropped: string[]; droppedPL: Record<string, DroppedPL> };` at `src/lib/ledger-merge.ts` L616, read this turn: THREE fields, not the two this sentence claimed until the audit round corrected it 2026-09-06, because the receipt (`droppedPL`) travels with the id list and not beside it — `mergeDay` accrues the ids into `funDropped` FROM BOTH SIDES so an earlier merge's drop is not lost, removes any id that has since been seated so the marker never outlives the loss it names, and `console.warn`s the day, the cap and the ids. MEASURED this turn: two $25 fun tickets `f1` and `f2` on one 2026-09-05 day merge to funT 1 / $25 with `funDropped: ["f1"]` and the warning "the $25 fun allotment is spent — 1 fun ticket(s) refused by the merge and NOT recorded on the day: f1". AND THE LOOP ITSELF IS NOW EXPORTED (defect K5) for the same reason `unionCore` was, so the fun allotment lives in ONE place and the device rail shares it: `export function unionFun<T extends SyncEntry>(base: T, other: T): FunUnion<T> | null {` (`src/lib/ledger-merge.ts` L747, grepped this turn; CITATION CORRECTED 2026-09-06 in the final round — this sentence spelled the return type inline as `{ funT: NonNullable<T["funT"]>; dropped: string[] } | null`, which no longer greps: the shape moved into the named `FunUnion<T>` alias and gained a `droppedPL` member), generic in the entry because `CfbLedgerEntry` narrows `funT` to a required `CfbTicket[]`, with the single cast made once inside the function and sound by construction (every element is a base element or a deep clone of an `other` element). AN OVER-CAP DAY IS ALSO VISIBLE NOW, AND IS KEPT RATHER THAN TRUNCATED (defect K3, 2026-09-06): after the unions `mergeDay` recomputes `coreSum` and `funT` sum against `allotmentCap` and `funCap` and stamps `capBreach { core?: {sum,cap}, fun?: {sum,cap} }` with a `console.warn`. MEASURED this turn: a stored $75 single-ticket day merged with a seven-ticket $175 copy comes out as 7 tickets / $175 with `capBreach: {"core":{"sum":175,"cap":150}}` and the warning "the merged day is OVER ITS ALLOTMENT and was kept, not truncated — core $175 of $150"; re-merging the result with itself yields the identical breach, so the marker is idempotent and is removed once the breach is gone. TRUNCATING WOULD DELETE WAGERS A DEVICE STILL SHOWS — the same silence K4 exists to end — so this is a DETECTOR, not an enforcer: no current writer can mint an over-cap entry (`assertCfbCardMoney` throws on `coreSum > CFB_PAPER.daily`; `buildLockEntry`'s TWO ALLOCATORS pair refuses a card the allocator did not size), and the merge is simply the last place a corrupt stored blob would otherwise be laundered into the durable record. AND A MERGED DAY CANNOT SHOW NO-PLAY OVER STAKED MONEY, IN EITHER BUCKET (defect N4, 2026-09-06; the QUOTE AND THE SCOPE CORRECTED here 2026-09-06 in the closing round — this sentence used to quote the rule as `if (next.noPlay && core.length) delete next.noPlay;`, a CORE-ONLY form the code has not had since the same day, so a maintainer who stopped at this bullet read the fun-only case as covered while the KNOWN LIMITS section below flatly contradicted it): TWO WRITE PATHS STATE THE RULE AND THE THIRD DELIBERATELY NO LONGER DOES — the CITATIONS AND THE COUNT CORRECTED here 2026-09-06 by the audit round, because the sentence that stood in this place said "ALL THREE WRITE PATHS STATE THE RULE, each quoted off its own file this turn" and then quoted TWO LINES THAT ARE IN NO FILE IN THIS TREE. It gave `applyCfbTopUp` as `if (next.noPlay && (core.length || funT.length)) delete next.noPlay;` — that function's OLD row-count form, which survives now only inside comments and test docblocks, the whole grep run this turn being `src/lib/cfb/store.ts` L492, `src/lib/ledger-merge.ts` L1400, `tests/cfb-lock-route.test.ts` L3542, `tests/ledger-merge.test.ts` L979 and L1036, and `tests/cfb-store-convergence.test.ts` L746. And it gave `upsertCfbEntries` as `if (kept.noPlay && (kept.core.length || kept.funT.length)) delete kept.noPlay;` — A LINE THE SAME ROUND THAT WROTE THE CITATION HAD DELETED, so the warrant "quoted off its own file this turn" was false at the moment it was written: `grep -rn "if (kept.noPlay && (kept.core.length" src app tests` this turn returns exactly two hits, `src/lib/cfb/store.ts` L768 — the comment reading "The line that used to sit here — … — was a third copy of a rule that already existed twice, and it was the copy that counted ROWS" — and one test docblock at `tests/cfb-store-convergence.test.ts` L1145. THE TWO LIVE STATEMENTS, each read off its own file this turn: `applyCfbTopUp` (`src/lib/cfb/lock-server.ts` L997) `if (next.noPlay && (staked > MONEY_EPS || funStaked > MONEY_EPS)) delete next.noPlay;`, over `const staked = cfbStakeOf(core);` (L938) and `const funStaked = cfbStakeOf(funT);` (L947) with `const MONEY_EPS = 1e-9;` (L131); and `mergeDay` (`src/lib/ledger-merge.ts` L1455) `if (out.noPlay && (stakeSum(out.core) > 1e-9 || stakeSum(out.funT ?? []) > 1e-9)) delete out.noPlay;` — both sited AFTER BOTH UNIONS so the buckets they read are the merged ones. `upsertCfbEntries` REACHES THE SAME ANSWER WITHOUT A LINE OF ITS OWN, which is why deleting its copy closed a drift rather than opening a hole: it seats the day as `const syncDay = (mergeLedgers([cur], [entry])[0] as CfbLedgerEntry | undefined) ?? cur;` (`src/lib/cfb/store.ts` L728) and spreads that day into `const kept: CfbLedgerEntry = { ...syncDay, grading };` (L748), and `noPlay` is deliberately absent from `LOCK_INSTANT` (`["sport", "date", "locked", "daily", "fun", "lockedAt", "source", "trigger"]`, L691), so the kernel's verdict rides through instead of being restated a third time in a form free to drift — which is precisely the drift that produced the false citation above. THE SYNC RAIL NOW TURNS THE FLAG ON STAKED MONEY RATHER THAN TICKET COUNT (defect A2, 2026-09-06), over the SAME projection `todayExposure` (`src/lib/bankroll.ts`) reduces over: `noPlay` claims "nothing was staked", and a row that stakes $0 stakes nothing, so a day carrying only a $0 ticket was ending its own no-play claim on the strength of an array length. THAT DISAGREEMENT IS NOW CLOSED, AND THE SENTENCE THAT STATED IT IS WITHDRAWN RATHER THAN QUIETLY DELETED (2026-09-06, the audit round): this bullet used to read "THAT LEAVES THE THREE RAILS DISAGREEING ON EXACTLY ONE SHAPE … a stored no-play day merged with a copy whose only row is a $0 fun ticket keeps `noPlay` on `mergeLedgers` and LOSES it on `upsertCfbEntries` — same result for a $0 core row — so on a zero-stake day the phone and the cloud still describe the day differently", and that stopped being true the moment `upsertCfbEntries` gave up its own copy of the rule. RE-MEASURED THIS TURN by driving both exported functions directly on that exact pair: a stored no-play day against a copy whose only row is a $0 FUN ticket comes out `"noPlay" in day` TRUE with `noPlay: true`, core 0 / funT 1, on `mergeLedgers` AND on `upsertCfbEntries`; the $0 CORE row gives the same answer on both rails, core 1 / funT 0. THE TWO RAILS NOW AGREE ON THE ZERO-STAKE SHAPE, and they agree because there is only one rule left for them to agree with. Nothing writes such a row today in any case (`planCfbTopUp` refuses a zero-stake parlay at its fun seat on `const noStake = !t || !(t.stake > 0);` (`src/lib/cfb/lock-server.ts` L965, grepped this turn) — CITATION CORRECTED 2026-09-06 in the final round: this sentence gave the seat as a two-break loop, `if (!(t.stake > 0) || funStake + t.stake > CFB_PAPER.fun + MONEY_EPS) break;`, at "L868", and that text DOES NOT GREP anywhere under `src/` today; it survives only inside that function's own docblock recording its removal, and the allotment half of it was dead when it was quoted, and `assertCfbCardMoney` holds every core stake at or above `CFB_RULES.minStake` $5), which is why no fixture covers it. The headline is an ABSOLUTE and it now holds: MEASURED THIS TURN by driving `mergeLedgers` and `upsertCfbEntries` directly, a stored NO-PLAY day (core `[]`, funT `[]`, `noPlay: true`, graded) merged with a copy whose ONLY money is `cfb-2026-09-05-topup1-fun-1` at $25 comes out core 0 / funT 1 / $25 with `"noPlay" in entry === false` on BOTH rails in BOTH argument orders — which is the fun-only case the old quote hid. MEASURED on the merge rail: the server records 2026-09-05 as a NO-PLAY (core `[]`, `noPlay: true`), the phone pulls it and grades the empty day (`gradeCfbEntry`'s `every` over an empty ticket list is vacuously true) so its copy wins `pickBase` on grading richness, the server then tops the day up, and the union appends the tickets INTO the no-play copy — `noPlay: true` standing over 2 core tickets and $50 staked, which the Builder renders as a NO-PLAY day. The flag is a claim about the card, so it cannot outlive the card being empty. WHAT THE UNION DOES NOT CARRY, said plainly: the entry's `topUps` log does not survive a merge — it rides the `pickBase` loser. That was measured harmless rather than assumed so: once the union runs the merged day carries the top-up's money, so `decideCfbTopUp` computes room = 0 and refuses "fully deployed"; the only residual exposure is a day still SHORT after two attempts whose log is lost, which resets the ATTEMPT count and costs at most one more game-lines pull while seating only money that still fits under $150. AND THE DEVICE'S OWN CONVERGENCE PATH IS NOW FIXED TOO (2026-09-06 — the statement this bullet carried, that "the upsert path is open" and that the device twin "still carries both of the overlay defects corrected above", is SUPERSEDED and was true only for one day): `upsertCfbEntries` in `src/lib/cfb/store.ts` used to keep `cur.core` wholesale, dropping the top-up's tickets on a phone that already held a stale copy of the date; it now unions core through its own `coreAppends` under the same two gates — AGREEMENT over the same seven-field leg identity (`lkey` / `gkey` / `label` / `prop` / `market` / `side` / `line`, never a price, never a stake), ALLOTMENT against the day's own `daily` — appends deep-copied and id-sorted, `done` recomputed over the MERGED core, plus a reopen step for the one case the overlay cannot see (an incoming copy with NO grading at all, where a stale `done: true` would otherwise stand over never-graded appended tickets). An append is still not a re-lock: no recorded stake is touched, `funT` / `lockedAt` / `source` / `trigger` / `noPlay` are untouched, and the upsert still returns `refused: true`, because the incoming card was refused AS A CARD. The device's `overlayGrading` twin now mirrors `overlayCfbGrading` — AND SINCE DEFECT B3 (2026-09-06) THAT INCLUDES THE CORROBORATION GATE the server has carried since L2: a stored `ungradable` is replaced only when EVERY leg of that ticket is settled in the INCOMING leg map, `ls.every((l) => settled(inc.legs?.[l.lkey]?.result))`, duplicated line-for-line in `src/lib/cfb/store.ts` rather than shared because that file is `"use client"` and `lock-server.ts` pulls server-only modules. MEASURED THIS TURN through `upsertCfbEntries`: a stored `ungradable` against an incoming `lost` carrying an EMPTY leg map stays `ungradable`; the same `lost` carrying its leg's real final lands. Without it one partial finals payload could turn a genuine 48-hour void into a settled loss that nothing afterwards could correct — the void being provisional is what made that door reachable. The rest of the mirror, unchanged: no `if (!cur) return inc` bypass, the same SETTLED / RESOLVED split, the same never-overwrite guard on BOTH the ticket and the leg map, the same `done` over the entry's own tickets. BOTH RAILS NOW RUN ONE UNION (2026-09-06), and the "three differences" this bullet listed a day ago are down to two: `coreAppends` and its private `agreeOnShared` / `betOf` / `LEG_IDENTITY` / cap are DELETED, `unionCore` was EXPORTED from `src/lib/ledger-merge.ts` and imported by `src/lib/cfb/store.ts`, so the device got the raise pass, the receipt gate and the desk-allotment fallback for free rather than in a copy that had already drifted inside one round — AND THAT IMPORT IS NOW GONE AGAIN (defect B1, 2026-09-06): `grep -rn unionCore src app` this turn returns `src/lib/ledger-merge.ts` and nothing else, because `unionCore(cur, entry)` FORCES the stored copy to be the base while the sync rail's base is `pickBase`'s winner, and THE BASE CHOICE IS PART OF THE ANSWER; the phone's core bucket now comes from the same merge the cloud runs (see the V1 paragraph below), so the two rails cannot report different staked money for one date — measured drift, recorded in the store's own docblock: a shared id at $15 on the device against $25 with a `topUp: 10` receipt on the server (the sync rail raised it, this rail had no raise pass at all), and a `daily`-less legacy day seating seven tickets ($175) here against six ($150) there. The import is safe from a `"use client"` file, checked rather than assumed: `ledger-merge.ts` carries no `"use client"` of its own and its only imports are `@/lib/paper-mode` and `@/lib/cfb/rules`, both constant-only modules with no imports at all; `unionCore` is generic in the entry, so a `CfbLedgerEntry` gets `CfbTicket[]` back with no adapter and no cast. The device rail also carries the N1 invalidation (`withdrawRaisedVerdicts`, run AFTER `overlayGrading` for the same reason `mergeDay` runs its copy after the grading-map merge). THOSE TWO DIFFERENCES ARE NOW CLOSED (defects V1 and V2, 2026-09-06), and one narrower one is not. V1: `upsertCfbEntries` used to keep `cur.funT` wholesale, so a device copy with an empty bucket against a server copy carrying `cfb-2026-09-05-topup1-fun-1` at $25 kept `[]` where `mergeLedgers` produced the parlay — the phone showed $0 of fun exposure against the server's $25 for the same pair. The first fix called the SAME exported `unionFun` here, which put the CAP in one place but not the CHOICE, and that was defect B2 (2026-09-06): `unionFun` seats the BASE's bucket first, the sync rail's base is `pickBase`'s winner and this rail's base is whatever copy the phone happens to hold, so two DIFFERENT $25 parlays on one date left the two rails seating OPPOSITE bets. `upsertCfbEntries` now DELEGATES rather than imitates — `const syncDay = (mergeLedgers([cur], [entry])[0] as CfbLedgerEntry | undefined) ?? cur;` (`src/lib/cfb/store.ts` L728, read this turn; the CITATION CORRECTED 2026-09-06 by the audit round, which found the fragment quoted here without its cast and without the `?? cur` fallback that stops a merge returning nothing from wiping the stored day) — and takes `funT`, `funDropped` and — since defects B1 and B5 in the closing round — `core` AND `capBreach` off the day the sync rail returns, so THE TWO RAILS SEAT THE SAME FUN WAGER AND THE SAME CORE BY CONSTRUCTION rather than by a copied tiebreak (`pickBase` is module-private to `src/lib/ledger-merge.ts`, and copying it here is exactly the drift D1 already cost); the second `[cfb-store]` fun-drop warning went with it, because it is now the same drop `[ledger-merge]` already names by id, not another one. MEASURED THIS TURN: a device copy holding `cfb-2026-09-05-fun-1` against a server copy holding `cfb-2026-09-05-topup1-fun-1`, both $25, gives `funT` `[topup1-fun-1]` and `funDropped` `[fun-1]` on `mergeLedgers` AND on `upsertCfbEntries`, in BOTH argument orders — one seated wager and one named loss, whichever copy is the stored one. The united `funT` also goes INTO the `merged` entry that `overlayGrading` measures `done` over, so a parlay appended by a top-up reopens a day the device had already marked graded — and a REOPEN pass covers the one case the overlay cannot, an incoming copy carrying no grading at all, by clearing `done` when any core or fun ticket has no verdict. V2: the rail applies the same one-line rule as `mergeDay` and `applyCfbTopUp`, and since the closing round it reads `if (kept.noPlay && (kept.core.length || kept.funT.length)) delete kept.noPlay;` (quoted off `src/lib/cfb/store.ts` this turn), so the pair that used to read `noPlay: true` over 2 core tickets and $50 staked comes out clean. AND THE FUN-ONLY HALF IS CLOSED TOO (defects A1 and B1, 2026-09-06): this bullet's own statement that "BOTH merge rails drop `noPlay` on `core.length` ALONE", and the matching KNOWN LIMIT that used to sit at (iv) below, are WITHDRAWN — both rails were widened to `(core.length || funT.length)`, and on the sync rail the clear was also MOVED, out from between the `blocks` union and the fun union to a point after the `funDropped` bookkeeping, because in its old position the fun bucket was not final when it ran (A1 was a regression of N4 inside the same day, which is why the position and not just the condition is stated here). MEASURED THIS TURN on both rails: a stored no-play day merged with a copy carrying the 3 core tickets gives core 3 and `noPlay` undefined on `mergeLedgers` AND on `upsertCfbEntries`, and the fun-only pair — the day whose ONLY money is the $25 parlay, precisely the board the card change now seats — gives core 0 / funT 1 / $25 with the flag ABSENT (`"noPlay" in entry === false`) on both rails and in both argument orders. Worth naming so nobody over-reads the rest: `lockCfb` short-circuits on an existing date before the upsert, so the Builder's LOCK button never exercises the union; the live device pull runs through `src/lib/cfb/sync.ts`, which calls `mergeLedgers`; and no production path routes a server pull through `upsertCfbEntries` today — it is the store's tested convergence primitive, kept in step with the sync rail so the two cannot drift, not a rail anything currently depends on. TESTS — EVERY COUNT BELOW WAS MEASURED IN THE CLOSING DOC PASS ITSELF (2026-09-06) AND NOTHING IS CARRIED FORWARD, because the counts this bullet used to state were stale in five places and one of them claimed ZERO FAILURES over a set that contained a RED file: the retired sentence read “over the nine gave 321 passing, 9 files, zero failures” and the per-file lines under it read 135 / 46 / 34 / 14 / 15 for the lock route, the kernel, the convergence suite, the grader and the card UI. What was actually observed, RE-MEASURED 2026-09-06 by the audit round and stated only from THIS turn's own runs — the `Tests 413 passed (413)` set with lock route 150, kernel 78, convergence 54, grader 29 and card UI 25 is WITHDRAWN, having been measured before four sibling rounds landed on those files: `npx vitest run --no-file-parallelism` over the nine files named below, RUN TWICE, gave `Test Files 9 passed (9)` / `Tests 488 passed (488)` both times with byte-identical per-file counts (a single green run is not proof when another writer may be editing the tree, and neither is a single red one) — lock route 157, kernel 112, convergence 55, grader 36, card UI 51, card 17, scheduler 22, separation 22, store 16. AND THE RED THIS BULLET USED TO CARRY IS GONE: `tests/cfb-store.test.ts` reported `Tests 2 failed | 14 passed (16)`, both `AssertionError: expected 25 to be 10`, when the closing round opened; it now reports 16 passing in both runs above, greened by the reconciliation described earlier in this bullet rather than by editing the file — the two assertions were never touched, and the receiptless raise they refused is now refused by the kernel itself. The describes cited next were re-grepped this turn: `tests/ledger-merge.test.ts` "the blocks union deep-copies the loser's records (INSTRUCTION 45, A2)" and "capBreach and funDropped are pinned on the MLB desk too (INSTRUCTION 45, A4)"; `tests/cfb-store-convergence.test.ts` "device rail vs sync rail — the closing three (B1, B2, B3)"; `tests/cfb-grade.test.ts` "C1 (2026-09-06) — a verdict booked from an unreadable score is correctable; an ordinary push is not" (the name this bullet previously gave that describe — “a game that IS final but whose score is unreadable waits the full void-recheck window, not 48 h” — does not exist in the file and is withdrawn), beside "C3 (2026-09-06) — the server rail's no-play rule is STAKED MONEY, not a row count", "C4 (2026-09-06) — the server passes over a day the KERNEL merged" and "C2 (2026-09-06) — the lock path refuses on the CAUSE as well as on the count"; and `tests/cfb-lock-route.test.ts` "D1 (2026-09-06) — an empty fun-only attempt holds the next one off; the window is a cooldown, not a kill" and "D3 (2026-09-06) — a fun-only seat is never re-flagged NO-PLAY by the route's own write path". `tests/cfb-lock-route.test.ts` 150 passing this turn (in-memory fake Redis, slate from the 2026-09-05 fixture, clock pinned): the window constants, the gate 503/401/503, the poke branch by branch (already-locked by device and by the server's own lock, waiting, locked with core in the $5–$25 band summing ≤ 150 and fun ≤ 25 and `source` "server-lock", no-slate, the late poke's "N of M games still ahead", the missed-window NO-PLAY note, merge keeping a device entry byte-for-byte, `dry=1` writing nothing, the store outage 502, the raced re-read), the comment-stripped source pins on keys and gate, DEFECT 1 both ways (the odds-missing refusal writes nothing and the next poke locks normally; and decide-before-you-spend — eight pre-window pokes make ESPN reads and zero priced boards), DEFECTS 2+3 (the `before-desk` floor with no fetch, a two-day gap getting BOTH records, the read bound, `CFB_SWEEP_DAYS`), DEFECT 4 (the marker key and its `EX`, and both trigger pairs), DEFECT 5 (`cfbPricedAhead` zero on a future-start game ESPN flags live, and the route refusing instead of locking a NO-PLAY it would call genuine), DEFECT 6 (`maxDuration`), the money guard on both the lock and the topped-up entry, the whole top-up describe (cap, Builder lock untouched, a day already at $150, after every kickoff, the overlapping-poke back-off, `dry`), and the whole settle describe (the key match against `finalsFromEspn`, a finaled day settling, a graded day costing no read, an ungradable game left PENDING, a device verdict not clobbered, a grading failure leaving the answer untouched, the read bound, an unfinished day not read at all), and the critic's five describes (the top-up cap bounding ATTEMPTS — a short day that plans nothing still burning one, the free retry-window pre-check, and an upstream failure mid-spend still costing the attempt, which is the only pin that binds the ORDER of claim-then-spend; a void date being terminal and two stuck dates not starving a fresh one; overlaying twice equalling overlaying once; the SETTLED-LEG half of the never-overwrite rule; and a corrupt ledger blob, a non-array ledger and a corrupt bank blob each answering an error status with ZERO writes), and the second and third passes' describes on top of them (a two-poke race driven end to end, where poke A is completed inside poke B's request window and the two must not share a ticket id; a pure duplicate-id pin over a core/core and a core/fun collision; a top-up onto a `done` day reopening it while the input object is verified unmutated, with a not-done day, an empty plan and a `grading: null` day all left alone; two consecutive outage pokes spending ZERO attempts, both naming `oddsMissing`, both stamping the dated marker, and the two surviving attempts then topping up as n=1 and n=2 when prices return, with `dry=1` on an outage writing nothing; a game left LIVE 48 h past kickoff grading `ungradable` and its date STOPPING being a settle candidate; three attempted dates, two of them stuck, the third read within three pokes; a locked day with an empty fun bucket gaining exactly one fun ticket at or under $25, a day that already carries one gaining none, and the answer and the note both saying what the fun bucket did; and the FIRST-pass `done` rule, which was unpinned and is now held by a test that goes red under the removed bypass), and the FOURTH pass's four describes on top of those (DEFECT M — a full-$150 core day with an empty bucket firing, a spent ticket cap firing, both buckets full refusing with a reason naming BOTH, a fun-only plan surviving the route's two discards, and the exact `topUps` row shape `{ core: 0, stake: 0, filled: false }`; MUTANTS — two correct lines nothing was asserting, each re-planted and killed this round; DEFECT S1 — `cfbSettleCandidate` true on a voided date at 51 h and the `CFB_VOID_RECHECK_MS` literal; DEFECT S2 — a date whose ESPN read THROWS still stamped, so the good date is reached on the next poke); `tests/ledger-merge.test.ts` 78 passing this turn, most of them the core union's (the CFB top-up surviving a stale graded push and summing to $150 exactly once, commutativity, a duplicate id never appended twice, the REFUSAL when two sides disagree about a shared id, the allotment bound refusing a seventh $25 ticket on a full day, the MLB block-fire shape keeping all four tickets, the fill-only accrual rules untouched, the funT union still working beside it, and the raises — a raised stake on a shared id surviving a stale graded push, the raise symmetric and idempotent, and — REWRITTEN by the closing round, quoted here because it is a rewrite and not a loosening — the pin formerly titled "a SMALLER stake on the other side never lowers the base — the raise is monotone", which asserted `40` and a stake sum of `75`, is now "a receiptless DISAGREEMENT lands on the smaller stake, and the day says so" (`tests/ledger-merge.test.ts`, read this turn) and asserts `12`, a stake sum of `47`, a `stakeConflict` of `{kept: 12, refused: 40}`, the stale verdict WITHDRAWN and `grading.done === false` — four claims where there were two, on the opposite number, because the pinned behaviour is deliberately different: the old justification was that no writer lowers a stake under a fixed id, and `buildModeCard` (`src/lib/server/lock-card.ts`) can re-mint a content-addressed id at a smaller pro-rata stake, so the clone was laundering a stale larger stake back in; a raise refused at the day's own allotment, and a `daily`-less raise bounded by `PAPER.daily`); the old "a legacy entry with no `daily` unions unbounded" case was REWRITTEN into two — the over-cap union now bounded at the desk allotment in both merge orders, and the original under-cap append (6 tickets, $150) re-asserted verbatim beside it; `tests/cfb-store-convergence.test.ts` 54 passing this turn (the device twins: the first pass and a later pass on the same inputs producing identical grading, an ungradable ticket letting the day reach `done`, a settled ticket AND a settled leg never overwritten, the union's six tickets at $150 once, its idempotence, its deep copies, a re-quoted leg still the same wager, the allotment bound, a rival card NOT merging, and `done` recomputed over the merged core) — now including the six-row table that asserts BOTH rails produce the SAME core array for one date, ticket for ticket and in the same order (raise-with-receipt, raise-without, a legacy day with no `daily`, a rival card, a plain append, an append past the allotment), the raise carried across to the device with its verdict withdrawn, and the first-pass `done` rule pinned by a mutant; `tests/cfb-grade.test.ts` 29 passing this turn — the 14 this bullet used to state plus the closing round's four C-describes — among them the one pinning the premise the provisional void rests on (a final that lands 50 h after kickoff still grades to its real result, because the escalation fires only on a leg still PENDING), and where the pin that encoded the OLD 48-hour rule was rewritten with the behaviour (a game ESPN still calls `live` 48 h past kickoff now asserts `ungradable` where it asserted `pending`, an `upcoming` case was added, and the in-window case — 47 h 59 m still `pending` — was restated; the postponed assertion is untouched); `tests/cfb-store.test.ts` 16 passing this turn, AND THAT IS A CORRECTION OF THIS BULLET'S OWN PREVIOUS CLAIM: it used to state, correctly at the time, that the file was NOT green and gave `Tests 2 failed | 14 passed (16)` with both failures reading `AssertionError: expected 25 to be 10`, in "pure: a second entry for a locked date keeps the original core and overlays grading + games" and "storage-backed: the refused re-lock never reaches the device record", and it called them a PIN TO REWRITE. They were not rewritten and did not need to be — the file is unedited and both assertions still read `toBe(10)`. What changed is underneath them: the device rail delegates its core to `mergeLedgers`, and `unionCore` now RECONCILES the two copies' shared id before the clone is trusted, so the receiptless $10 → $25 raise those two fixtures carry is refused in both merge orders and $10 stands. Both runs print the kernel's own warning beside the green: "[ledger-merge] 2026-09-05: 1 shared core id(s) disagreed on stake with no topUp receipt — the SMALLER stake stands: cfb-2026-09-05-core-1"; `tests/scheduler-route.test.ts` 22 passing this turn (the forward rides under `cfb` after `mlbTick`, `forwardCfbLock` never throws, `attachCfb` leaves the MLB body/status unchanged, the auth-only gate as a source pin plus a negative pin forbidding the old `res.status !== 200` shape, (b) the rejection not propagating, (d1) an unauthed poke never reaching the CFB desk, (d2) an authed poke whose MLB tick 503s STILL forwarding, (e) the CFB 502 riding through whole while the poke stays 200); `tests/cfb-separation.test.ts` 22 passing this turn, pinning the `CFB_LOCK` literal beside `CFB_PAPER` and the lock route's keys and gate; and the two card files this round's headline change lives in — `tests/cfb-card.test.ts` 17 passing this turn (the 2026-09-05 fixture card itself; "the fun allotment is gated independently of the core (INSTRUCTION 45)", which is the describe that holds the change Josh asked for; and synthetic slates through the real model, one of them a REWRITE of the pin that used to assert a core-empty board is a NO-PLAY) and `tests/cfb-card-ui.test.ts` 25 passing this turn. The kernel round added FOUR new describes to `tests/ledger-merge.test.ts` beyond the ones listed above — "a raise's receipt is the DIFFERENCE of the two copies' top-ups (INSTRUCTION 45, K1)", "noPlay never survives beside real tickets (INSTRUCTION 45, N4)", "an over-cap stored day is kept but MARKED, never laundered silently (INSTRUCTION 45, K3)" and "a fun ticket the cap drops is reported, not silently vanished (INSTRUCTION 45, K4)" — plus an EXTENDED pin for K2 inside the existing MLB block-fire describe, which is where the money metadata and the `blocks` union are held — and one to `tests/cfb-store-convergence.test.ts`, "device rail vs sync rail — the fun bucket and the no-play flag (defects V1, V2)", which asserts the two rails against each other rather than against a hand-written expectation. THE CLOSING ROUND — THE CLONE IS RECONCILED, NOT TRUSTED (2026-09-06, `src/lib/ledger-merge.ts`, read this turn). Six consecutive rounds each fixed a real money defect and each shipped a new one INSIDE the fix, and the common root is one line of doctrine: `mergeDay` deep-clones `pickBase`'s winner WHOLESALE and believes it, while the unions only ever bounded what was APPENDED. This round fixes the clone. `unionCore` now runs a RECONCILIATION pass over every id BOTH copies hold BEFORE anything is measured against the allotment: `hi` is the larger stake, `lo` the smaller, and the larger is taken only when it carries a receipt — `const receipted = Number.isFinite(tu) && Math.abs((Number(hi.stake) || 0) - (Number(lo.stake) || 0) - (tu - prev)) < 1e-6;` over `const prev = Number(lo.topUp) || 0;` and `const tu = Number(hi.topUp);` (`src/lib/ledger-merge.ts` L975–L977, read verbatim this turn; the math-bar form this sentence carried inside backticks was a paraphrase, which reads as source and is not — corrected 2026-09-06 by the audit round), the same difference-of-two-top-ups test the raise pass already used, now read off the unordered pair. With no receipt the SMALLER stake stands and the disagreement is recorded as `stakeConflict[id] = { kept, refused }` in BOTH directions, including when the base already held the smaller stake and nothing moves. Because `hi`/`lo` are defined by the stakes and not by which copy `pickBase` seated, the answer is identical in both merge orders. `topUp` travels with the stake it explains and is DELETED rather than left stale when the winner carries none, so `stake − topUp` (the allocator's own sizing) never lies. The sum the append pass then measures against `allotmentCap` is money the two copies AGREE on; what the allotment refuses is named on `dropped` with its money beside it on `droppedPL` (`receiptOf` → `{ result, payout, stake }`, plus `placed` / `actualStake` when the ticket carries them, absent answers omitted rather than written as nulls) instead of vanishing. `raised` was renamed `restaked`, because the reconciliation moves a stake DOWN as well as up. An OVER-CAP set is still kept WHOLE and MARKED (`capBreach { core?: {sum,cap}, fun?: {sum,cap} }` plus a `console.warn`), never truncated. THE FUN CAP STILL EVICTS — AND CAN NO LONGER DESTROY MONEY NO RECEIPT DESCRIBES. `unionFun` seats its pool by `funRank` — and that rank is CORRECTED here 2026-09-06: an earlier version of this sentence said "2 = LIVE MONEY (`placed === true` with no resolved verdict on either side)" and filed "unplaced ungraded sizings" at 0, and it NO LONGER HOLDS, because the rank now reads nothing off the ticket at all. Read verbatim in `src/lib/ledger-merge.ts` (L657–L660) this turn, the whole body is `if (!v) return t.placed === true ? 3 : 1;` then `return v.result === "won" && v.payout > 0 ? 2 : 0;` over the pair's own `verdictOf` (which resolves only `won` / `lost` / `push`) — FOUR BANDS, NOT THREE, and the CITATION IS CORRECTED here 2026-09-06 by the audit round, which found this sentence still quoting the pre-F1 three-band body `void t; if (!v) return 2; return v.result === "won" && v.payout > 0 ? 1 : 0;` that the file has not carried since defect F1 landed. THE FOUR BANDS: 3 = LIVE MONEY, ungraded and answered `placed: true`; 2 = a settled WIN with `payout > 0`, the one number on the day no re-grade can rebuild once the ticket it was priced on is gone; 1 = AN UNPLACED UNGRADED SIZING, the shape BOTH desks actually mint (CFB stamps no `placed` key at all, MLB's `toTicket` stamps `placed: false, actualStake: 0` at `src/lib/server/lock-card.ts` L412 and L522, and `grep -rn "placed: true" src app` this turn returns docblock prose in `src/lib/ledger-merge.ts` and no writer anywhere); 0 = a settled `lost` / `push`, fully reconstructible by re-grading the survivor. THE BAND IS MONOTONE FOR EVERY TICKET THIS REPO CAN MINT, and that is the whole point of the fourth band: an ungraded ticket enters at 1 and its own verdict moves it to 2 (won) or 0 (lost/push), so GRADING A TICKET CAN PROMOTE IT AND CAN NEVER DEMOTE IT — which is exactly the F1 regression the band closed, where a ticket graded WON fell BELOW its still-ungraded rival and was evicted with its payout, `funDropped` naming it and `ledgerStats` losing the win from Net P/L, ROI and the win/loss record. The single demotion the ordering admits is 3 → 2, a `placed: true` paper ticket that then wins, and no writer in this repo produces one: `buildReading` (`src/lib/server/self-reading.ts` L124) records a VIOLATION for any paper ticket whose `placed !== false`. What the band deliberately does NOT do is stop a settled winner being evicted by money that is still LIVE — rank 2 sits below rank 3 on purpose, because the winner's money is fully described by the receipt it leaves on `funDroppedPL` and the live wager's is not describable at all — then by payout, then by the pool's own `seq` (the base's bucket in the base's order, then the ids only the other copy holds in ticket-id order), so seating is a fact about the ticket and the pair's grading and never about which copy `pickBase` seated. A ticket the base ALREADY HELD can now be displaced, which is why `mergeDay` compares the seated ID LIST rather than its length. And EVERY evicted ticket leaves a receipt through the same `receiptOf`, not only settled ones, so a wager is never deleted in silence. `funCap` is now a CEILING as well as a floor — `recorded.length ? Math.min(desk, Math.max(...recorded)) : desk` — so a stored blob's own `fun` can LOWER the merged day's fun ceiling but can never raise it above `CFB_PAPER.fun` / `PAPER.fun`; an inflated `fun: 100` buys no second $25 ticket on either desk. AND SO IS `allotmentCap` — the gap this bullet's KNOWN LIMITS list did not name last round, that a stored copy could RAISE its own CORE ceiling where the fun rail already refused exactly that shape, is CLOSED: read this turn, `allotmentCap` ends on the identical line, `recorded.length ? Math.min(desk, Math.max(...recorded)) : desk`, with `desk` = `CFB_PAPER.daily` when either side carries `sport: "cfb"` and `PAPER.daily` otherwise (both printed this turn as 150, both `.fun` as 25), so a recorded `daily` can only LOWER the merged day's core ceiling and never lift it above the desk's own $150. THE MARKERS NOW REACH BOTH RAILS AND THE SCREEN. `mergeDay` surfaces `coreDropped`, `coreDroppedPL` and `stakeConflict`, accrued from both sides and each filtered to ids that are NOT seated at the stake the marker names — so a later merge arriving with a receipt CLEARS the marker instead of leaving it contradicting the ticket beside it — beside the existing `funDropped` / `funDroppedPL` / `capBreach`, every one of them warned about by name. The DEVICE rail no longer copies markers one at a time: `upsertCfbEntries` (`src/lib/cfb/store.ts`) builds its record as `const kept: CfbLedgerEntry = { ...syncDay, grading };` and takes back only `LOCK_INSTANT = ["sport", "date", "locked", "daily", "fun", "lockedAt", "source", "trigger"]`, so every marker the kernel adds travels to the phone with no per-field edit and cannot be forgotten by the next one. ON THE CARD, `cfbDayMarks(e)` and `CfbDayMarksNote` (`src/components/cfb/CfbLedger.tsx`, imported by `src/components/cfb/CfbBuilder.tsx`) render the breach as a line inside the day's own money span and the refused wagers as a `<details>` naming each id with its stake, its verdict and what it paid; a day carrying no markers renders no extra node at all. `cfbDayMarks` NOW READS ALL SEVEN, and the sentence that stood here — that it read "`capBreach`, `funDropped` and `funDroppedPL` ONLY — the three core-side markers are stored, merged and synced but not yet drawn" — is WITHDRAWN 2026-09-06. Read this turn in `src/components/cfb/CfbLedger.tsx`, `cfbDayMarks` builds `drops` from `dropsIn(e, "core")` (the `coreDropped` / `coreDroppedPL` channels) BESIDE the fun list, `cuts` from `stakeConflict` gated on the seated ticket, and `rivals` from `betConflict` gated the same way, on top of `capBreach` — so limit (v) below is CLOSED rather than narrowed. A VERDICT BOOKED FROM AN UNREADABLE SCORE IS NOW CORRECTABLE (`src/lib/cfb/lock-server.ts`). A game whose score the grader could not read was booked as a 0-0 PUSH, and the never-overwrite-a-settled-verdict rule then made that phantom permanent. The evidence is the LEG line rather than the ticket's, because `settle()`'s ticket detail carries no score while `gradeCfbLeg` writes `${score} · …`: `ZERO_READ = /^0-0 · /` over `grading.legs[lkey].detail`. So `overlayCfbGrading`'s ticket loop reads `if (isSettled(stored) && !(zeroRead(id) && corroborated(id))) continue;`, where `corroborated` is the EXISTING void-exception bar (every leg of the ticket settled in the INCOMING leg map) rather than a second rule, and `cfbSettleCandidate` counts such a ticket as `reopenable` inside `CFB_VOID_RECHECK_MS` (7 days, `src/lib/cfb/rules.ts` L308, re-read this turn) so the final is actually fetched. The exception can CORRECT but cannot FABRICATE: a game that really was 0-0 re-grades to the byte-identical push, so the overwrite is a no-op, and every other settled shape — an ordinary spread push included — is still refused. It is self-terminating (once the corrected verdict lands nothing is reopenable) and terminates at 7 days in the bad case; the cost is at most one KEYLESS ESPN read per poke on a date carrying a 0-0 push, and zero Odds credits. The DEVICE half is not fixed and is stated as limit (vi): `src/lib/cfb/store.ts`'s `overlayGrading` has no zero-read exception (grepped this turn: neither `zeroRead` nor `0-0` appears in that file or in `src/components/cfb/CfbLedger.tsx`), so the phone is corrected on the next sync rather than by its own pass. KNOWN LIMITS, stated rather than assumed away: (i) `/api/cfb/lock` still has NO cron of its own in `vercel.json` — it rides the scheduler pulse entirely, so if the ticker and both Vercel entries all miss a PT day, nothing locks that day and the sweep is what records it, up to `CFB_SWEEP_DAYS` back; AND THE ~15-MIN TICKER IS NOT IN THIS REPO — no `.github/workflows/` file pokes `/api/scheduler` (checked 2026-09-06), `docs/cron-jobs.md` records it as external infrastructure, and the only schedule this repo actually declares is `vercel.json`'s two `/api/scheduler?poke=` entries (`45 21 * * *` and `0 0 * * *`, both re-read 2026-09-06), so nothing here can prove the pulse is running. Re-read this turn, `vercel.json` declares exactly TWO crons, BOTH of them `/api/scheduler`, and the string "cfb" appears NOWHERE in the file (`grep -ic cfb vercel.json` = 0) — so "per day", as far as anything this repo can guarantee, is TWO POKES A DAY, not the ~15-minute ticker the MLB desk effectively gets, and a $150 + $25 day that needs a lock, a top-up and a settle is leaning on infrastructure that is not in the tree. The fun arm does not change this — it rides the same pulse and buys no board of its own; and the STEADY-STATE Odds cost per CFB date is unchanged by it: 6 credits for the lock's own game-lines pull plus at most `CFB_TOPUP_MAX` (2) top-up boards at 6 each = 18 credits per date at the ceiling and 6 on a date that never tops up, none of it counted by `CFB_PROPS.dailyBudget`, which counts props pulls only (the 6-credit figure is the prod measurement of 2026-09-05, quota 17578 -> 17572; `CFB_TOPUP_MAX` re-read in `src/lib/cfb/rules.ts` this turn); (i-b) AN OUTAGE LONGER THAN THE SWEEP DEPTH LOSES THOSE DAYS PERMANENTLY: the walk covers at most `CFB_SWEEP_DAYS` (3) previous PT dates and STOPS at the first that is `before-desk` or already recorded, `sweepPrevDates` is the only thing on this route that ever writes a date other than the poke's own, and nothing re-opens an older one — so a four-day gap loses its oldest day for good, and a hole BEHIND an already-recorded day is never reached at all, because the walk stops on that day. Depth 3 is chosen so that any poke from Saturday through Tuesday still reaches Saturday, which is where the CFB money is; it is a bound, not a guarantee; (ii) every race guard is read-then-check-then-write, not atomic, so two pokes whose reads interleave before either write CAN both write — harmless rather than merely bounded, because `mergeLedgers` unions by date and `mergeDay`/`pickBase` (`src/lib/ledger-merge.ts`) keeps ONE of the two equally valid server locks (grading richness, then CLV count, then confirmed count, then JSON length), so a day never carries both cards' stakes; the guarded paths themselves are pinned by the three `raced` cases (the lock, the sweep, the top-up), and the core union added 2026-09-06 does not change that, though the gate itself is CORRECTED here TWICE, and the second correction RETIRES the first. A version of this sentence said "ON A RIVAL DATE NO STAKE IS RECONCILED AT ALL", quoting a whole-date branch `if (rivals) { conflict[String(t.id)] = { kept: mine, refused: yours }; out.push(t); continue; }` at "L961–L965" and a one-expression `const appendable = betConflict.length ? [] : other.core.filter(…)` at "L1008". NEITHER GREPS TODAY: the whole-date branch was DELETED by the closing round (CLOSING K2, 2026-09-06) on the finding that a whole-DATE refusal is the same shape of error as the whole-ENTRY agreement bail defect A2 had just removed one level down, reintroduced one level up — the lesson being that a disagreement about one ticket is a fact about THAT TICKET and never about the date, the entry or any other id. WHAT THE CODE READS NOW, each fragment grepped one at a time this turn: the pre-pass is `if (m && !sameBet(t, m)) betConflict.push(String(t.id));` (`src/lib/ledger-merge.ts` L964) feeding `const rivals = betConflict.length > 0;` (L966 — not the L935 the retired sentence gave), and the reconciliation loop is bounded by MONEY rather than by the date — CITATIONS CORRECTED 2026-09-06 in the final round: this sentence gave the first arm as the one-liner `if (receipted && lift <= 0) win = t;` at "L1045", which DOES NOT GREP today (that arm is a block, and it now also writes the kept-mine receipt on a rival pair), and its "L1046" / "L1051" / "L970" numbers have all moved. Named as symbols instead, and each re-grepped this turn: the four-arm chain in `unionCore` is measured against `let projected = stakeSum(base.core);` and the raise arm is `} else if (receipted && projected + lift <= cap + 1e-6) {`, with `const cap = allotmentCap(base, other);` taken once at the head of the function. So a shared id the two copies AGREE the bet of has its top-up receipt read normally even on a rival date, and is refused only when seating it would carry the merged core past the day’s own allotment — which is the only harm the rival’s stamp could actually do, and the harm the retired branch existed to prevent. The APPEND pass is still refused wholesale, because appending a rival lock’s surplus would stake the Saturday twice: `const unseen = other.core.filter((t) => t.id && !ids.has(t.id)).sort(byId);` (L1105) feeds `const appendable = betConflict.length ? [] : unseen;` (L1112). MEASURED THIS TURN through `mergeLedgers`, both argument orders, on a matched pair ONE FIELD APART — four $25 tickets on the device copy against a server copy whose `cfb-2026-09-05-core-2` is topped up to $50 carrying `topUp: 25`, a receipt that passes the difference test exactly: CONTROL (every id naming the same game) merges to `{"stakes":[25,50,25,25],"sum":125,"allocSum":125}` with no markers at all; the DEFECT SHAPE (the ONLY change — `cfb-2026-09-05-topup1-core-4` names a different game on the two copies) merges to `{"stakes":[25,50,25,25],"sum":125,"allocSum":125,"betConflict":["cfb-2026-09-05-topup1-core-4"]}` with `stakeConflict` ABSENT. The unrelated ticket’s drift no longer strands the receipted raise at $100, no longer deletes $25 of deployed money from `allocSum`, and no longer hands `decideCfbTopUp` $25 of fresh room to stake again on a $150 day; the disputed id alone is named, its own ticket standing at the base’s stake on the base’s game. The refusals that DO fire are still disclosed and still warned about by name, on branched sentences that match which gate refused — "shared core id(s) disagreed on stake across RIVAL CARDS — nothing was reconciled between them and this card's own stake stands" against the byte-unchanged receiptless "the SMALLER stake stands", both grepped in `src/lib/ledger-merge.ts` this turn — and two copies that agree are still bounded by ALLOTMENT at the DESK’s own $150, which a stored `daily` may lower and can no longer raise. The claim write is read-then-write like every other path here, so two pokes racing between a claim and its write can still lose one claim; after the 2026-09-06 claim fix what that costs is bounded and named — an attempt spent without seating money, or one poke's appended tickets lost to the other's SET, after which the day is short again and a later poke may re-seat under the same $150 — and the fun bucket can no longer compound it either, because `funCap` bounds a merged `funT` at the day's own $25 so two pokes' parlays can never sum to $50 on the ledger; while the two expensive outcomes are CLOSED: a COMPLETED attempt can no longer be erased from the count (a claim replaces only an unfilled row, `filled`-tested rather than `core === 0`-tested, so a completed fun-only attempt is never mistaken for one), and two tickets can no longer share an id (ids count on from the entry's own core length, and `assertCfbCardMoney` throws on a duplicate id before anything is written, so even a path that produced one would cost a loud 502 rather than a quiet double verdict). Bounded and pinned, not atomic; (iii) capabilities the MLB desk has that CFB still lacks, each re-checked against the code 2026-09-06 — per-BLOCK locking and the block ladder (`/api/generate?block=`, one fire per poke), the self-check's reading artifact and its repair path (`writeReading` / `buildReadingSafe`) and the reason-record branch — and with that artifact the UNGATED READ URL, because `/api/board` serves the MLB day's card AND its reading to anyone (deliberately not sync-phrase gated, per that route's own header), whereas the only ways to read a locked CFB card are the phone and `/api/cfb/ledger`, which `syncAuthed` gates, since `/api/cfb` serves the slate and carries no card and no reading — and CALIBRATION: the scheduler's grading tick forwards to `/api/calibrate?grade=only`, which has no CFB code, so the settle pass scores CFB days and moves the CFB bankroll but nothing calibrates the CFB model off them yet (`grep -i cfb app/api/calibrate/route.ts` returns nothing, read 2026-09-06); (iv) the CONVERGENCE of the two rails is no longer core-only AND ITS NO-PLAY HALF IS NOW CLOSED — the limit this entry used to state, that "BOTH rails drop `noPlay` on `core.length` alone", is WITHDRAWN 2026-09-06: all three write paths test both buckets, the sync rail's clear was moved to where both buckets are final, the device rail takes its fun bucket and its `funDropped` from `mergeLedgers` instead of from a copied tiebreak, and its overlay carries the server's corroboration gate — all re-measured on both rails in both argument orders this turn. AND THE ONE PATH THAT WAS STILL OPEN HERE IS CLOSED (defect B3, 2026-09-06): the limit this entry used to state — that “`gradeCfb` → `applyCfbGrading` still writes the DEVICE grader's verdict WHOLESALE, with no overlay and no corroboration” — is WITHDRAWN. Read off `src/lib/cfb/store.ts` this turn, `applyCfbGrading` writes `overlayGrading(entries[i].grading, grading, entries[i])`, the same module-private rule the upsert rail calls with the same three arguments, and `gradeCfb` returns what was STORED (`applyCfbGrading(...)?.grading ?? null`) rather than what the grader proposed, because after the overlay those can differ. ONE rule now governs every path that can overwrite a verdict on the device, so a partial finals payload can no longer turn a genuine 48-hour void into a settled loss on the phone either; (v) THE MARKERS ARE DECLARED, AND ALL SEVEN ARE DRAWN — THIS LIMIT IS CLOSED, and it is REWRITTEN rather than deleted so the record shows what changed. IT USED TO READ "THE MARKERS ARE STILL UNDECLARED, AND HALF OF THEM ARE STILL UNDRAWN … NONE of them is declared on `CfbLedgerEntry` (`src/lib/cfb/types.ts`, re-read this turn — `noPlay?: boolean` at L215 is still the only optional of this kind there)", and BOTH HALVES ARE FALSE AS OF THIS TURN. DECLARED: `coreDropped?: string[]` (L283), `coreDroppedPL?: Record<string, CfbMergeDropPL>` (L285), `funDropped?: string[]` (L287), `funDroppedPL?: Record<string, CfbMergeDropPL>` (L289), `stakeConflict?: Record<string, CfbMergeStakeConflict>` (L292), `betConflict?: string[]` (L294) and `capBreach?: { core?: { sum: number; cap: number }; fun?: { sum: number; cap: number } }` (L297) are all named fields on `CfbLedgerEntry` in `src/lib/cfb/types.ts`, read this turn, beside the two exported record types they lean on, `export type CfbMergeDropPL = { result: string; payout: number; stake: number; placed?: boolean; actualStake?: number };` (L213) and `export type CfbMergeStakeConflict = { kept: number; refused: number };` (L218); the docblock over them states "ALL SEVEN ARE OPTIONAL AND ARE DELETED WHEN THEY NO LONGER HOLD", which is the kernel's own `else delete` discipline written into the type, so `undefined` is the normal state of a clean day. The `noPlay` citation in the retired sentence is corrected too: it sits at `src/lib/cfb/types.ts` L230, not L215, and it is no longer the only optional of its kind there. WHAT REPLACES THIS LIMIT, because a closed limit is not the same as no limit: the seven are DOCUMENTED but not ENFORCED at the writer — the kernel still assigns through a cast, `if (stillBetConflict.length) (out as Record<string, unknown>).betConflict = stillBetConflict;` (quoted in the type's own docblock at L275–L276 this turn), so `tsc` cannot spell-check the write and the field names go on being held by tests rather than by the compiler. The half that is now CLOSED: `capBreach`, `funDropped` and `funDroppedPL` are rendered on the day's card by `cfbDayMarks` / `CfbDayMarksNote` (grepped this turn in `src/components/cfb/CfbLedger.tsx` and `src/components/cfb/CfbBuilder.tsx`). The half that is now ALSO CLOSED (2026-09-06, second closing pass): the sentence this entry used to carry — that `coreDropped`, `coreDroppedPL` and `stakeConflict` were "visible today only in the stored JSON and in a `console.warn`" — is WITHDRAWN. `cfbDayMarks` reads the core side too, executed this turn: a refused core wager is a row in the same drop list tagged `core` (`$25 core · won · pays $60.00`) and a refused raise is a gold sentence measured against the SEATED ticket (`$15 stands · a $25 raise was refused · $10 not staked`). AND THE SEVENTH IS DRAWN TOO, so the last open half of this limit is closed as well: the sentence that stood here — "the SEVENTH marker the closing round added, `betConflict`, is written by the kernel and read by NOTHING — `grep -rn betConflict src app` this turn finds it in `src/lib/ledger-merge.ts` and nowhere else — so a refused rival card reaches the phone's screen as no pixel at all" — is WITHDRAWN. `grep -rn betConflict src app` THIS turn finds it in FIVE files, `src/lib/ledger-merge.ts` (23 hits), `src/components/cfb/CfbLedger.tsx` (6), `src/lib/cfb/store.ts` (3), `src/lib/cfb/types.ts` (3) and `src/components/cfb/CfbBuilder.tsx` (1); `cfbDayMarks` reads it at `src/components/cfb/CfbLedger.tsx` L237–L242, GATED ON THE SEATED TICKET exactly as the cuts are, since the sentence it draws claims "this device's ticket stands" and may only be claimed while that ticket is actually on the day; and EXECUTED this turn on a merged rival day, `cfbDayMarks` answers `rivals: ["cfb-2026-09-05-1"]` and the day's disclosure carries the line "cfb-2026-09-05-1 · another copy names a DIFFERENT bet — this device's ticket stands" under the note "Rival cards are never mixed, so nothing from that copy was added to this day.", pinned by value in `tests/cfb-card-ui.test.ts` ("cfb-card-ui — a refused rival card is disclosed (B2)", L489, grepped this turn); (vi) the DEVICE has no void re-check window: `gradeCfbPending` (`src/components/cfb/CfbLedger.tsx`) selects on `!e.grading?.done` and knows nothing of `CFB_VOID_RECHECK_MS` (7 days; CITATION CORRECTED 2026-09-06 — the constant is at `src/lib/cfb/rules.ts` L308, not the L260 an earlier version of this line gave, and the filter reads in full `readCfbLedger().filter((e) => !e.grading?.done && e.date <= today && (e.core.length > 0 || e.funT.length > 0))`), so the provisional-void re-read that DEFECT S1 bought lives on the server settle pass only — a phone that graded a day to `ungradable` will not re-read it on its own, and the correction reaches it on the next sync rather than from its own pass; (vii) the MLB allocator's DOUBLE-PICK is still unfixed and was re-read this turn: in `src/lib/server/lock-card.ts` the GATED pass is `shAllocate(p, daily, …)` over the whole priced set, and only the FORCED pass filters `rest` by the `ids` / `legs` sets built from `carriedTix`, so a carried ticket can be re-picked by the gated pass of a later fire. That is an MLB defect, not a CFB one — CFB has no block ladder and its top-up narrows the board by seated game — and it is named here only because this bullet's K2 paragraph touches the same file's money fields; (viii) `capResidue` is deliberately stale on a merged day (see K2 above), so a merged day's residue line describes the last fire that ran rather than the merged whole; and (ix) the ALLOCATION RULES themselves are unchanged and that is still an OWNER DECISION THAT HAS NOT BEEN MADE — `oneLegPerGame: true`, `forcedMinEvPct: 0`, `forcedMaxDec: 1.75`, `maxStake: 25`, `tickets: { min: 3, max: 7 }` all re-read in `src/lib/cfb/rules.ts` 2026-09-06 — so nothing in this round widens what a thin slate can deploy of the $150 core, AND THIS IS EXACTLY WHY THE FIXTURE LEAVES $75 UNDEPLOYED: its three tickets are each already at the $25 `maxStake`, `oneLegPerGame` forbids a second ticket on a seated game, and no other game on that slate offers a side at or above `forcedMinEvPct` under `forcedMaxDec` 1.75, so the $75 stays in the note rather than being forced onto a price the rules refuse. Widening any of those four is the owner's call and it has not been made; what it changes is that the $25 fun money is no longer thrown away when the core gate seats nothing. EVERY LIMIT IN THIS BULLET WAS RE-VERIFIED AGAINST THE CODE AS IT STANDS AT THE END OF THE CLOSING ROUND (2026-09-06, each figure below read in the doc pass itself) — every one of them SURVIVES except (v), which is NARROWED and not withdrawn (`capBreach` / `funDropped` / `funDroppedPL` are now drawn on the card; the three core-side markers are not), on top of the no-play half of (iv) and the `applyCfbGrading` half of (iv) already withdrawn above; the supporting reads: the four allocation constants above are still `maxStake: 25` / `tickets: { min: 3, max: 7 }` / `forcedMaxDec: 1.75` / `forcedMinEvPct: 0` / `oneLegPerGame: true` in `src/lib/cfb/rules.ts` and widening them REMAINS AN OWNER DECISION THAT HAS NOT BEEN MADE, which is precisely why the fixture leaves $75 undeployed; `vercel.json` still declares exactly TWO crons, both `/api/scheduler` (`?poke=evening` at `45 21 * * *`, `?poke=night` at `0 0 * * *`), with `grep -ic cfb vercel.json` = 0 and no `.github/workflows/` file poking `/api/scheduler` at all (`grep -rln api/scheduler .github/workflows/` returns nothing), so “per day”, as far as anything in this tree can guarantee, is still TWO POKES A DAY; the steady-state Odds cost per CFB date is unchanged at 6 credits for the lock's own game-lines pull plus at most `CFB_TOPUP_MAX` (2, re-read in `src/lib/cfb/rules.ts` this turn) top-up boards at 6 each — 18 credits a date at the ceiling, 6 on a date that never tops up, none of it counted by `CFB_PROPS.dailyBudget`; and the MLB capabilities CFB still lacks are all four still absent — per-BLOCK locking and the block ladder, the self-check's reading artifact (`writeReading` / `buildReadingSafe` appear only in `app/api/scheduler/route.ts` and `app/api/generate/route.ts`, never on a CFB path) and with it the UNGATED READ URL (`/api/board` carries no sync gate by its own header's decision, while `/api/cfb/ledger` refuses on `if (!syncAuthed(req))`), and CALIBRATION (`grep -ic cfb app/api/calibrate/route.ts` = 0, so the settle pass moves the CFB bankroll and nothing calibrates the CFB model off it). The exact reads behind that paragraph, all made this turn: `CFB_RULES` printed from `src/lib/cfb/rules.ts` as `maxStake: 25`, `minStake: 5`, `tickets: { min: 3, max: 7 }`, `forcedMaxDec: 1.75`, `forcedMinEvPct: 0`, `oneLegPerGame: true`, with `CFB_TOPUP_MAX = 2` (L187), `CFB_SWEEP_DAYS = 3` (L133) and `CFB_VOID_RECHECK_MS = 7 * 24 * 3600_000` (L308); `vercel.json` read whole — a `crons` array of exactly two entries, `/api/scheduler?poke=evening` at `45 21 * * *` and `/api/scheduler?poke=night` at `0 0 * * *`, `grep -ic cfb vercel.json` = 0; `grep -rln "api/scheduler" .github/workflows/` returns NOTHING (the directory holds board-archive, context, hr-overround, line-history, model, props-history and ufc workflows only), so the ~15-minute ticker remains infrastructure this repo cannot prove is running and TWO POKES A DAY is still all it declares; `writeReading` / `buildReadingSafe` appear only in `app/api/generate/route.ts`, `app/api/scheduler/route.ts` and `src/lib/server/self-reading.ts`; the block ladder is `app/api/scheduler/route.ts` L278 fetching `/api/generate?block=…` and `grep -ic cfb app/api/generate/route.ts` = 0; `syncAuthed` does not appear in `app/api/board/route.ts` at all while `app/api/cfb/ledger/route.ts` L36 reads `if (!syncAuthed(req)) return NextResponse.json({ error: "bad-sync-key" }, { status: 401 });`. The only figure in that paragraph NOT re-measured this turn is the 6-credits-per-game-lines-pull unit price, which is the prod quota reading of 2026-09-05 (17578 → 17572) and cannot be re-taken from the tree; the 18-credit ceiling is that unit price times the 1 + `CFB_TOPUP_MAX` boards a date can buy. A SECOND CLOSING PASS THEN RE-MEASURED THE MONEY PATH AND THE FIXTURE (2026-09-06; every figure in this paragraph was EXECUTED against the tree this turn, not read off a report). THE CORE CEILING IS CLOSED, and the proof is a merge, not a reading: a four-ticket base merged against a stored copy claiming `daily: 500` and carrying eight $25 tickets seats SIX tickets / $150 in BOTH argument orders, refuses the rest BY NAME (`coreDropped ["id7","id8"]`, each with its own `coreDroppedPL` receipt) and warns "the $150 core allotment is spent — 2 core ticket(s) refused by the merge"; the mirror case on the fun rail, a copy claiming `fun: 100`, still seats exactly ONE $25 parlay. What the ceiling deliberately does NOT do is truncate a day that was ALREADY over cap before the merge — that day is still kept whole and MARKED, which is the same doctrine as before — measured on the same pair with the base's four tickets removed from the argument, the merge keeps all eight, stamps `capBreach {"core":{"sum":200,"cap":150}}` and warns "the merged day is OVER ITS ALLOTMENT and was kept, not truncated — core $200 of $150"; note the stamped `cap` is 150 on a day whose own `daily` field still reads 500, which is the clamp saying so in the stored record. A SINGLE LEG-SHAPE MISMATCH NO LONGER DISABLES THE WHOLE RECONCILIATION: `legsDiffer` is now a POSITIVE disagreement over `LEG_IDENTITY` (`if (u == null || v == null) return false;`), so the conditionally-written `gkey` is not evidence of a different bet, and the agreement gate sits PER ID inside `unionCore`. Measured this turn: a pair whose shared `id1` differs only by that field RECONCILES — the receiptless raise is refused, $15 stands, `stakeConflict {"id1":{"kept":15,"refused":25}}` — while only the genuinely different `id2` lands on `betConflict ["id2"]`, byte-identical in both argument orders. THE FUN RAIL DECIDES WHICH WAGER SURVIVES BY GRADING, NOT BY WHICH COPY WAS SEATED FIRST — executed on five pairs, both orders each: an UNGRADED $25 parlay outranks a settled WINNER paying $92.50, a settled winner paying $60 outranks a settled LOSER, and two winners are separated by payout (the $90 seats, the $30 drops); every evicted ticket leaves a full `{result, payout, stake}` receipt on `funDroppedPL`, so a wager is never deleted in silence. AND AN UNGRADED DROPPED WAGER IS NO LONGER SHOWN AS A LOSS ON THE CARD: `cfbDropLine` (`src/components/cfb/CfbLedger.tsx`) renders a receipt whose result is none of `won` / `lost` / `push` as "$25 fun · pending — not graded yet, no P/L booked", with NO payout figure at all, and the day's total counts settled receipts only — measured, a day carrying a $60 core winner beside a pending fun drop reads `dropPl` 35, a day carrying one settled fun loser reads −25, and a day with no markers still returns `null` and draws no extra node. EVERY OTHER LIMIT ABOVE WAS RE-VERIFIED ONCE MORE IN THIS PASS AND EVERY ONE SURVIVES, except (v), narrowed again in its own entry. The reads, all made this turn: the ALLOCATION constants are still `maxStake: 25`, `minStake: 5`, `tickets: { min: 3, max: 7 }`, `forcedMaxDec: 1.75`, `forcedMinEvPct: 0`, `oneLegPerGame: true` in `src/lib/cfb/rules.ts` (L42–L50), and WIDENING THEM so a thin slate can deploy the full $150 REMAINS AN OWNER DECISION THAT HAS NOT BEEN MADE — which is exactly why the fixture leaves $75 undeployed; `vercel.json` read whole declares exactly TWO crons, BOTH `/api/scheduler` (`?poke=evening` at `45 21 * * *`, `?poke=night` at `0 0 * * *`), with `grep -ic cfb vercel.json` = 0 and `grep -rln "api/scheduler" .github/workflows/` returning nothing across the seven workflows that directory holds (board-archive, context, hr-overround, line-history, model, props-history, ufc), so `/api/cfb/lock` still has no cron of its own and "per day" is still TWO POKES A DAY as far as this tree can guarantee; the STEADY-STATE Odds cost per CFB date is unchanged at 6 credits for the lock's own game-lines pull plus at most `CFB_TOPUP_MAX` (2, `src/lib/cfb/rules.ts` L187 this turn) top-up boards at 6 each — 18 credits a date at the ceiling, 6 on a date that never tops up, none of it counted by `CFB_PROPS.dailyBudget`, and the 6-credit unit remains the 2026-09-05 prod quota reading (17578 → 17572), the one figure here that cannot be re-taken from the tree; and the MLB capabilities CFB STILL LACKS are all four still absent — per-BLOCK locking and the block ladder (`app/api/scheduler/route.ts` L278 fetches `/api/generate?block=…`, `grep -ic cfb app/api/generate/route.ts` = 0), the self-check's reading artifact (`writeReading` / `buildReadingSafe` appear in `src/lib/server/self-reading.ts`, `app/api/generate/route.ts` and `app/api/scheduler/route.ts` only), with it the UNGATED READ URL (`grep -c syncAuthed app/api/board/route.ts` = 0, while `app/api/cfb/ledger/route.ts` L36 refuses on `if (!syncAuthed(req))`), and CALIBRATION (`grep -ic cfb app/api/calibrate/route.ts` = 0, so the settle pass moves the CFB bankroll and nothing calibrates the CFB model off it). WHAT THE MONEY METADATA DOES ON A MERGE, ADDED 2026-09-06 by the audit round and read off `src/lib/ledger-merge.ts` L1076–L1080 and L1151–L1165 this turn: `carryMoneyMeta` walks three keys — `MONEY_META` is `["allocSum", (t: SyncTicket) => Number(t.stake) || 0]`, `["gatedSum", (t: SyncTicket) => ((t as Record<string, unknown>).forced ? 0 : (Number(t.stake) || 0) - (Number(t.topUp) || 0))]`, `["topUpSum", (t: SyncTicket) => Number(t.topUp) || 0]` — and it treats the FIRST differently from the other two. `allocSum` IS DERIVED, not carried: the arm at L1157–L1160 reads `if (key === "allocSum") {`, `rec[key] = money(sum);`, `continue;`, `}` — the money-rounded sum of the projection over the tickets the merge ACTUALLY SEATED, so it is a restatement of the seated core and cannot drift from it. `gatedSum` and `topUpSum` are CARRIED BY DELTA — `const delta = sum - before.reduce((s, t) => s + project(t), 0);` then `rec[key] = money(cur + delta);`, skipped entirely on `if (Math.abs(delta) < 1e-9) continue;` — because those two describe how the money was ACQUIRED (allocator sizing versus accumulated top-up residue), which the seated list alone cannot rebuild. All three are written only when the entry already carries the key (`if (rec[key] === undefined || !Number.isFinite(cur)) continue;`), so a merge never mints a money field a writer did not. MEASURED this turn on the rival 2026-09-05 pair above, whose two copies recorded `allocSum` 40 and 65: the merged day carries `allocSum` 80, the sum of the three tickets actually seated — neither input, and no delta of them. AND THIS IS AN MLB-SIDE FIELD ON A SHARED KERNEL: `grep -rln allocSum src app` this turn finds `src/lib/ledger-merge.ts`, `src/lib/server/blocks.ts`, `src/lib/server/lock-card.ts`, `src/lib/server/self-reading.ts`, `src/lib/server/card-view.ts`, `app/api/generate/route.ts` and, under `src/lib/cfb/`, only a COMMENT in `store.ts` — no CFB writer mints one; and `decideCfbTopUp` reads neither `allocSum` nor any other stored sum, it recomputes from the day's own tickets with `const staked = cfbStakeOf(entry.core);` (`src/lib/cfb/lock-server.ts` L462) over `export const cfbStakeOf = (tix: CfbTicket[]): number => tix.reduce((s, t) => s + t.stake, 0);` (L400), which is why a stale metadata field cannot mis-size a CFB top-up. THE DAY'S DISCLOSURE IS NOW A PURE FUNCTION, PINNED BEHAVIOURALLY RATHER THAN BY SOURCE TEXT (2026-09-06, defects B1 and B2, `src/components/cfb/CfbLedger.tsx` read this turn): `cfbDayMarks(e)` reads the seven markers off the entry, `cfbDisclosureOf(marks)` (L329) turns them into `{ lines, drops }` — every sentence, every `key`, every tone class, and whether a drop list exists at all — and `cfbDayDisclosure(e)` (L362) composes the two and answers `null` for a day carrying no markers, which is the null both surfaces guard their marker elements on. `CfbDayMarksNote` (L392) is left holding markup only, and its own docblock says why: "THIS COMPONENT DECIDES NOTHING … A regex over this JSX was the only guard the user-visible deliverable had, and a mutation pass showed the whole body could be made unreachable with the suite green — so the decision moved out rather than the pins moving in." The row builders beside it are pure too (`cfbDropLine` L259, `cfbRivalLine` L277, `cfbBreachChip` L288), so the suite compares VALUES instead of matching source — which is the point, since a source-text pin passes a behaviourally identical reword and fails a harmless one. EXECUTED rather than read: `cfbDayDisclosure` over the merged rival day returns `{"lines":[{"key":"bet:cfb-2026-09-05-1","claim":"cfb-2026-09-05-1 · another copy names a DIFFERENT bet — this device's ticket stands","note":"Rival cards are never mixed, so nothing from that copy was added to this day."}],"drops":null}`, and `tests/cfb-card-ui.test.ts` carries the describes "cfb-card-ui — the day's disclosure is decided by a pure function (B1)" (L398) and "cfb-card-ui — a refused rival card is disclosed (B2)" (L489), both grepped this turn. AN OWNER DECISION, NOT A PATCH — WHEN THE CFB LOCK CAN ACTUALLY FIRE, AND WHAT THIS REPOSITORY CANNOT KNOW ABOUT IT (raised 2026-09-06; `vercel.json` is untouched by this pass and nothing here should be changed on the strength of this paragraph alone). THE MEASUREMENT, EXECUTED THIS TURN against the repo's own 2026-09-05 fixture at the fixture's own clock: `CFB_LOCK.leadMs` is `60 * 60_000` = 3600000 ms, 60 minutes (`src/lib/cfb/rules.ts` L114), the fixture's only kickoffs are 16:00Z and 16:30Z, so `decideCfbLock` puts `locksAt` at 15:00:00Z. `decideCfbLock` never CLOSES that window — past `locksAt` it answers `lock` for ever, with `ahead` counting only the kickoffs still in front — so the window that matters FOR MONEY is `[firstKickoff − 60 min, firstKickoff)`, and on this slate it is exactly one hour wide. `vercel.json` declares exactly TWO crons, both on `/api/scheduler`, at `45 21 * * *` (21:45Z) and `0 0 * * *` (00:00Z), and `grep -rln "api/scheduler" .github/workflows/` returns nothing across the seven workflows that directory holds. DRIVEN AT BOTH OF THOSE TIMES on the fixture: 21:45Z gives `decide=lock ahead=0/12`, `cfbPricedAhead` 0, and an entry of core 0 / $0 of the $150, funT 0 / $0 of the $25, `noPlay: true`, note "NO-PLAY — lock window missed: every one of the 12 games on 2026-09-05 had kicked off before the server could lock. Nothing staked; no line that was already gone was priced."; 00:00Z the following day gives a byte-identical result. SO, PLAINLY: ON THE TWO POKES THIS REPOSITORY ITSELF SCHEDULES, THE 2026-09-05 SLATE DEPLOYS $0 OF THE $150 AND $0 OF THE $25. The $75 / $25 card this bullet reports below is reachable only from a poke INSIDE the one-hour window, and the earliest poke `docs/cron-jobs.md` describes is 15:00Z. WHAT THE REPO CAN KNOW: those two cron times; that neither is inside the window for this slate; that `/api/cfb/lock` has no cron of its own (`grep -ic cfb vercel.json` = 0); and that a Hobby-plan cap of TWO cron entries is PINNED IN THE SUITE — `tests/vercel-cron-pokes.test.ts` L69–L70, the `it` named "the Hobby plan cap is respected (2 cron jobs) and the git deploy config survived" asserting `expect((cfg.crons ?? []).length).toBeLessThanOrEqual(2);`, read this turn — so "add a third in-repo cron" is not an option that exists. WHAT THE REPO CANNOT KNOW: whether anything fires at 15:00Z at all. `docs/cron-jobs.md` L17 describes an external cron-job.org entry hitting `/api/scheduler` "every 15 min, hours 15–23 and 0–2", and that document opens under a red banner — "🔴 THE ENTRIES BELOW WERE NEVER CREATED — 2026-08-06, ground truth from Josh's dashboard" — recording that an earlier set of entries was authored as ready-to-paste, never pasted, and that "the record carried them as live for ten days"; the same document says at L105 that "cron-job.org's punctuality at these hours is unverified". No test, no file and no measurement in this tree can confirm that entry exists today: it is a setting in a third-party account. THE CFB DESK'S WHOLE $150 + $25 DAY THEREFORE DEPENDS ON A TICKER THE REPOSITORY CAN NEITHER SEE NOR PIN, while the two pokes it CAN pin both land after every kickoff on the one slate there is a fixture for. THE OPTIONS, for Josh and not for a writer: (1) SPEND ONE OF THE TWO HOBBY CRONS on a slot inside the window — the cheapest change, but it costs one of the two evening pokes the MLB desk's own gap analysis put there, so it is a trade and not a fix; (2) CONFIRM THE EXTERNAL TICKER on the cron-job.org dashboard and record the confirmation the way `docs/cron-jobs.md` records ground truth — dated and attributed — which costs nothing in the repo and removes the unknown, and which the banner above argues is the first thing to do whatever else is chosen; (3) WIDEN `CFB_LOCK.leadMs` so the window opens earlier and a 15:00Z poke still catches a 16:00Z kickoff with a real lead — this changes WHAT the desk prices and not merely when, so it is an allocation decision of the same kind as the rules below; (4) UPGRADE THE PLAN and lift the two-cron cap, which is money; (5) ACCEPT IT and treat the external ticker as the desk's scheduler of record, in which case the banner's own lesson applies and the entry must be verified and dated rather than assumed. NOTHING HERE IS RECOMMENDED AND NOTHING HERE IS APPLIED. AND THE FIXTURE ITSELF, RE-DRIVEN THIS TURN through `buildCfbBoard` → `buildCfbCard` → `buildCfbLockEntry` over the repo's own `tests/fixtures/cfb/` captures (`espn-scoreboard-2026-09-05.json`, `odds-ncaaf-2026-09-05.json`, `espn-fpi.json`) at `now = 2026-09-05T12:00:00Z` with `bankroll: 2500`: the board is 12 GAMES and 72 PRICED ROWS, all 12 still ahead at the lock instant, `cfbPricedAhead` 60 — and AT THE LOCK the 2026-09-05 fixture day deploys 3 CORE TICKETS AT $25 EACH = $75 OF THE $150 and ONE FUN PARLAY AT $25 = $25 OF THE $25, with `daily` 150, `fun` 25 and NO `noPlay` flag on the entry at all (`"noPlay" in entry` is false — absent, not present-and-false). Every one of those figures is CONFIRMED against the last verified set and none is corrected, and `buildCfbCard`'s own first note still reads, character for character, "$75 of the $150 stayed undeployed — every ticket is at the $25 max and no other game offers a side ≥ 0% EV under 1.75." THE CLOSING ROUND’S THREE REMAINING MONEY RULES, EVERY FIGURE BELOW EXECUTED THROUGH `mergeLedgers` THIS TURN IN BOTH ARGUMENT ORDERS AND IDENTICAL IN BOTH (2026-09-06). (1) A REFUSED WAGER’S VERDICT IS WITHDRAWN, AND THE DAY RE-GRADES INSTEAD OF CLOSING ON A FICTION. Because the grading maps merge FILL-ONLY, a rival copy’s verdict used to be copied in under a shared id whose bet the merge had just refused — and `won` is a resolved result no overlay may overwrite, with `done: true` telling every grader there is nothing left to recompute, so the day closed on that fiction permanently. The withdrawal was driven off the union of both dispute channels — CITATION CORRECTED AND THE RULE ITSELF NARROWED 2026-09-06 in the final round, see (2) of THE FINAL ROUND section at the end of this bullet: the line this sentence quoted as `const withdrawn = united ? [...new Set([...united.restaked, ...united.betConflict])].sort() : [];` at "L1669" DOES NOT GREP any more, and neither its `delete tix[id];` nor its `out.grading.done = false;` is unconditional now. The live rule is the `baseGraded` / `disputed` / `withdrawn` / `legWithdrawn` chain in `mergeDay`, quoted and measured in that section. MEASURED on two copies of 2026-09-05 sharing `cfb-2026-09-05-core-1` on DIFFERENT games, the base seating it on g1 with core-2 already graded `lost` and `done: true`, the rival naming g2 and grading core-1 `{result:"won", payout:47.73}`: the merged day answers `{"seatedGkey":"g1","verdictC2":{"result":"lost","payout":0},"done":false,"legKeys":["g2b|spread|home|-3"],"betConflict":["cfb-2026-09-05-core-1"],"realizedPL":-25,"coreSum":50}` — the $47.73 payout is GONE from `grading.tickets`, the orphan `g2|spread|home|-3` leg key is gone with it, and the day is reopened for the grader. THE REMEDY IS SCOPED TO THE DISPUTED ID exactly as the detector is: core-2’s own `lost` verdict and its own leg key are untouched. And the merge does NOT invent the verdict it just refused — `ticketPL` returns 0 for a ticket carrying no grade (`if (!g) return 0;`, `src/lib/bankroll.ts` L102), so the withdrawn ticket scores nothing until the reopen’s re-grade prices it, which is why the merged day measures −25.00 (core-2’s loss alone) and the honest −50.00 for that two-ticket $50 day is reachable only after that re-grade. (2) EVERY DISCARDED CORE TICKET LEAVES A RECEIPT NAMING ITS MONEY. Both refusal gates now write the same two channels — the allotment’s `if (sum + stake > cap + 1e-6) {` (L1115) and the rival lock’s `if (betConflict.length) {` loop over `unseen` (L1106) — each doing `dropped.push(String(t.id));` beside `droppedPL[String(t.id)] = receiptOf(t, verdictOf(String(t.id), base, other));` (L1109 and L1117, both grepped this turn). MEASURED — Josh’s one-ticket Builder lock ($20 on g9, confirmed) against a server RIVAL lock holding the same positional core-1 from a different board plus core-2 and core-3 at $25, core-2 already graded a $61.25 winner: `{"ids":["cfb-2026-09-05-core-1"],"sum":20,"allocSum":20,"betConflict":["cfb-2026-09-05-core-1"],"coreDropped":["cfb-2026-09-05-core-2","cfb-2026-09-05-core-3"],"coreDroppedPL":{"cfb-2026-09-05-core-2":{"result":"won","payout":61.25,"stake":25},"cfb-2026-09-05-core-3":{"result":"pending","payout":0,"stake":25}}}` — $50 of wagers a device still shows are refused BY NAME WITH THEIR MONEY instead of vanishing, where the same pair used to come out with `coreDropped` and `coreDroppedPL` both absent. THE RECEIPT IS NOT PERMANENT: measured on the same base against an AGREEING copy carrying all three ids, the merged day seats three tickets / $70 with `coreDropped`, `coreDroppedPL` and `betConflict` all ABSENT — a later sync that resolves the dispute clears the marker rather than leaving it contradicting the ticket beside it. The console warning is branched to match the gate: "belong to a RIVAL lock and were refused, not staked on this card — each is named with its money" on the rival path, the byte-unchanged "core allotment is spent — " wording on the cap’s. Note what the refusal deliberately does NOT do: it does not suppress the day’s remaining room. `allocSum` is DERIVED from the seated core, so the $20 card reports $20 and the top-up decider is offered the $130 this card is genuinely short — the refused money was never staked on THIS card, and hiding that room would leave the day under the $150 Josh asked for, the same instruction missed from the other side. (3) THE FUN SEAT IS MONOTONE ACROSS A FULL GRADING SEQUENCE. `unionFun`’s ranking gained a seated-wins term BEFORE payout — `[...pool].sort((p, q) => q.rank - p.rank || Number(q.mine) - Number(p.mine) || q.payout - p.payout || p.seq - q.seq)` (L767, grepped this turn) — so within a band a candidate the day ALREADY SEATS outranks one it does not, and only two candidates the day does not seat are separated by the money. MEASURED over the whole sequence on the two ids `planCfbTopUp` mints, both orders at every step: BOTH UNGRADED seats `cfb-2026-09-05-topup2-fun-1`, funSum 25, `funDropped ["cfb-2026-09-05-topup1-fun-1"]` with receipt `{"result":"pending","payout":0,"stake":25}`, realizedPL 0; THE SEATED TICKET WINS $150 — same seat, realizedPL 125; THE RIVAL’S OWN $200 VERDICT LANDS — SAME SEAT, realizedPL still 125, and the evicted ticket’s receipt merely updates to `{"result":"won","payout":200,"stake":25}`; RE-MERGE — same seat, realizedPL 125. Pre-fix the third step moved the seat to `cfb-2026-09-05-topup1-fun-1` and realizedPL from 125 to 175 on $25 of fun money. The day’s P/L is now a function of the card the desk actually holds, not of what a paper ticket on a card it does not hold happened to pay. RE-VERIFIED BY EXECUTION AT THE CLOSE OF THE ROUND, AND THE CRON READING IS CONFIRMED RATHER THAN CORRECTED (2026-09-06, driven not read): `CFB_LOCK.leadMs` printed as 3600000 ms — `leadMs: 60 * 60_000,` at `src/lib/cfb/rules.ts` L114 — over the fixture’s only two kickoff instants, 2026-09-05T16:00:00.000Z and 16:30:00.000Z, so `decideCfbLock` puts `locksAt` at 2026-09-05T15:00:00.000Z and the window that matters for money is [firstKickoff − 60 min, firstKickoff). DRIVEN AT THE TWO TIMES `vercel.json` ACTUALLY SCHEDULES — 21:45Z (`{ "path": "/api/scheduler?poke=evening", "schedule": "45 21 * * *" }`) and 00:00Z (`{ "path": "/api/scheduler?poke=night", "schedule": "0 0 * * *" }`), the file read whole this turn — `decideCfbLock` answers `lock` with `ahead: 0` of `total: 12`, `cfbPricedAhead` 0, and `buildCfbLockEntry` writes CORE 0 tickets / $0 of the $150 and FUN 0 tickets / $0 of the $25, `noPlay: true`, note "NO-PLAY — lock window missed: every one of the 12 games on 2026-09-05 had kicked off before the server could lock. Nothing staked; no line that was already gone was priced." — byte-identical at both times, against the fixture’s own noon figures of 3 core / $75 and 1 fun parlay / $25 from the same code. A poke at 15:00:00Z, the first instant of the window, DOES catch it: `decideCfbLock` answers `lock` with `ahead: 12`, and the entry is again 3 core / $75 and 1 fun / $25 — so the whole difference between a $100 day and a $0 day is which minute the poke arrives, with zero margin at the open. THE DESIGN THEREFORE DEPENDS ON AN EXTERNAL SCHEDULER THIS REPOSITORY CANNOT VERIFY: `docs/cron-jobs.md` L17 describes a cron-job.org entry hitting `/api/scheduler` "every 15 min, hours 15–23 and 0–2" (UTC), which is a setting in Josh’s own third-party account; that document opens at L3 under "🔴 THE ENTRIES BELOW WERE NEVER CREATED — 2026-08-06, ground truth from Josh’s dashboard", recording that an earlier set of exactly such entries was authored as ready-to-paste, never pasted, and that "the record carried them as live for ten days" (L9), and it says at L105 that "cron-job.org’s punctuality at these hours is unverified". No test, no file and no measurement in this tree can confirm that entry exists today. A THIRD IN-REPO CRON IS NOT AN AVAILABLE OPTION: `tests/vercel-cron-pokes.test.ts` L69–L70, the `it` named "the Hobby plan cap is respected (2 cron jobs) and the git deploy config survived", asserts `expect((cfg.crons ?? []).length).toBeLessThanOrEqual(2);` — read this turn — while `grep -ic cfb vercel.json` = 0 and `grep -rln "api/scheduler" .github/workflows/` returns nothing across the seven workflows that directory holds (board-archive, context, hr-overround, line-history, model, props-history, ufc). THIS REMAINS AN OWNER DECISION AND NOTHING HERE IS APPLIED — `vercel.json` is untouched by this pass — with the five options already listed above unchanged: spend one of the two Hobby crons on a slot inside the window (a trade against the evening poke, not a fix); CONFIRM the external ticker on the cron-job.org dashboard and record the confirmation dated and attributed the way that document records ground truth, which costs nothing in the repo and is what the banner’s own lesson argues for first; widen `CFB_LOCK.leadMs` so the window opens earlier, which changes WHAT is priced and not merely when; upgrade the plan; or accept the external ticker as the scheduler of record, in which case it must be verified and dated rather than assumed. THE FIXTURE FIGURES, RE-MEASURED THIS TURN AND CONFIRMED WITH NOTHING CORRECTED: driven through `buildCfbBoard` → `buildCfbCard` → `buildCfbLockEntry` over `tests/fixtures/cfb/espn-scoreboard-2026-09-05.json`, `odds-ncaaf-2026-09-05.json` and `espn-fpi.json` with `bankroll: 2500`, the board is 12 GAMES and 72 PRICED ROWS with `cfbPricedAhead` 60, and AT THE LOCK the 2026-09-05 fixture day deploys 3 CORE TICKETS AT $25 EACH = $75 OF THE $150 (ids `cfb-2026-09-05-core-1` … `-core-3`) and ONE FUN PARLAY OF 3 LEGS AT $25 = $25 OF THE $25 (`cfb-2026-09-05-fun-1`), with `daily` 150, `fun` 25 and NO `noPlay` key on the entry at all (`"noPlay" in entry` is false — absent, not present-and-false). Read at the fixture’s own noon clock and again at the true lock instant 15:00:00Z, the two are identical figure for figure; the one honest qualification is that at 12:00:00Z `decideCfbLock` answers `waiting`, so noon is a card-builder reading and 15:00:00Z is the first instant the route would actually write it. `buildCfbCard`’s own first note still reads, character for character, "$75 of the $150 stayed undeployed — every ticket is at the $25 max and no other game offers a side ≥ 0% EV under 1.75." THE THREE OWNER-FACING FACTS THIS BULLET IS REQUIRED TO KEEP STATING WERE RE-VERIFIED AT THE CLOSE OF THE ROUND AND ALL THREE ARE STILL TRUE — on top of, not instead of, limits (i)–(ix) above and their own re-verification paragraphs. THE ALLOCATION RULES ARE UNCHANGED AND WIDENING THEM IS AN OWNER DECISION THAT HAS NOT BEEN MADE — printed from `src/lib/cfb/rules.ts` this turn as `maxStake: 25` (L42), `minStake: 5` (L43), `tickets: { min: 3, max: 7 }` (L44), `forcedMaxDec: 1.75` (L46), `forcedMinEvPct: 0` (L48), `oneLegPerGame: true` (L50) — so nothing in this round widens what a thin slate can deploy of the $150 core, and that is exactly why the fixture leaves $75 undeployed: its three tickets are each already at the $25 `maxStake`, `oneLegPerGame` forbids a second ticket on a seated game, and no other game on that slate offers a side at or above `forcedMinEvPct` under `forcedMaxDec` 1.75. THE STEADY-STATE ODDS COST PER CFB DATE IS UNCHANGED: 6 credits for the lock’s own game-lines pull plus at most `CFB_TOPUP_MAX` (2, `src/lib/cfb/rules.ts` L187 this turn) top-up boards at 6 each — 18 credits a date at the ceiling, 6 on a date that never tops up, none of it counted by `CFB_PROPS.dailyBudget`, which counts props pulls only; the 6-credit unit remains the 2026-09-05 prod quota reading (17578 → 17572) and is the ONE figure in this bullet that cannot be re-taken from the tree. THE MLB CAPABILITIES CFB STILL LACKS ARE ALL FOUR STILL ABSENT, each re-grepped this turn: per-BLOCK locking and the block ladder (`app/api/scheduler/route.ts` L278 fetches `/api/generate?block=…`, `grep -ic cfb app/api/generate/route.ts` = 0); the self-check’s reading artifact (`writeReading` / `buildReadingSafe` appear in `src/lib/server/self-reading.ts`, `app/api/generate/route.ts` and `app/api/scheduler/route.ts` only, never on a CFB path); with it the UNGATED READ URL (`grep -c syncAuthed app/api/board/route.ts` = 0, while `app/api/cfb/ledger/route.ts` L36 refuses on `if (!syncAuthed(req)) return NextResponse.json({ error: "bad-sync-key" }, { status: 401 });`); and CALIBRATION (`grep -ic cfb app/api/calibrate/route.ts` = 0, so the settle pass scores CFB days and moves the CFB bankroll while nothing calibrates the CFB model off them). AND FOUR PROSE CLAIMS IN FILES THIS PASS DOES NOT OWN WERE STALE WHEN THIS PARAGRAPH WAS WRITTEN — THREE OF THE FOUR HAVE SINCE BEEN CORRECTED IN THEIR OWN FILES AND ARE WITHDRAWN, re-read one at a time this turn: the `src/lib/cfb/types.ts` `stakeConflict` doc now states both rules explicitly, the `src/components/cfb/CfbLedger.tsx` index-signature parenthetical now says the signature is gone, and the `src/lib/cfb/lock-server.ts` `topUps` docblock now says the cast is load-bearing. ONLY THE `app/api/cfb/lock/route.ts` ONE IS STILL LIVE; it is limit (xi) of the final round below. The paragraph is kept as written for the record — recorded here rather than left to be re-discovered, since no code is wrong at any of the four and each would otherwise read as a live fact about the CFB desk. `src/lib/cfb/types.ts` L328–L329 still documents `stakeConflict` as "a shared id whose two copies disagreed on stake with no `topUp` receipt: the SMALLER stake stands as `kept`, the larger is named as `refused`"; BOTH halves are now false of the rival branch, which keeps `mine` — this card’s stake, not necessarily the smaller — and which can refuse a raise whose receipt WAS read and accepted, purely on allotment grounds. The card itself no longer says that: the note `cfbDisclosureOf` renders is the disjunction `Two copies of ${c.id} named different stakes and the merge would not seat the larger — either no top-up receipt accounted for it, or seating it would have carried this day past its allotment.` (`src/components/cfb/CfbLedger.tsx` L397, grepped verbatim this turn), and the list heading is now `Refused by the merge · ${marks.drops.length}` (L407) rather than the "Dropped by the cap" wording the rival gate made false. `src/components/cfb/CfbLedger.tsx` L79–L80 says "`SyncEntry` carries an `[k: string]: unknown` index signature, and `CfbLedgerEntry` extends it" — false since the marker names were closed to the compiler: `export type CfbLedgerEntry = NoIndex<SyncEntry> & {` at `src/lib/cfb/types.ts` L258, over `type NoIndex<T> = { [K in keyof T as string extends K ? never : number extends K ? never : K]: T[K] };` (L256). The surrounding WHY A READER AND NOT A RENDER-TIME LOOKUP rationale still stands; only the parenthetical is stale. `src/lib/cfb/lock-server.ts` L405 says "`CfbLedgerEntry` extends `SyncEntry`, whose index signature already admits the field" of `topUps` — false for the same reason; `cfbTopUpsOf` reads it through a `Record<string, unknown>` cast so it compiles and behaves as before, but a `topUps` written into a `CfbLedgerEntry` object LITERAL would now be a TS2353 and must be declared in `src/lib/cfb/types.ts` instead. `app/api/cfb/lock/route.ts` L467 says of `gradedAt` that "the field is on SyncEntry’s index signature, not its shape" — it is now on `CfbLedgerEntry`’s own shape (`gradedAt?: number | null;` at `src/lib/cfb/types.ts` L342, beside `attemptedAt?: number;` at L348); the reason that comment gives for preferring `Number(e.gradedAt) || …` over `??` — a stored JSON blob may carry a non-numeric or absent value — is still correct at runtime. THE FORWARD-LOOKING COST OF THAT CLOSURE, stated once: a NEW field added to a `CfbLedgerEntry` object literal now raises TS2353 instead of passing in silence, which is the protection working — and the fix for such an error is to DECLARE the field in `src/lib/cfb/types.ts`, never to cast the literal to `Record<string, unknown>`, which would restore exactly the misspelled-marker defect the closure removed. THE FINAL ROUND OF INSTRUCTION 45 (2026-09-06) — FOUR KERNEL CHANGES, EACH MEASURED HERE THIS TURN, NONE OF THEM IN A FILE THIS DOC OWNS. (1) EVERY STAKE RAISE IS NOW BOUNDED BY THE DESK ALLOTMENT ON BOTH THE ORDINARY AND THE RIVAL PATH. `unionCore` takes the bound once at the head of the merge, `const cap = allotmentCap(base, other);`, and carries a running total, `let projected = stakeSum(base.core);`, that is now hoisted OUT of the rival branch, so the raise arm `} else if (receipted && projected + lift <= cap + 1e-6) {` governs the ordinary path as well as the rival one — the rival flag `const rivals = betConflict.length > 0;` now only chooses WHICH receipt is written, never whether the money is checked. Before this round only the rival path was bounded, and two stored copies of the same date each already at exactly the $150 allotment could merge to $170. Measured this turn on two such copies: preA 150, preB 150, merged coreSum 150 and allocSum 150 in BOTH argument orders, six ids at $25 each, the refused lift receipted as `stakeConflict` {"cfb-2026-09-05-core-1":{"kept":25,"refused":45}}, orderFree true and idempotent true; the MLB twin of the same fixture measured 150 → 150 in both orders with {"p:abc":{"kept":25,"refused":45}}, so the bound is the shared kernel's and not a CFB special case. (2) A DAY WHOSE SEATED WAGER DIFFERS FROM THE ONE A COPY PRICED NOW WITHDRAWS ONLY THE DISPUTED VERDICT, AND CONVERGES INSTEAD OF CYCLING. The already-graded ids are captured BEFORE grading crosses — `const baseGraded = new Set(Object.keys((base.grading?.tickets ?? {}) as Record<string, unknown>));` — and the disputed set is the rival ids minus those: `const disputed = united ? united.betConflict.filter((id) => !baseGraded.has(id)) : [];`. The withdrawal is then `const withdrawn = united ? [...new Set([...united.restaked, ...disputed])].sort() : [];`, the leg sweep the wider `const legWithdrawn = united ? [...new Set([...withdrawn, ...united.betConflict])].sort() : [];` so no orphan leg key is left standing over a game the day does not seat, the per-ticket deletion is conditional, `if (raised.has(id)) delete tix[id];`, and so is the reopen, `if (withdrawn.length) out.grading.done = false;`. The narrowing matters because a verdict the base already owned is no longer thrown away merely because a rival copy names a different bet under the same id — that discard was what made the day oscillate. Measured this turn over six merge/grade/merge cycles in BOTH orders, identical both ways: cycle 1 done=false, P/L −25, one verdict; cycle 2 done=true, P/L −50, both verdicts lost; cycles 3, 4, 5 and 6 byte-identical to cycle 2, with `legKeys` fixed at ["g2b|spread|home|-3"] and `betConflict` fixed at ["cfb-2026-09-05-core-1"] throughout — a fixed point reached at cycle 2 from either side. (3) THE APPEND REFUSAL IS DATE-WIDE; THE RECONCILIATION IS NOT. `const appendable = betConflict.length ? [] : unseen;` — ONE rival id anywhere on the date refuses EVERY unseen ticket the other copy carries, because a card is a portfolio and half of a rival card is not a card; reconciliation of ids the two copies already share stays per id. Measured this turn, both orders identical: ids core-2 and core-3 survive at $50 and $25 for a $75 day, `betConflict` names core-3 alone, the unseen `cfb-2026-09-05-topup1-core-4` is refused and receipted with its money in `coreDroppedPL` as {"result":"pending","payout":0,"stake":25}, and core-2 still reconciles on its own to {"kept":50,"refused":25} — so the date-wide half of the rule never loses money silently: every discarded ticket leaves a named receipt carrying its stake. (4) THE FALSE-CODE-QUOTE CLASS IS NOT GUARDED BY ANY TEST, AND THIS SENTENCE CORRECTS THE ONE THAT SAID IT WAS (closing round, 2026-09-06). What stood here claimed that a guard file, `tests/comment-citation.test.ts`, "reads every tracked source comment and every tracked .md file". That claim is false twice over. Its COVERAGE half was reported false by the guard's own author the round after it was written — the scanner lost synchronisation inside template literals and on an apostrophe in JSX text and went blind to every comment after that point — and this file cannot re-verify that measurement, because the guard no longer exists to run; it is recorded as attributed, not as read. Its EXISTENCE half is false as of this turn: `ls tests/comment-citation.test.ts` answers `No such file or directory`, and a grep for the string `comment-citation` across the tracked tree returns exactly ONE file — this one, where every occurrence of the string sits inside this correction or inside the limit that repeats it below. THE GUARD WAS DELETED DELIBERATELY, BY THE AGENT WHO OWNED IT, and the reason is kept rather than summarised away because it bounds what any future attempt can achieve: with its scanner corrected, every red it printed on this tree was a FALSE red, because this repo's correction notes print the false quote and the true quote inside one window, so any retraction phrase that exempts the false half exempts the true half with it — the guard goes blind precisely in the paragraphs it exists to police. Binding an attestation to ONE quote (a tag carried by the quote itself) rather than to a window of characters is the fix; that is new machinery and it was written down and not built. WHAT GUARDS THIS CLASS TODAY IS REVIEW, PLUS ONE CONVENTION THAT COSTS NOTHING AND CANNOT GO STALE: a comment or a doc line NAMES A SYMBOL, and an expression is quoted only under a dated retraction. A symbol name is greppable and survives an edit; a quoted expression is stale the moment anyone touches the line, which is how this class recurred round after round — and once inside a single turn, when the file being quoted was under concurrent rewrite by another agent while the quote about it was being written. The sequencing rule that follows needs no code either and is likewise written down rather than built: a citation sweep is the LAST act of a turn, never an early one, because a sweep that ran before the final edit proves nothing about the final file — every recurrence of this defect has that shape. Several citations in this file were false and have been corrected in place above rather than deleted, each marked where it stood. THE LINE NUMBERS THROUGHOUT THIS FILE HAVE DRIFTED and are not to be trusted; they are deliberately NOT renumbered, because this bullet is one very long line and a bulk renumber is exactly the unverifiable edit that produced the false quotes in the first place. READ THE SYMBOL NAME, NOT THE NUMBER. THE CRON PARAGRAPH, RE-VERIFIED BY EXECUTION THIS TURN AND UNCHANGED IN SUBSTANCE. `CFB_LOCK.leadMs` is `leadMs: 60 * 60_000,` in `src/lib/cfb/rules.ts` and measured 3600000 ms = 60 minutes; the lock window is therefore [firstKickoff − 1h, firstKickoff]. On the repo's own 2026-09-05 fixture (12 games, 72 priced rows, two distinct kickoffs at 16:00Z and 16:30Z) the window is [2026-09-05T15:00:00Z, 2026-09-05T16:00:00Z]: at 12:00Z `decideCfbLock` returns waiting with locksAt 15:00Z and firstKickoff 16:00Z, and at 15:00Z it returns lock with ahead 12/12. `vercel.json` carries exactly two crons, at 45 21 * * * and 0 0 * * *, and BOTH LAND AFTER THAT WINDOW HAS CLOSED. Executed at both times against the same fixture: at 2026-09-05T21:45Z, ahead 0/12 and `cfbPricedAhead` 0, entry core 0 / coreSum $0 / funT 0 / funSum $0, noPlay true, note "NO-PLAY — lock window missed: every one of the 12 games on 2026-09-05 had kicked off before the server could lock. Nothing staked; no line that was already gone was priced."; at 2026-09-06T00:00Z, byte-identical. Against the fixture's own noon reading — waiting, card core 3 / coreSum $75, funT 1 / funSum $25 — the two shipped crons deploy $0 of the $175 on this slate. THE DESK THEREFORE DEPENDS ON AN EXTERNAL cron-job.org ENTRY IN THE OWNER'S ACCOUNT THAT THIS REPOSITORY CANNOT VERIFY, and `docs/cron-jobs.md` opens with a red banner recording that an earlier set of exactly such entries "WERE NEVER CREATED" and that "the record carried them as live for ten days" — the reason no claim about a live external cron may be written here from inference. `tests/vercel-cron-pokes.test.ts` pins the two-cron ceiling with `expect((cfg.crons ?? []).length).toBeLessThanOrEqual(2);`, so adding a third Vercel cron is a test change, not a config change. THIS IS AN OWNER DECISION, and its options as the repo shows them are: (a) create the cron-job.org entry and confirm it from the dashboard, the only place the truth lives, then record the confirmation here with its date; (b) repoint one of the two existing Vercel crons to a time inside the window — cheap, but a fixed UTC time cannot track a first kickoff that moves week to week; (c) raise the cron count, which requires changing the assertion above and is a plan question outside this repo; (d) add a GitHub Actions schedule, which is the one option this repo could verify itself — seven workflows already run here (board-archive, context, hr-overround, line-history, model, props-history, ufc) and NOT ONE of them touches CFB; or (e) accept a manual lock, the owner opening the desk before the first kickoff, which is what actually happens today. Until one is chosen, the honest statement is that nothing in this repository schedules the CFB lock. WHAT THE $150 CORE AND THE $25 FUN ACTUALLY DEPLOY AT THE LOCK, RE-MEASURED THIS TURN ON THE REPO'S OWN 2026-09-05 FIXTURE AT THE FIXTURE'S OWN CLOCK. The allotment is configured exactly as Josh asked: `CFB_PAPER` read this turn from `src/lib/cfb/rules.ts` is {"since":"2026-09-05","daily":150,"fun":25}, and the entry the lock writes stamps daily 150 and fun 25. What deploys is less. At the lock instant, 2026-09-05T15:00:00Z, with the board built from `tests/fixtures/cfb/espn-scoreboard-2026-09-05.json`, `odds-ncaaf-2026-09-05.json` and `espn-fpi.json`, `buildCfbLockEntry` returns 3 core tickets summing $75 (ids cfb-2026-09-05-core-1 through -3, read off `entry.core`) and 1 fun ticket of $25 on 3 legs (read off `entry.funT`) — the FUN allotment fully deployed, the CORE at half. The card states its own reason: "$75 of the $150 stayed undeployed — every ticket is at the $25 max and no other game offers a side ≥ 0% EV under 1.75." The binding constraints, read from `CFB_RULES` this turn, are maxStake 25, oneLegPerGame true, forcedMaxDec 1.75, tickets min 3 / max 7, maxLegs 2, maxDec 2.6, minEvPct 2, minStake 5, kellyFrac 0.25 and kellyCap 0.02, with the fun pool at legs 3–5, minDec 4, maxDec 40, minEvPct −3. So the answer to the instruction is: the money IS set to $150 and $25; the slate, under the current rules, seats $100 of it. THE KNOWN LIMITS AT THE CLOSE OF THIS WORK — the deliverable of the final round. Every item below was verified against the code as it stands THIS TURN and carries its status; the CLOSED ones keep their evidence so nobody re-opens them from an old memory, and the LIVE ones say what the owner would actually see. Nothing here is a plan: where a fix needs new machinery it is named as a recommendation and was deliberately not built, because the round that built machinery to fix machinery is the round that put this list back. The fixture figures in the paragraph directly above were re-driven twice this turn and came back identical, so the money statement they support is confirmed and not corrected. (i) CLOSED, AND IT WAS THE HIGHEST-VALUE MONEY DEFECT ON THE LIST — a stale 48-hour VOID no longer destroys a corrected verdict. Driven this turn through `mergeLedgers` in BOTH argument orders on a synthetic 2026-09-05 day (one core ticket, one copy holding {"result":"ungradable","payout":0} and the other the corrected {"result":"won","payout":47.73}): the void copy serialises 489 bytes against the corrected copy's 486, so `pickBase` STILL seats the void copy as the base in both orders — the merged day carries that copy's own note — and the merged verdict is nevertheless won / 47.73 in both, reproduced on a second run. What changed is not the seat but the grading merge: `settlementOf` in `src/lib/ledger-merge.ts` lets a SETTLEMENT fill over the ABSENCE of one, and `pending` / `ungradable` are the absence of one. Two disagreeing SETTLEMENTS are still decided by the base — that behaviour was pinned, not loosened, and ids under dispute are excluded so a rival card's payout is not imported. (ii) LIVE, MEDIUM, NOT THIS DOC'S FILE — `pickBase` still decides WHICH COPY IS THE BASE partly on the byte length of the serialised entry. Read this turn in `src/lib/ledger-merge.ts`: the rank is grade score, then CLV count, then confirmed count, then serialised length, with a raw string comparison as the final tiebreak. Byte length is evidence about nothing; the fixture in (i) is the proof, where the STALER copy wins the seat in both orders. Today the settlement rule makes the verdict come out right anyway, but the base still decides every fill-only field where the loser's answer is simply not consulted. RECOMMENDED AND NOT BUILT: rank on a settlement count instead of a byte length. That is a new rank term, it moves every merge in the file, and this round refused to add it. What the owner would see if it bites again: two phones holding the same day, and the one with the longer JSON silently deciding a field neither of them settled. (iii) LIVE, MONEY, NOT THIS DOC'S FILE — TWO CORE TICKETS ON THE SAME GAME BOTH SEAT ON A MERGE. `unionCore` dedupes by ticket ID only; the rule that forbids it, `CFB_RULES.oneLegPerGame` in `src/lib/cfb/rules.ts` (its own comment: "one leg per game per ticket, and no two core tickets share a game"), is CFB-only and the shared kernel never reads it. MEASURED THIS TURN, both orders identical and reproduced on a second run: one copy holding `cfb-2026-09-05-core-1` on game `g1` and the other holding `cfb-2026-09-05-core-9` on the SAME `g1` merge to TWO seated tickets over one game, $50 committed where the desk's own rule allows $25. WHAT THE OWNER WOULD SEE: a CFB card carrying both sides of one game, or the same game twice, after a sync between two devices that locked it differently — money the card's own rules say cannot be there. NOT FIXED, and it cannot be fixed by correction: the kernel is shared with MLB, where several tickets over one game are legitimate wagers, so teaching the merge to dedupe on the game key would silently DELETE real MLB money. RECOMMENDED AND NOT BUILT: hand `unionCore` the day's rule set — the object that already exists in `src/lib/cfb/rules.ts` — and dedupe on game only where `oneLegPerGame` is true. Until then the defect stands and is stated rather than hidden. (iv) CLOSED, WORDING — the two console warnings that asserted causes the code never checked are gone. Verified this turn by reading the emitted strings in `src/lib/ledger-merge.ts`: the stake warning's non-rival branch now says only that shared ids disagreed on stake and that the SMALLER stake stands (that fragment is byte-unchanged; measured this turn, three files besides the kernel carry it verbatim — `src/lib/cfb/store.ts`, `src/lib/cfb/types.ts` and `tests/ledger-merge.test.ts` — and NOT the two the kernel's own comment beside that warning names, which is itself a live false citation, recorded at (xv)), and the clause "with no topUp receipt" — false whenever a receipt WAS read and the raise was then overruled by the allotment — no longer appears in it; the cap-breach warning no longer ends with the sentence claiming a stored copy was already over cap before the merge, a claim about the INPUTS that nothing on the merge rail establishes. Both deleted clauses survive only inside dated comments recording the deletion, and `tests/cfb-card-ui.test.ts` independently forbids the over-cap wording in the UI note. (v) LIVE, LOW, NOT THIS DOC'S FILE — A REFUSAL IS NAMED BUT ITS REASON IS NOT. Read this turn: the marker the merge publishes as `stakeConflict` is `{ kept, refused }` and carries no reason code, and the warning beside it now states only what the code can prove. Three different refusals share that one channel — a rival lock holds the id, no receipt explains the difference, or the desk allotment had no room for the raise — and at warn time the function cannot tell them apart. WHAT THE OWNER WOULD SEE: the card says money was refused and cannot say which rule refused it. RECOMMENDED AND NOT BUILT: a reason field on that marker. It is one new field and it would have been the fourth marker added this round, which is exactly the pattern this round exists to stop. (vi) LIVE, LOW, NOT THIS DOC'S FILE — THE TOP-UP RECEIPT TEST IS PAIR-LOCAL. `receiptedPair` in `src/lib/ledger-merge.ts` compares ONE id's two copies and asks whether the stake difference is explained by the recorded top-up; nothing audits a day's top-ups against the block-fire record that minted them, whose own arithmetic lives in `decideTopUp` in `src/lib/server/blocks.ts` (read this turn: the day's owed is the allotment minus the recorded allocated sum). A cross-ticket audit is a new concept and was not built. WHAT THE OWNER WOULD SEE: a day whose individual raises each look receipted while the day as a whole was never entitled to them. (vii) NOT RE-LITIGATED, BY DESIGN — `capResidue` is deliberately untouched by the merge. Verified this turn: it is written only in `src/lib/server/lock-card.ts`, where it records what the per-ticket ceiling could not absorb IN ONE FIRE, and the kernel names it only to say it does not carry it. It is not cumulative and not a position, so merging it would invent money. (viii) NOT GUARDED BY A TEST — CITATION CURRENCY. Stated in full at (4) above: the one guard that existed for a single round was deleted by its own author, `ls tests/comment-citation.test.ts` answers `No such file or directory` this turn, and a grep for `comment-citation` over the tracked tree finds only this file. The class is handled by REVIEW and by the convention that a comment names a symbol and quotes an expression only under a dated retraction. Deciding whether a backticked fragment is a claim about today's source or a quoted corpse is a judgement about English prose, and every regex approximation of it fires on the correction notes while staying silent on the defects. (ix) CLOSED — the stale prose citations this bullet named last round have been corrected in their own files, and each was re-checked here this turn. In `tests/cfb-lock-route.test.ts` the three quotes (a fun-open line, a stake ternary and a guarded conflict write) now stand under an explicit dated correction that names `unionCore`, `cfbFunRefusedOf` and `stakeConflict` instead: the live arm reads `const funOpen = entry.funT.length === 0 && funRefused.length === 0;` in `src/lib/cfb/lock-server.ts`, and the string `stakeConflict` greps ZERO times in that file, which is what the surrounding claim turns on. In `app/api/cfb/lock/route.ts` the tier paragraph now names `stampOf` and cites the declaration `gradedAt?: number | null;` in `src/lib/cfb/types.ts`, where the entry type is built with a wrapper that closes the inherited index signature — both halves of the old claim were false and both are retracted in place. NOTHING SELF-REPORTED AND UNFIXED IS SHIPPING FROM THOSE TWO FILES. (x) OWNER DECISION, NOT A DEFECT, AND THE MOST CONSEQUENTIAL OPEN ITEM ON THIS PAGE — NOTHING IN THIS REPOSITORY SCHEDULES THE CFB LOCK. Executed this turn, twice, on the repo's own fixture: `CFB_LOCK` reads {"leadMs":3600000,"forwardTimeoutMs":25000}, so the lead is 60 minutes and the window is [first kickoff − 1h, first kickoff]; the 2026-09-05 fixture has 12 games, 72 priced rows and two distinct kickoffs, 16:00Z and 16:30Z, so the window is 15:00Z to 16:00Z. `vercel.json` carries exactly two crons, at 45 21 * * * and 0 0 * * *, and BOTH LAND AFTER THAT WINDOW HAS CLOSED. Driven at those two instants against the same fixture, the decision is lock with ahead 0 of 12 and priced-ahead 0, and the entry written is core 0 / $0, fun 0 / $0, noPlay true, note "NO-PLAY — lock window missed: every one of the 12 games on 2026-09-05 had kicked off before the server could lock. Nothing staked; no line that was already gone was priced." — byte-identical at both times. Against the same fixture at its own noon reading the day is waiting with a card of 3 core tickets at $75 and 1 fun parlay at $25, so on the pokes this repository actually schedules the slate deploys $0 of the $175. WHAT THE REPO CANNOT KNOW: whether an external cron-job.org entry in the owner's account pokes the scheduler inside the window. It cannot be verified from here, and `docs/cron-jobs.md` opens with a red banner recording that an earlier set of exactly such entries "WERE NEVER CREATED" while "the record carried them as live for ten days" — which is why no claim about a live external cron may be written here from inference. `tests/vercel-cron-pokes.test.ts` pins the two-cron Hobby ceiling, so adding a third Vercel cron is a TEST change, not a config change. THE OPTIONS, as the repo shows them: (a) create the cron-job.org entry and confirm it in the dashboard — the only place that truth lives — then record the confirmation here with its date; (b) repoint one of the two existing Vercel crons to a time inside the window, cheap but a fixed UTC time cannot track a first kickoff that moves week to week; (c) raise the cron count, which changes that pinned ceiling and is a plan question outside this repo; (d) add a GitHub Actions schedule, the one option this repository could verify itself — seven workflow files exist and a case-insensitive grep for cfb matches NONE of them; or (e) accept a manual lock, the owner opening the desk before the first kickoff, which is what happens today. Nothing here is recommended and nothing here is applied. (xi) OWNER DECISION, NOT A DEFECT — WIDENING THE CFB ALLOCATION RULES SO A THIN SLATE CAN DEPLOY THE FULL $150 HAS STILL NOT BEEN DECIDED. Read this turn, `CFB_RULES` is maxStake 25, minStake 5, tickets min 3 / max 7, maxLegs 2, maxDec 2.6, forcedMaxDec 1.75, forcedMinEvPct 0, minEvPct 2, oneLegPerGame true, kellyFrac 0.25, kellyCap 0.02, with the fun pool at legs 3-5, minDec 4, maxDec 40, minEvPct −3; `CFB_PAPER` is {"since":"2026-09-05","daily":150,"fun":25}. The measured consequence on the shipped fixture is the case for putting it to the owner: at the lock the fun allotment deploys in full and the core deploys $75 of $150, and the card says so itself — "$75 of the $150 stayed undeployed — every ticket is at the $25 max and no other game offers a side ≥ 0% EV under 1.75." Each of those four rails is a deliberate risk control, so loosening any one of them is a bankroll decision and not a code cleanup. (xii) VERIFIED, STEADY-STATE COST PER CFB DATE — the game-lines half is ONE Odds API call per pricing pass, `oddsPayload` in `src/lib/cfb/slate-server.ts`, whose URL carries regions us,eu and markets h2h,spreads,totals: the 6-credit shape the props docblock in `src/lib/cfb/rules.ts` records measuring on prod. The response is cached for `export const ODDS_TTL = 240;` seconds, and the settle pass buys NOTHING — `finalsFromEspn` builds its board with `oddsEvents: []`, the only occurrence of that form in the file. Player props are the expensive half and are capped separately: the same docblock records about 31 credits per event measured on prod, not the 6 the endpoint's own note implies. (xiii) VERIFIED, CAPABILITY GAP — the MLB desk still has surfaces CFB does not. Counted this turn: 26 API route files in `app/api`, of which 4 are CFB (`app/api/cfb/route.ts`, `lock`, `ledger`, `props`) and 22 are not, and CFB has no counterpart to `app/api/clv/route.ts`, `app/api/calibrate/route.ts`, `app/api/calibration/route.ts`, `app/api/predictions/route.ts`, `app/api/sharp/route.ts`, `app/api/digest/route.ts`, `app/api/propsnap/route.ts` or `app/api/stats/route.ts`. Of the seven files in `.github/workflows`, a case-insensitive grep for cfb matches none, so nothing scheduled archives a CFB board, captures CFB line history or stores CFB props history — all three of which exist for MLB. The string `clv` appears in only three CFB files (`src/lib/cfb/store.ts`, `src/lib/cfb/types.ts`, `app/api/cfb/lock/route.ts`), so closing-line value is recorded on the CFB ledger with no route, no archive and no report behind it. (xiv) VERIFIED, HOUSEKEEPING, NOT CFB — the repository tracks numbered duplicate files. Measured this turn: 16 tracked paths whose name ends in a space and a digit before the extension, and every one of the 16 is BYTE-IDENTICAL to its live twin, so today they vouch for nothing the live file does not. The hole is latent rather than live, and one of them is a test copy that the suite's own include pattern cannot match, so it would never run if it did diverge. They belong to whoever owns those directories and are simply not deleted here. (xv) CLOSED THIS TURN, CITATION — THE TWO FALSE CITATIONS THIS BULLET RAISED HAVE BEEN CORRECTED IN THEIR OWN FILES. They were found by the verification pass, reported rather than shipped silently, and then fixed in place before the commit; both were prose inside comments and neither changed behaviour. First, the comment block above the stake warning in `src/lib/ledger-merge.ts` claimed the receiptless wording was left byte-unchanged because three files outside the kernel quote it verbatim, and named two that carry it ZERO times. That justification was false and is gone: the wording is now kept for the only reason that holds — it is still exactly what that branch does — and the block states that nothing outside the kernel quotes this warn string and no test asserts it by value. Re-measured after the correction: `disagreed on stake` occurs outside the kernel in exactly four files (`src/lib/cfb/store.ts`, `src/lib/cfb/types.ts`, `src/components/cfb/CfbLedger.tsx`, `tests/cfb-card-ui.test.ts`) and every one carries the CARD'S note or this field's superseded doc, each framed in the past tense as retracted; the phrase `the SMALLER stake stands` additionally appears in two `tests/ledger-merge.test.ts` test NAMES, which describe the rule and assert no string. Second, `src/lib/cfb/store.ts` quoted the kernel's stake warning under a measured-this-turn warrant INCLUDING the clause "with no topUp receipt" that the kernel deleted this round, and beside a stale test count; the quote is replaced by a description naming the emitter, and the count is re-measured (`tests/cfb-store.test.ts` reports "Tests 20 passed (20)", not the 16 it claimed). LINE NUMBERS AND QUOTED EXPRESSIONS WERE DELIBERATELY OMITTED from both corrections: they are what goes stale, and this class has recurred across eight rounds for that reason. The convention that survives is the one at (viii) — name the symbol, and quote an expression only under a dated retraction. (xvi) VERIFIED, THIS FILE — the line numbers throughout this bullet have drifted and are not to be trusted; read the symbol names instead. Every symbol named in this limits list was grepped one at a time this turn and every one resolves. This bullet is also very long, which is itself a limit: it is the accreted record of many rounds and it is read from the end. THE ONE-LINE SUMMARY FOR THE OWNER, if nothing else here is read: the money is configured exactly as asked, $150 core and $25 fun per CFB day; on the repo's own slate the rules seat $100 of it at the lock; and nothing inside this repository schedules that lock, so the day is locked when somebody opens the desk. INSTRUCTION 48 (2026-09-09) then made the locked card grow all day — `CFB_TOPUP_MAX` 2 → 6 per arm and an append-only assertion in `applyTopUp` / `topUpDate` (`src/lib/append-only.ts`); see `docs/session-handoff.md`.
- Live window (INSTRUCTION 40, 2026-09-05): `CFB_PROPS.liveRevalidateSec` (600) is the cache window whenever any event actually being priced is in play — it drives the per-event data-cache `revalidate`, the Redis `SET EX`, and the stored-board staleness check (`boardWindowSec` in `props-store.ts`, falling back to `revalidateSec` when the value is missing or malformed); otherwise the window stays `revalidateSec` (7200). The board (`CfbPropsBoard`) carries `live` (in-play events priced) and `ttlSec` (the window it was written under); the Builder and the Board read `ttlSec` for their query `staleTime` and the Builder footnote prints "cached 10 min · N in play" off it. Live rows keep status `live` (the LIVE / MIXED parlay views read that) but `playable` and Kelly stay pre-kick only. The budget gate is unchanged — a live slate re-prices at most every 10 min until the day's credits are spent.
- Review fixes (2026-09-05, after INSTRUCTION 40): (1) the stored board is fresh only inside min(its own `ttlSec`, the CURRENT slate's window) — `boardFresh` in `props-store.ts` — so a pre-kick board (7200) stops being honoured 600 s after a game inside it kicks off; the first request after kickoff re-prices. (2) A live pull re-prices ONLY what moved: the in-play events (at most `CFB_PROPS.liveMaxEvents` = 6, live first) plus any upcoming game the stored board has no rows for; every other upcoming game's rows are carried over from the stored board while it is inside `revalidateSec` (the board names its games in `priced`). Carried rows adopt the CURRENT slate's status (same-day follow-up): a game that kicked off since its rows were priced is reported `status: "live"`, `playable: false`, so the Board never runs a pre-kick Kelly on an in-play line. A full 12-event re-price every 10 min would have spent the 1200-credit day in four pulls. (3) The board is written with `EX CFB_PROPS.boardRetainSec` (36 h) — retained past its window — and when the daily budget refuses a pull the route serves the carried rows flagged `stale: true` with the original `generatedAt` (source `redis`, note "showing the last priced lines") instead of `rows: []`; the Builder and Board footnotes print "N in-play games show lines as priced at H:MM", adding "— today's props budget is used up" only when `budgeted` is true (same-day follow-up: the first deploy blamed the budget for every stale board; prod had spent 92 credits). (4) The client `staleTime` is what is LEFT of the window (`cfbPropsStaleMs` in `client.ts`: `ttlSec` less the board's age), not the whole window from the fetch; `cfbCacheLabel` is shared so the Builder and the Board footnotes can never disagree.
- THE CAESARS-MISSING RULE (2026-09-05, Josh: "They are still 12 games today that haven't started w/ current Anytime TD odds"; read on prod ~15:45 PT: 11 upcoming games carried DK / FD anytime-TD rows and no Caesars quote on any row while Caesars was posting them — priced once before Caesars posted, then ridden for the whole 2 h carry): `CFB_PROPS.czMissingRevalidateSec` (1800) and `czMissingWindowSec` (4 h). An UPCOMING game on the stored board that HAS rows and, on at least one MARKET with rows, carries no Caesars quote (`czMissingGameIds` in `props.ts` — keyed on the market, so Caesars yardage props without a Caesars anytime TD still re-check), with kickoff inside 4 h, is re-fetched once its own `pricedAt` is older than 30 min (`czMissingDue` in `props-store.ts`); outside 4 h the 2 h carry stands, live games keep their 10-min rule. A game with ZERO rows is never Caesars-missing — it stays on the 2 h empty-event hold, upcoming or live (review fix: 29 of the 46 priced games that day were FBS-vs-FCS games with no props at any book; re-asking them every 30 min would have wanted ~7,200 credits against the 2,500 rail). The check runs before the Redis "fresh board" answer (a fresh 2 h board would otherwise hide it) — and skipping that answer does not re-queue the live games: a live game with rows is fetched only once its own `pricedAt` is older than `liveRevalidateSec` (review fix; it used to re-queue every live game the moment any upcoming game turned due). The pull's need is ordered live → never priced → Caesars-missing → 2 h-expired, so a tight budget buys the cheapest wins first (which also means a busy live afternoon can starve the re-checks — the rail binds). Next's data cache is stale-while-revalidate (a stale entry answers with the OLD body and refreshes in the background), so a Caesars-missing re-pull and a live re-pull of a game already on the board are sent `cache: "no-store"`; first pulls and 2 h-expired pulls keep `next.revalidate` at the pull's window. A re-pull that fails (non-2xx / network) keeps the game's stored rows and old `pricedAt` on the answer and the written board. The board carries `czMissing` and `noProps`, counted by `propsCoverage` over priced games only; while `czMissing` > 0 the board's `ttlSec` is min(window, 1800) so the phone's own staleTime follows the 30-min rule (nothing else calls the route). Both footnotes lead with "priced N of M games" where N = `fetched − noProps` (zero-row games are not "priced"), then "K games post player props at other books but no Caesars line yet — re-checked every 30 min inside 4 h of kickoff" and "M games on the slate have no player props posted at the books we price", each only when > 0, the figures off the constants. A refused game with no stored rows no longer flags the board stale. Cost, honestly: 8 re-checks × 31 = 248 credits per game worst case, but the 12 such games Josh named would want 12 × 248 = 2,976 — more than the daily rail; the rail refuses past 2,500 and the refused games keep their last priced rows.
- Picks (`src/lib/cfb/picks.ts`, `buildCfbPicks`): categories all / ml / spread / total / the six prop markets, ranked grade S → F, then EV at Caesars descending (null EV sorts last), then probability. Parlay leg pool = Caesars-priced rows with EV ≥ `CFB_PARLAYS.minLegEvPct` (−3). Tiers under `CFB_PARLAYS`: SAFER 2–3 legs, each ≥ 58 % to hit, decimal ≤ 3.5, ranked by combined probability, drawn from sides AND props (decision 2026-09-05: Caesars posts no moneyline on the big favourites, so a sides-only SAFER was empty on the opening slate; the 66–79 % anytime-TD favourites now qualify); LONGSHOT 4–6 legs, decimal 8–60, seeded from the likeliest legs and filled by EV; MIX 3–5 legs with at least one side and one prop, decimal 3–20. Views: PARLAYS (upcoming legs), MIXED (upcoming + live legs), LIVE (in-play legs only); at most 2 legs per game, never two legs on one player, never both sides of a market, 6 tickets per tier per view. A ticket's American price is derived from the rounded decimal the card shows, so the two never disagree on a half-cent edge. INSTRUCTION 42 (2026-09-05): categories admit LIVE rows too (sides from in-play games, props whose slate game is live), graded the same way, Kelly null, `playable` false, tagged LIVE in the table; `CfbPicks.liveRows` counts them. Twelve parlay sets (`CfbPicks.sets`, keys `CFB_PARLAY_CATEGORIES`: ml, spread, total, the six prop markets, combo, mixed, live), each up to `CFB_PARLAYS.perCategory` (50) distinct tickets: single-market sets are pregame legs all from that market, 2–6 legs, one leg per game, decimal 1.5–60; combo = pregame side + prop, 3–6 legs, decimal 2–60; mixed = at least one live and one pregame leg; live = in-play legs only. The 50 are chosen round-robin across leg counts (2..6, each bucket in EV order) so a set is not fifty six-leg tickets, then sorted by EV. `legFits` rejects a second leg on the same market of one game for props as well as sides (review fix). Benchmark pinned in `tests/cfb-picks.test.ts`: 68 games / 2,992 prop rows built in ~45 ms. TIERED LEG POOL (2026-09-05, Josh: "only showing 4 Anytime TD parlays … should be showing 50+"): a single-market set builds from tier 1 (EV ≥ −3) first and, when short of 50, extends to tier 2 — Caesars-priced upcoming legs of that market down to `CFB_PARLAYS.setFloorEvPct` (−12) — via the seeded fills and then a bounded exhaustive walk; every-leg-tier-1 tickets rank first (by EV), then the rest by EV, and each `CfbParlay` carries `gated` (true = every leg cleared −3). `CFB_PARLAYS.setBands.anytime_td` = 2–4 legs, decimal 4–250 (ATD legs price 3–8 each, so the shared 60 cap forbade a third leg); other markets keep 2–6 / 1.5–60. Seeds are now the 60 likeliest AND the 60 highest-EV legs. Legacy tiers, combo, mixed and live stay tier 1 only. Benchmark ~70 ms.
- Builder: `CfbProps` has a market nav (SIDES · ANYTIME TD · PASS TDS · PASS YDS · RECEPTIONS · RUSH YDS · REC YDS); a prop leg reaches the slip only when the row has a fair price and Caesars' line equals the consensus line (`propLegOf`), so the slip never prices one line while showing another.

## Freeze fix (INSTRUCTION 39)
- Root cause, the same as Scramble Lab's: a history-push navigation arms iOS Safari's edge-swipe-back recognizer, which wedges taps until the user swipes back; per-item `backdrop-filter` over the motion video freezes the page.
- Every internal `<Link>` is `replace` (shell brand, rail, phone icons, tab bar, landing chooser, game cards, the UFC builder); nothing pushes history, so there is nothing to swipe back to. `html, body { touch-action: manipulation }`. Backdrop blur stays only on shell chrome (rail, phone header, tab bar, the sticky market bars, the slips, the player-sheet body); per-item surfaces (`.glass`, `.liquid-glass`, `.ticket`, `.stat-tile`, table headers, tooltips, the player-sheet overlay) tint themselves instead. `app/template.tsx` is opacity-only, 0.45 s.
- Guard: `tests/nav-flat.test.ts` (no `href` push, no `router.push`, the touch-action rule, no blur on the per-item classes). Observed red first.

## Quota (INSTRUCTION 39)
- Measured 2026-09-05 from the Odds API headers on the first prod props pull: 24 events cost 753 credits (`x-requests-used` 2428 → 3187), about 31 credits per event — a slate pull costs 6.
- `CFB_PROPS` (`src/lib/cfb/rules.ts`, INSTRUCTION 42 values since 2026-09-05 — `maxEvents` 60 (was 12), `liveMaxEvents` 24 (was 6), `dailyBudget` 2500 (was 1200); the rest of this bullet describes the earlier shape): `maxEvents` 12, `revalidateSec` 7200, `liveRevalidateSec` 600 (2026-09-05, the window while any priced game is live), `dailyBudget` 1200, `measuredCreditsPerEvent` 31. `src/lib/cfb/props-store.ts` wraps the shared Redis client: the priced board is stored at `pl:cfb:props:v1:<date>` for `boardRetainSec` (36 h — retained past its window so the budget-out answer can serve it stale; freshness is `boardFresh`, capped by the current slate's window), the day's spend at `pl:cfb:props:spend:v1:<ptDate>` (36 h). Order in the route: Redis read (fresh → answer) → what needs pricing (every selected live event — the selector already holds them to `liveMaxEvents` — + upcoming games not carried) → budget gate (`allowed = floor((dailyBudget − spent) / 31)`) → fetch the allowed events → merge with the carried rows → tally the real `x-requests-used` delta → write back; nothing affordable → the stored rows, `stale: true`. The body reports `source` (`redis` / `fetch` / `none`), `budgeted`, `stale`, `priced` and `spentToday`; without the store env the route runs on the Next data cache alone and `spentToday` is `null`. Worst case without the budget: 12 × 31 × 12 pulls a day = 4,464 credits pre-kick, plus ≤ 6 × 31 per 10-min live pull; with it, 1,200. The footnotes ("cached 2 h", "today's props budget is used up") derive the hours from the constant.
- Tests: `tests/cfb-props-route.test.ts` (in-memory fake Redis, fetch served from the synthetic fixture, clock pinned), `tests/cfb-props.test.ts`.

## Tests and fixtures
- `tests/cfb-*.test.ts` `it(` blocks, grep-counted 2026-09-05: normal 7, names 12, model 22, card 12, grade 13, store 16, route 20, separation 20, props 24, picks 20, props-ui 24, props-route 17; plus `tests/nav-flat.test.ts` (freeze guard). Synthetic props fixture `tests/fixtures/cfb/odds-ncaaf-event-props.synthetic.json` (ECU@ALA, 4 books, 6 markets, 6 players — synthetic, not a capture).
- Fixtures in `tests/fixtures/cfb/`: `espn-scoreboard-2026-09-05.json` (12 games), `odds-ncaaf-2026-09-05.json` (14 events), `espn-fpi.json` (138 teams) — fixture captures cut from the raw feeds on 2026-09-04 and 2026-09-05. Fixture board: 0 unmatched; Caesars posts no moneyline on ALA / IU / OSU / ARMY / SYR / NEB in the fixture, so those ML rows are unplayable by design (prod shows the same "no CZ" rows).

## Known behaviours (not defects)
- Local dev has no `ODDS_API_KEY`: `/api/cfb` returns `oddsMissing: true` and the desk shows the scores-only fallback. Verify prices on prod.
- Local dev `/games` (both desks) can sit on the Suspense placeholder in the in-app browser while the SSR stream is complete; prod hydrates (walked 2026-09-05). Recorded in `progress.md`.
- "no CZ" moneyline rows show the fair price but no grade and no Kelly.
- A fresh slate odds pull costs 6 credits and `/api/cfb` caches for 240 s; a props pull costs about 31 credits per event and is capped, cached in Redis for 2 h pre-kick / 10 min while any priced game is live, and budgeted (see Quota).

## Headshots, own-team logos, Season Lab (INSTRUCTION 46, 2026-09-08)
- `props-context.ts` keeps `athleteId / headshot / teamId / teamAbbr / pos` per ESPN byathlete row; `props.ts parseEventProps` adopts them only when the ESPN teamId is one of the game's two teams (a same-name player on another roster gets nothing). `card.ts legOf` copies player/headshot/pos/teamId onto `CfbTicketLeg`.
- `TeamMark.tsx PlayerMark`: headshot (ESPN combiner URL) over the player's own team logo; `PairMark` is for game-level picks (spread/total/ML) or the no-team fallback only.
- Ledger deep link: `/props?cfb=1&game=<id>&mkt=<key>&player=<name>` — `app/props/page.tsx` flips the sport to CFB on `cfb=1`; `CfbProps` reads the params.
- Season Lab: `src/lib/cfb/season.ts` (`CFB_SEASON` kellyBank 250, ticketDefault 5, ticketMax 25, sameTeamHaircut 0.9; `paceOf` on pace / behind / cleared / dead), `season-store.ts` (`pl_cfb_season`), `CfbSeason.tsx`, `app/season/page.tsx`, `app/api/cfb/season/route.ts` (ESPN byathlete + FPI only — the Odds API has no NCAAF season markets, lines are typed by hand). Nav entry `/season` is `cfbOnly` and hidden while the switch is on MLB.

## Allocation widened to $250 / $50 max / 3–10 tickets, and the engine goes shared (2026-09-08)
Josh, verbatim: "1. Widen the CFB allocation to $250  2. NFL needs to be built NOW  3. Allocation should be set to $350". Item 1 is this section; items 2–3 are `docs/nfl-desk.md`.
- `src/lib/cfb/rules.ts`: `CFB_PAPER.daily` 150 → **250** (`:37`; `fun` stays 25, `since` stays 2026-09-05), `CFB_RULES.maxStake` 25 → **50** (`:50` — kellyCap 0.02 × CFB_BANK_BASE 2500 = $50, so the Kelly ceiling at the base bankroll IS the stake cap), `CFB_RULES.tickets` {3,7} → **{3,10}** (`:53`; 10 × $50 = $500 ≥ $250, so a day can always deploy in full). Every pin, string and doc that said $150 / $25 max / 3–7 for CFB moved with them; the dated narratives above (INSTRUCTION 39/40/46 paragraphs quoting "$75 of the $150", "$25 max", "7-ticket cap") are HISTORY measured under the old constants and are left as written. Entries already stored keep their own recorded `daily` — the ledger never re-derives an old day's allotment from today's constant (the merge kernel's cap is per entry, from a `DESK_PAPER` table keyed on the entry's `sport`: mlb `PAPER`, cfb `CFB_PAPER`, nfl `NFL_PAPER`, `src/lib/ledger-merge.ts`).
- Config invariants pinned for BOTH leagues in `tests/nfl-config.test.ts` and enforced at runtime by `assertLeagueConfig` (`src/lib/football/league.ts`): `kellyCap × bankBase === maxStake`; `tickets.max × maxStake ≥ daily`; `idPrefix === id`.
- The engine is now shared: `CFB_LEAGUE: LeagueConfig` is built at the bottom of `src/lib/cfb/rules.ts` FROM the existing `CFB_*` constants (the same objects, so every literal-regex pin on that file still matches). `src/components/cfb/*` are the shared football surfaces (paths and exports unchanged), reading league specifics through `useLeague()` (`src/components/football/LeagueContext.tsx`, default `CFB_DESK` from `src/lib/cfb/desk.ts`); `src/lib/cfb/store.ts` is `makeDeviceStore(CFB_LEAGUE)` re-exported under today's names; the CFB lock route and props route are thin shells over `src/lib/server/football-lock.ts` / `football-props.ts` with `CFB_LEAGUE` as a REQUIRED first argument. `fpiIndex` reads its columns BY NAME (`fpi`, `fpirank`) from `categories[].names` — positional before, which happened to be right for FBS and wrong for the NFL. `matchOddsEvent` takes the league's `matchWindowMs` as a fourth argument (both 3 h).
- The Stats page's NCAAF pill, Season Lab (`/season`) and the `cfbOnly` nav gate are unchanged and remain CFB-only.
